---
name: benchmark-models
preamble-tier: 2
version: 1.0.0
description: |
  跨多个 AI 模型运行基准测试。当被要求"跨模型基准"
  或"模型基准"时使用。(gstack)
allowed-tools: [Bash, Read, Write, Edit, Grep, Glob, AskUserQuestion]
triggers:
  - benchmark models
  - cross-model benchmark
  - model comparison
---

{{PREAMBLE}}

# /benchmark-models — 跨模型基准测试

## 0. 模型选择

AskUserQuestion："你想将哪些模型包含在基准测试中？
A) Claude 3.5 Sonnet vs Claude 3 Opus（默认）
B) Claude vs GPT-4o vs Gemini
C) 让我选择特定模型"

**停止。** 在用户响应之前不要继续。

## 1. 基准任务选择

AskUserQuestion："什么类型的任务？
A) 代码生成（实现算法、API 端点）
B) 代码审查（查找 bug、改进）
C) 文档（从代码生成文档）
D) 全部 3 种类型"

## 2. 基准执行

对于每个模型 + 任务组合：
1. 提供相同的提示
2. 测量响应时间
3. 评估输出质量（正确性、完整性、风格）
4. 记录 token 成本

```bash
mkdir -p benchmark-results/models/
```

## 3. 结果比较

生成比较表：
```
+====================================================================+
|                跨模型基准结果                                  |
+====================================================================+
| 任务            | 模型       | 时间   | 质量  | 成本  |
|-----------------|------------|--------|-------|-------|
| [任务]      | [模型]     | Xs     | N/10  | $X    |
+--------------------------------------------------------------------+
```

AskUserQuestion："想要更深入的任务或特定模型分析吗？
A) 不，这些结果很好
B) 更深入分析 [模型/任务]
C) 添加更多模型"

## 4. 完成摘要

```
+====================================================================+
|                    基准模型摘要                                |
+====================================================================+
| 最佳整体         | [模型] — [原因]                           |
| 最佳价值      | [模型] — [原因]                           |
| 最佳速度      | [模型] — [原因]                           |
+====================================================================+
```

"基准结果在 benchmark-results/models/ 中。比较每个模型的性能。"
