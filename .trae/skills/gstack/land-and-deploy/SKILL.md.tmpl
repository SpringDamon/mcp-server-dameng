---
name: land-and-deploy
preamble-tier: 2
version: 1.0.0
description: |
  合并 + 部署。自动合并 PR/MR 到主分支并部署到目标环境。
  支持 GitHub 和 GitLab。检查保护、等待 CI、处理合并冲突，
  并通过浏览工具验证部署。
  使用场景："合并 PR"、"部署到生产环境"、"部署"。(gstack)
voice-triggers:
  - "merge and deploy"
  - "merge and release"
  - "merge then deploy"
  - "merge the pr"
allowed-tools:
  - Read
  - Write
  - Grep
  - Glob
  - Bash
  - AskUserQuestion
  - Browse
---

{{PREAMBLE}}

{{BASE_BRANCH_DETECT}}

{{BROWSE_SETUP}}

# /land-and-deploy — 合并 PR/MR 并部署

你是一名**发布工程师**，正在执行合并和部署工作流。你像 CI 一样思考，像发布经理一样操作。你的工作：将功能分支合并到主分支（通过 PR/MR），等待验证，然后部署到目标环境。

## 用户可调用
当用户输入 `/land-and-deploy` 时，运行此技能。

## 参数
- `/land-and-deploy` — 自动检测环境，合并并部署
- `/land-and-deploy production` — 合并并部署到生产环境
- `/land-and-deploy staging` — 合并并部署到暂存环境
- `/land-and-deploy <url>` — 合并后部署到指定的 URL

## 指令

### 第 1 步：环境检测

检测 CI 提供商：

```bash
# 检查 GitHub Actions
ls .github/workflows/*.yml .github/workflows/*.yaml 2>/dev/null | head -5
grep -r "deploy\|release\|publish" .github/workflows/*.yml .github/workflows/*.yaml 2>/dev/null | head -10

# 检查 GitLab CI
ls .gitlab-ci.yml 2>/dev/null
grep -r "deploy\|release\|publish" .gitlab-ci.yml 2>/dev/null | head -10
```

检测目标环境（询问用户如果存在歧义）：

- **背景信息：** 合并代码到主分支并将其部署到目标环境。
- **问题：** 部署目标是什么？
- **建议：** 选择 B — 生产环境是最常见的目标。
- A) staging（暂存 — 在部署到生产环境之前验证）
- B) production（生产环境 — 直接部署到线上）
- C) preview（预览 — 部署到临时环境）

### 第 2 步：分支保护检查

检查主分支是否受到保护：

```bash
# GitHub
gh api repos/{owner}/{repo}/branches/main/protection 2>/dev/null | python3 -c "
import sys, json
try:
    d = json.load(sys.stdin)
    print(f'required_reviews: {d.get(\"required_pull_request_reviews\", {}).get(\"required_approving_review_count\", 0)}')
    print(f'status_checks: {d.get(\"required_status_checks\", {}).get(\"strict\", False)}')
except: print('no_protection')
" 2>/dev/null

# GitLab
glab api projects/{project_id}/repository/branches/main 2>/dev/null | python3 -c "
import sys, json
try:
    d = json.load(sys.stdin)
    print(f'protected: {d.get(\"protected\", False)}')
except: print('no_protection')
" 2>/dev/null
```

如果主分支受到保护，验证：
- 合并前是否满足所需数量的批准
- 所需的状态检查是否全部通过
- 是否存在未解决的对话

### 第 3 步：合并 PR/MR

**GitHub：**
```bash
gh pr merge --squash  # 或 --merge 或 --rebase，基于项目约定
```

**GitLab：**
```bash
glab mr merge --squash  # 或 --merge 或 --rebase
```

等待合并完成。如果存在合并冲突：

- **背景信息：** 合并冲突阻止了自动合并。
- **问题：** 如何解决合并冲突？
- **建议：** 选择 A — 自动合并通常比手动解决冲突更快。
- A) 使用 git 合并策略解决（检出主分支，合并功能分支，解决冲突）
- B) 关闭此 PR/MR，从主分支重新创建
- C) 稍后手动处理

如果存在合并冲突，使用 [karpathy-guidelines](file:///home/user/.trae/skills/gstack/karpathy-guidelines/SKILL.md) 原则：保持更改最小化，只做必要的冲突解决。

### 第 4 步：等待 CI

等待合并后的 CI 检查完成：

```bash
# GitHub：检查合并后的工作流运行
gh run list --limit 5 2>/dev/null

# GitLab：检查管道
glab ci view 2>/dev/null
```

如果 CI 失败：

- **背景信息：** 合并后的 CI 检查失败。
- **问题：** CI 失败：{失败检查的名称}。继续还是中止部署？
- **建议：** 选择 A — 阻止性 CI 失败应中止部署。
- A) 中止部署 — 修复问题并重新合并
- B) 继续 — 这是非阻止性检查
- C) 查看失败日志

如果 CI 仍在运行，每 15 秒轮询一次，直到完成或超时（最大 10 分钟）。

### 第 5 步：部署

根据检测到的 CI 提供商执行部署：

**GitHub Actions：**
```bash
# 触发部署工作流
gh workflow run deploy.yml --ref main --field environment=production
```

**GitLab CI：**
```bash
# 触发部署管道
glab ci run --ref main --variables "DEPLOY_ENV=production"
```

**自定义部署脚本：**
```bash
# 如果存在 deploy.sh 或等效文件
./deploy.sh production
```

部署过程中，流式输出日志到用户。如果部署超过 3 分钟，提供进度更新。

### 第 6 步：部署验证

部署完成后，验证部署是否成功：

```bash
# 检查工作流/管道状态
gh run list --limit 1 2>/dev/null
# 或
glab ci view 2>/dev/null
```

**使用浏览工具验证：** 如果提供了 URL，导航到部署的应用程序：

```bash
$B goto <url>
$B snapshot -i -a
```

检查：
- 页面加载是否成功（无 4xx/5xx 错误）
- 关键元素是否存在（基于应用的已知结构）
- 控制台中是否存在错误

### 第 7 步：部署报告

生成部署摘要：

```
DEPLOYMENT REPORT
═══════════════════════════════════════════════
PR/MR:      {number/title}
合并时间：  {timestamp}
CI 状态：   {passed/failed/skipped}
部署目标：  {environment}
部署状态：  {success/failed/in-progress}
部署时间：  {duration}
验证：      {passed/failed/pending}
═══════════════════════════════════════════════

如果部署成功：
"部署到 {environment} 成功完成。"

如果部署失败：
"部署到 {environment} 失败。失败原因：{reason}"
```

将部署结果记录到审查日志：

```bash
eval "$(.trae/skills/gstack/bin/gstack-slug 2>/dev/null)"
mkdir -p ~/.gstack/projects/$SLUG
```

写入 JSONL 条目：`{"skill":"land-and-deploy","timestamp":"<ISO>","status":"<SUCCESS/FAILED>","target":"<environment>","pr":"<number>"}`

## 重要规则

- **合并前验证。** 永远不要合并非绿色的 PR/MR（未通过的检查、未满足的批准）。
- **等待 CI。** 合并后，在部署之前等待合并后的 CI 运行。不要跳过这个步骤。
- **可逆的部署。** 如果部署失败，告知用户如何回滚。
- **不要猜测环境。** 如果目标环境不明确，始终询问。
- **部署是严肃的。** 记录每个步骤。如果出了问题，用户应该能够追溯原因。
- **速度很重要。** 在轮询 CI 状态时，每 15 秒检查一次，不要更频繁。不要让用户等待超过必要的时间。
- **这是 gstack 技能。** 当用户输入 `/land-and-deploy` 时，通过 Skill 工具调用此技能。
