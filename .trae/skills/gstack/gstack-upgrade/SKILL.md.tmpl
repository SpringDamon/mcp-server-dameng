---
name: gstack-upgrade
version: 1.1.0
description: |
  将 gstack 升级到最新版本。自动检测全局安装与本地 vendored 安装，
  执行升级并展示更新内容。当被要求“升级 gstack”、“更新 gstack”
  或“获取最新版本”时使用。
voice-triggers:
  - "upgrade the tools"
  - "update the tools"
  - "gee stack upgrade"
  - "g stack upgrade"
triggers:
  - upgrade gstack
  - update gstack version
  - get latest gstack
allowed-tools:
  - Bash
  - Read
  - Write
  - AskUserQuestion
---

# /gstack-upgrade

将 gstack 升级到最新版本并展示更新内容。

## 内联升级流程

本部分内容被所有技能前言引用，用于检测 `UPGRADE_AVAILABLE` 时的处理逻辑。

### 步骤 1：询问用户（或自动升级）

首先，检查是否启用了自动升级：
```bash
_AUTO=""
[ "${GSTACK_AUTO_UPGRADE:-}" = "1" ] && _AUTO="true"
[ -z "$_AUTO" ] && _AUTO=$(.trae/skills/gstack/bin/gstack-config get auto_upgrade 2>/dev/null || true)
echo "AUTO_UPGRADE=$_AUTO"
```

**如果 `AUTO_UPGRADE=true` 或 `AUTO_UPGRADE=1`：** 跳过 AskUserQuestion。记录日志“自动升级 gstack v{old} → v{new}...”并直接进入步骤 2。如果自动升级期间 `./setup` 失败，从备份（`.bak` 目录）恢复并警告用户：“自动升级失败 — 已恢复之前版本。请手动运行 `/gstack-upgrade` 重试。”

**否则**，使用 AskUserQuestion：
- 问题："gstack **v{new}** 可用（当前版本为 v{old}）。是否立即升级？"
- 选项：["是的，立即升级", "始终自动升级", "暂不升级", "不再询问"]

**如果选择“是的，立即升级”：** 进入步骤 2。

**如果选择“始终自动升级”：**
```bash
.trae/skills/gstack/bin/gstack-config set auto_upgrade true
```
告知用户：“已启用自动升级。未来的更新将自动安装。”然后进入步骤 2。

**如果选择“暂不升级”：** 写入休眠状态，采用递增退避策略（首次休眠 24 小时，第二次 48 小时，第三次及以后 1 周），然后继续当前技能操作。不再提及升级事宜。
```bash
_SNOOZE_FILE="$HOME/.gstack/update-snoozed"
_REMOTE_VER="{new}"
_CUR_LEVEL=0
if [ -f "$_SNOOZE_FILE" ]; then
  _SNOOZED_VER=$(awk '{print $1}' "$_SNOOZE_FILE")
  if [ "$_SNOOZED_VER" = "$_REMOTE_VER" ]; then
    _CUR_LEVEL=$(awk '{print $2}' "$_SNOOZE_FILE")
    case "$_CUR_LEVEL" in *[!0-9]*) _CUR_LEVEL=0 ;; esac
  fi
fi
_NEW_LEVEL=$((_CUR_LEVEL + 1))
[ "$_NEW_LEVEL" -gt 3 ] && _NEW_LEVEL=3
echo "$_REMOTE_VER $_NEW_LEVEL $(date +%s)" > "$_SNOOZE_FILE"
```
注意：`{new}` 是来自 `UPGRADE_AVAILABLE` 输出的远程版本号 — 需从更新检查结果中替换。

告知用户休眠时长：“下次提醒将在 24 小时后”（或 48 小时或 1 周，取决于等级）。提示：“在 `~/.gstack/config.yaml` 中设置 `auto_upgrade: true` 可启用自动升级。”

**如果选择“不再询问”：**
```bash
.trae/skills/gstack/bin/gstack-config set update_check false
```
告知用户：“已禁用更新检查。运行 `.trae/skills/gstack/bin/gstack-config set update_check true` 可重新启用。”
继续当前技能操作。

### 步骤 2：检测安装类型

```bash
if [ -d "$HOME/.trae/skills/gstack/.git" ]; then
  INSTALL_TYPE="global-git"
  INSTALL_DIR="$HOME/.trae/skills/gstack"
elif [ -d "$HOME/.gstack/repos/gstack/.git" ]; then
  INSTALL_TYPE="global-git"
  INSTALL_DIR="$HOME/.gstack/repos/gstack"
elif [ -d ".trae/skills/gstack/.git" ]; then
  INSTALL_TYPE="local-git"
  INSTALL_DIR=".trae/skills/gstack"
elif [ -d ".agents/skills/gstack/.git" ]; then
  INSTALL_TYPE="local-git"
  INSTALL_DIR=".agents/skills/gstack"
elif [ -d ".trae/skills/gstack" ]; then
  INSTALL_TYPE="vendored"
  INSTALL_DIR=".trae/skills/gstack"
elif [ -d "$HOME/.trae/skills/gstack" ]; then
  INSTALL_TYPE="vendored-global"
  INSTALL_DIR="$HOME/.trae/skills/gstack"
else
  echo "ERROR: gstack not found"
  exit 1
fi
echo "Install type: $INSTALL_TYPE at $INSTALL_DIR"
```

上述输出的安装类型和目录路径将在后续所有步骤中使用。

### 步骤 3：保存旧版本号

使用步骤 2 输出的安装目录：

```bash
OLD_VERSION=$(cat "$INSTALL_DIR/VERSION" 2>/dev/null || echo "unknown")
```

### 步骤 4：执行升级

使用步骤 2 检测到的安装类型和目录：

**对于 git 安装**（global-git、local-git）：
```bash
cd "$INSTALL_DIR"
STASH_OUTPUT=$(git stash 2>&1)
git fetch origin
git reset --hard origin/main
./setup
```
如果 `$STASH_OUTPUT` 包含 "Saved working directory"，警告用户：“注意：本地修改已暂存。在技能目录中运行 `git stash pop` 可恢复。”

**对于 vendored 安装**（vendored、vendored-global）：
```bash
PARENT=$(dirname "$INSTALL_DIR")
TMP_DIR=$(mktemp -d)
git clone --depth 1 https://github.com/garrytan/gstack.git "$TMP_DIR/gstack"
mv "$INSTALL_DIR" "$INSTALL_DIR.bak"
mv "$TMP_DIR/gstack" "$INSTALL_DIR"
cd "$INSTALL_DIR" && ./setup
rm -rf "$INSTALL_DIR.bak" "$TMP_DIR"
```

### 步骤 4.5：处理本地 vendored 副本

使用步骤 2 的安装目录。检查是否还有本地 vendored 副本，以及团队模式是否激活：

```bash
_ROOT=$(git rev-parse --show-toplevel 2>/dev/null)
LOCAL_GSTACK=""
if [ -n "$_ROOT" ] && [ -d "$_ROOT/.trae/skills/gstack" ]; then
  _RESOLVED_LOCAL=$(cd "$_ROOT/.trae/skills/gstack" && pwd -P)
  _RESOLVED_PRIMARY=$(cd "$INSTALL_DIR" && pwd -P)
  if [ "$_RESOLVED_LOCAL" != "$_RESOLVED_PRIMARY" ]; then
    LOCAL_GSTACK="$_ROOT/.trae/skills/gstack"
  fi
fi
_TEAM_MODE=$(.trae/skills/gstack/bin/gstack-config get team_mode 2>/dev/null || echo "false")
echo "LOCAL_GSTACK=$LOCAL_GSTACK"
echo "TEAM_MODE=$_TEAM_MODE"
```

**如果 `LOCAL_GSTACK` 非空 且 `TEAM_MODE` 为 `true`：** 删除 vendored 副本。团队模式下使用全局安装作为唯一可信来源。

```bash
cd "$_ROOT"
git rm -r --cached .trae/skills/gstack/ 2>/dev/null || true
if ! grep -qF '.trae/skills/gstack/' .gitignore 2>/dev/null; then
  echo '.trae/skills/gstack/' >> .gitignore
fi
rm -rf "$LOCAL_GSTACK"
```
告知用户：“已删除 `$LOCAL_GSTACK` 处的 vendored 副本（团队模式已激活 — 全局安装是唯一可信来源）。准备好后提交 `.gitignore` 变更。”

**如果 `LOCAL_GSTACK` 非空 且 `TEAM_MODE` 不为 `true`：** 通过从刚升级完成的主安装复制来更新它（与 README 中 vendored 安装方法相同）：
```bash
mv "$LOCAL_GSTACK" "$LOCAL_GSTACK.bak"
cp -Rf "$INSTALL_DIR" "$LOCAL_GSTACK"
rm -rf "$LOCAL_GSTACK/.git"
cd "$LOCAL_GSTACK" && ./setup
rm -rf "$LOCAL_GSTACK.bak"
```
告知用户：“同时更新了 `$LOCAL_GSTACK` 处的 vendored 副本 — 准备好后提交 `.trae/skills/gstack/`。”

如果 `./setup` 失败，从备份恢复并警告用户：
```bash
rm -rf "$LOCAL_GSTACK"
mv "$LOCAL_GSTACK.bak" "$LOCAL_GSTACK"
```
告知用户：“同步失败 — 已恢复 `$LOCAL_GSTACK` 的先前版本。请手动运行 `/gstack-upgrade` 重试。”

### 步骤 4.75：运行版本迁移

在 `./setup` 完成后，运行旧版本和新版本之间的所有迁移脚本。
迁移脚本用于处理 `./setup` 无法覆盖的状态修复（过时的配置、孤立文件、目录结构变更）。

```bash
MIGRATIONS_DIR="$INSTALL_DIR/gstack-upgrade/migrations"
if [ -d "$MIGRATIONS_DIR" ]; then
  for migration in $(find "$MIGRATIONS_DIR" -maxdepth 1 -name 'v*.sh' -type f 2>/dev/null | sort -V); do
    # 从文件名提取版本号：v0.15.2.0.sh → 0.15.2.0
    m_ver="$(basename "$migration" .sh | sed 's/^v//')"
    # 仅当此迁移版本比旧版本新时才运行
    # （对于相同段数的点分版本，简单字符串比较即可）
    if [ "$OLD_VERSION" != "unknown" ] && [ "$(printf '%s\n%s' "$OLD_VERSION" "$m_ver" | sort -V | head -1)" = "$OLD_VERSION" ] && [ "$OLD_VERSION" != "$m_ver" ]; then
      echo "Running migration $m_ver..."
      bash "$migration" || echo "  Warning: migration $m_ver had errors (non-fatal)"
    fi
  done
fi
```

迁移脚本位于 `gstack-upgrade/migrations/` 中，是幂等的 bash 脚本。每个文件命名为
`v{VERSION}.sh`，仅在从旧版本升级时运行。有关如何添加新迁移脚本，请参阅 CONTRIBUTING.md。

### 步骤 5：写入标记 + 清除缓存

```bash
mkdir -p ~/.gstack
echo "$OLD_VERSION" > ~/.gstack/just-upgraded-from
rm -f ~/.gstack/last-update-check
rm -f ~/.gstack/update-snoozed
```

### 步骤 6：展示更新内容

读取 `$INSTALL_DIR/CHANGELOG.md`。查找旧版本和新版本之间的所有版本条目。按主题归类，
总结为 5-7 个要点。不要信息过载 — 重点关注面向用户的变更。除非有重大影响，否则跳过内部重构。

格式：
```
gstack v{new} — 已从 v{old} 升级！

更新内容：
- [要点 1]
- [要点 2]
- ...

祝你顺利发布！
```

### 步骤 7：继续操作

展示更新内容后，继续执行用户最初调用的技能操作。升级已完成 — 无需进一步操作。

---

## 独立使用方式

当直接作为 `/gstack-upgrade` 调用时（而非从前言触发）：

1. 强制进行新的更新检查（绕过缓存）：
```bash
.trae/skills/gstack/bin/gstack-update-check --force 2>/dev/null || \
.trae/skills/gstack/bin/gstack-update-check --force 2>/dev/null || true
```
使用输出来判断是否有可用升级。

2. 如果输出 `UPGRADE_AVAILABLE <old> <new>`：执行上述步骤 2-6。

3. 如果无输出（主安装已是最新）：检查是否存在过期的本地 vendored 副本。

运行上述步骤 2 的 bash 代码块来检测主安装的类型和目录（`INSTALL_TYPE` 和 `INSTALL_DIR`）。然后运行上述步骤 4.5 的检测代码块来检查本地 vendored 副本（`LOCAL_GSTACK`）和团队模式状态（`TEAM_MODE`）。

**如果 `LOCAL_GSTACK` 为空**（没有本地 vendored 副本）：告知用户“你已使用最新版本（v{version}）。”

**如果 `LOCAL_GSTACK` 非空 且 `TEAM_MODE` 为 `true`：** 使用上述步骤 4.5 团队模式删除代码块移除 vendored 副本。告知用户：“全局 v{version} 已是最新。已移除过期的 vendored 副本（团队模式已激活）。准备好后提交 `.gitignore` 变更。”

**如果 `LOCAL_GSTACK` 非空 且 `TEAM_MODE` 不为 `true`**，比较版本号：
```bash
PRIMARY_VER=$(cat "$INSTALL_DIR/VERSION" 2>/dev/null || echo "unknown")
LOCAL_VER=$(cat "$LOCAL_GSTACK/VERSION" 2>/dev/null || echo "unknown")
echo "PRIMARY=$PRIMARY_VER LOCAL=$LOCAL_VER"
```

**如果版本不同：** 执行上述步骤 4.5 同步代码块来从主安装更新本地副本。告知用户：“全局 v{PRIMARY_VER} 已是最新。已更新本地 vendored 副本：v{LOCAL_VER} → v{PRIMARY_VER}。准备好后提交 `.trae/skills/gstack/`。”

**如果版本相同：** 告知用户“你已使用最新版本（v{PRIMARY_VER}）。全局和本地 vendored 副本均已是最新。”
