---
name: claude
preamble-tier: 3
version: 1.0.0
description: |
  Claude Code CLI 包装器，适用于非 Claude 主机 - 三种模式。审查模式：通过
  claude -p 进行独立的差异审查。挑战模式：对抗性失效模式审查。咨询模式：
  使用只读文件工具向 Claude 询问仓库情况。当被要求"claude 审查"、"claude 挑战"、
  "问 claude"、"来自 claude 的第二意见"或"外部视角"时使用。(gstack)
triggers:
  - claude review
  - claude challenge
  - ask claude
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion
---

{{PREAMBLE}}

{{BASE_BRANCH_DETECT}}

# /claude - Claude 外部视角

你正在从非 Claude 主机运行 `/claude` 技能。这将包装 `claude -p`
来获取独立的 Claude Code 第二意见，同时不允许嵌套的 Claude 修改文件。

生成的外部调用名称为 `gstack-claude`。

---

## 步骤 0：检查 Claude CLI

```bash
CLAUDE_BIN=$(command -v claude 2>/dev/null || echo "")
[ -z "$CLAUDE_BIN" ] && echo "NOT_FOUND" || echo "FOUND: $CLAUDE_BIN"
```

如果返回 `NOT_FOUND`，停止并告知用户：
"未找到 Claude CLI。请安装 Claude Code，然后重新运行此技能。"

检查认证状态：

```bash
if [ -f "$HOME/.claude/.credentials.json" ] || [ -n "${ANTHROPIC_API_KEY:-}" ]; then
  echo "AUTH_FOUND"
else
  echo "AUTH_MISSING"
fi
```

如果返回 `AUTH_MISSING`，停止并告知用户：
"未找到 Claude 认证信息。请交互式运行 `claude` 登录，或导出 `ANTHROPIC_API_KEY`，然后重新运行此技能。"

---

## 安全边界

嵌套的 Claude 必须专注于用户的仓库，且不得从此技能内部运行 gstack 技能。

所有 `claude -p` 调用必须包含：

- `--disable-slash-commands`
- 审查/挑战模式：`--tools ""`
- 咨询模式：`--allowedTools Read,Grep,Glob --disallowedTools Bash,Edit,Write`

在此技能中，永远不要向嵌套的 Claude 传递 `Bash`、`Edit` 或 `Write`。

所有提示词必须写入临时文件并通过 stdin 传入。绝不要将用户文本直接插入到 shell 命令中。

---

## 步骤 1：检测模式

解析用户输入：

1. `/claude review` 或 `/claude review <instructions>` - **审查模式**（步骤 2A）
2. `/claude challenge` 或 `/claude challenge <focus>` - **挑战模式**（步骤 2B）
3. `/claude` 无参数，或 `/claude <其他任何内容>` - **咨询模式**（步骤 2C）

如果模式不明显且存在差异，询问用户是要审查、挑战还是咨询。

---

## 共享辅助工具

在每个模式中使用以下代码片段。

创建临时文件：

```bash
PROMPT_FILE=$(mktemp /tmp/gstack-claude-prompt-XXXXXX)
RESP_FILE=$(mktemp /tmp/gstack-claude-response-XXXXXX.json)
ERR_FILE=$(mktemp /tmp/gstack-claude-error-XXXXXX.txt)
```

在每个模式结束时清理：

```bash
rm -f "$PROMPT_FILE" "$RESP_FILE" "$ERR_FILE"
```

解析 JSON 输出：

```bash
python3 - "$RESP_FILE" <<'PY'
import json, sys
path = sys.argv[1]
try:
    obj = json.load(open(path))
except Exception as exc:
    print(f"CLAUDE_JSON_PARSE_ERROR: {exc}")
    sys.exit(0)

if obj.get("is_error"):
    print("CLAUDE_ERROR: true")

result = obj.get("result") or obj.get("response") or ""
if result:
    print(result)

usage = obj.get("usage") or {}
input_tokens = usage.get("input_tokens", 0) or 0
output_tokens = usage.get("output_tokens", 0) or 0
cache_read = usage.get("cache_read_input_tokens", 0) or 0
model = obj.get("model") or "unknown"
session_id = obj.get("session_id") or ""

print(f"\nTokens: input={input_tokens} output={output_tokens} cache_read={cache_read} | Model: {model}")
if session_id:
    print(f"SESSION_ID:{session_id}")
PY
```

如果 stderr 包含 `auth`、`login` 或 `unauthorized`，告知用户：
"Claude 认证失败。请交互式运行 `claude` 进行认证，或导出 `ANTHROPIC_API_KEY`。"

---

## 步骤 2A：审查模式

在无工具模式下使用嵌套 Claude 审查当前分支的差异。

1. 获取基准分支并捕获差异：

```bash
_REPO_ROOT=$(git rev-parse --show-toplevel) || { echo "ERROR: not in a git repo" >&2; exit 1; }
cd "$_REPO_ROOT"
DIFF_FILE=$(mktemp /tmp/gstack-claude-diff-XXXXXX.patch)
git fetch origin <base> --quiet 2>/dev/null || true
git diff "origin/<base>" > "$DIFF_FILE" 2>/dev/null || git diff "<base>" > "$DIFF_FILE"
```

如果差异文件为空，停止并说明：
"无需审查 - 与基准分支相比没有变更。"

2. 写入提示词文件：

```bash
cat > "$PROMPT_FILE" <<'EOF'
你是一位直言不讳的 Claude Code 审查员。审查这个 git diff，查找缺陷、
生产环境故障模式、安全问题、缺失的测试和可维护性问题。直接了当。不要夸奖。
尽可能引用文件和变更的代码。

用户的额外指示（如果有）：
<custom review instructions>

差异内容：
EOF
cat "$DIFF_FILE" >> "$PROMPT_FILE"
```

3. 运行 Claude：

```bash
cat "$PROMPT_FILE" | claude -p --output-format json --disable-slash-commands --tools "" > "$RESP_FILE" 2>"$ERR_FILE"
```

4. 展示解析后的输出：

```
CLAUDE 说（代码审查）：
============================================================
<来自 RESP_FILE 的解析结果>
============================================================
```

5. 清理：

```bash
rm -f "$DIFF_FILE" "$PROMPT_FILE" "$RESP_FILE" "$ERR_FILE"
```

---

## 步骤 2B：挑战模式

在无工具模式下使用嵌套 Claude 进行对抗性失效模式审查。

1. 使用与审查模式相同的差异捕获命令获取差异。

2. 写入提示词：

```bash
cat > "$PROMPT_FILE" <<'EOF'
你是一位对抗性的 Claude Code 审查员。在用户发现问题之前，尝试找出这个变更的漏洞。
查找边界情况、竞态条件、安全漏洞、资源泄漏、静默数据损坏、错误的错误处理和
运维失效模式。要彻底。不要夸奖。如果用户提供了重点领域，请优先关注。

重点领域（如果有）：
<focus>

差异内容：
EOF
cat "$DIFF_FILE" >> "$PROMPT_FILE"
```

3. 运行 Claude：

```bash
cat "$PROMPT_FILE" | claude -p --output-format json --disable-slash-commands --tools "" > "$RESP_FILE" 2>"$ERR_FILE"
```

4. 展示解析后的输出：

```
CLAUDE 说（对抗性挑战）：
============================================================
<来自 RESP_FILE 的解析结果>
============================================================
```

5. 清理：

```bash
rm -f "$DIFF_FILE" "$PROMPT_FILE" "$RESP_FILE" "$ERR_FILE"
```

---

## 步骤 2C：咨询模式

向 Claude 询问仓库情况。咨询模式可以检查文件，但仅使用只读工具。

1. 检查是否存在已有的 Claude 会话：

```bash
cat .context/claude-session-id 2>/dev/null || echo "NO_SESSION"
```

如果存在会话，询问用户是否要继续该会话还是重新开始。

2. 写入提示词：

```bash
cat > "$PROMPT_FILE" <<'EOF'
你是 Claude Code，作为这个仓库的独立外部视角。直接回答用户的问题。
你只能使用 Read、Grep 和 Glob 来检查仓库文件。不要使用 Bash。
不要编辑或写入文件。不要调用斜杠命令或 gstack 技能。

用户问题：
<user prompt>
EOF
```

3. 运行 Claude。

对于新会话：

```bash
cat "$PROMPT_FILE" | claude -p --output-format json --disable-slash-commands --allowedTools Read,Grep,Glob --disallowedTools Bash,Edit,Write > "$RESP_FILE" 2>"$ERR_FILE"
```

对于恢复的会话：

```bash
cat "$PROMPT_FILE" | claude -p --resume "<session-id>" --output-format json --disable-slash-commands --allowedTools Read,Grep,Glob --disallowedTools Bash,Edit,Write > "$RESP_FILE" 2>"$ERR_FILE"
```

4. 解析并保存会话 ID：

```bash
SESSION_ID=$(python3 - "$RESP_FILE" <<'PY'
import json, sys
try:
    obj = json.load(open(sys.argv[1]))
    print(obj.get("session_id") or "")
except Exception:
    print("")
PY
)
if [ -n "$SESSION_ID" ]; then
  mkdir -p .context
  printf "%s\n" "$SESSION_ID" > .context/claude-session-id
fi
```

5. 展示解析后的输出：

```
CLAUDE 说（咨询）：
============================================================
<来自 RESP_FILE 的解析结果>
============================================================
会话已保存 - 再次运行 /claude 可继续此对话。
```

6. 清理：

```bash
rm -f "$PROMPT_FILE" "$RESP_FILE" "$ERR_FILE"
```

---

## 错误处理

- **未找到二进制文件：** 停止并给出安装说明。
- **缺少认证：** 停止并给出登录/API 密钥说明。
- **来自 stderr 的认证失败：** 显示 stderr 行并要求用户重新认证。
- **JSON 解析失败：** 显示来自 `$RESP_FILE` 的原始标准输出和来自 `$ERR_FILE` 的标准错误。
- **空响应：** 告知用户 "Claude 未返回响应。检查 stderr 获取错误信息。"
- **恢复失败：** 删除 `.context/claude-session-id` 并使用新会话重试。

---

## 重要规则

- 嵌套 Claude 在咨询模式下是只读的，在审查/挑战模式下无工具访问权限。
- 始终包含 `--disable-slash-commands`。
- 永远不要向嵌套 Claude 传递 `Bash`、`Edit` 或 `Write`。
- 绝不要将用户文本插入到 shell 命令中。
- 如实展示 Claude 的响应，然后在其后添加主机代理的综合分析。
