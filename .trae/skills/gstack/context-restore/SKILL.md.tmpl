---
name: context-restore
preamble-tier: 2
version: 1.0.0
description: |
  恢复之前由 /context-save 保存的工作上下文。加载最近的
  已保存状态（默认跨所有分支），以便你可以从离开的地方
  继续 — 即使在 Conductor 工作区交接之间。
  使用场景："恢复"、"恢复上下文"、"我刚才做到哪了"或
  "从我离开的地方继续"。与 /context-save 配合使用。
  原名 /checkpoint resume — 重命名是因为 Claude Code 在当前环境中将 /checkpoint
  视为原生回退别名。(gstack)
allowed-tools:
  - Bash
  - Read
  - Glob
  - Grep
  - AskUserQuestion
triggers:
  - resume where i left off
  - restore context
  - where was i
  - pick up where i left off
  - context restore
---

{{PREAMBLE}}

# /context-restore — 恢复已保存的工作上下文

你是一名**阅读同事详细会话笔记的 Staff 工程师**，
准确地在他们离开的地方继续工作。你的工作是加载最近的已保存
上下文并清晰地呈现出来，以便用户可以无缝接续工作。

**硬性门控：** 不要实现代码更改。此技能仅读取已保存的
上下文文件并呈现摘要。

**默认：加载所有分支中最近保存的上下文。** 这与
`/context-save list` 故意不同，后者默认当前分支。
`/context-restore` 用于 Conductor 工作区交接 — 在一个分支上保存的上下文
可以从另一个分支恢复。

**不要按当前分支过滤候选集。** `list` 流程会
这样做；`/context-restore` 不会。

---

## 检测命令

解析用户的输入：

- `/context-restore` → 加载最近保存的上下文（任意分支）
- `/context-restore <title-fragment-or-number>` → 加载特定的已保存上下文
- `/context-restore list` → 告诉用户"请使用 `/context-save list` — 列表
  功能在保存端"并退出。此处不做模式检测。

---

## 恢复流程

### 步骤 1：查找已保存的上下文

```bash
{{SLUG_SETUP}}
CHECKPOINT_DIR="${GSTACK_HOME:-$HOME/.gstack}/projects/$SLUG/checkpoints"
if [ ! -d "$CHECKPOINT_DIR" ]; then
  echo "NO_CHECKPOINTS"
else
  # 使用 find + sort 替代 ls -1t。两个原因：
  # 1. 规范顺序是文件名 YYYYMMDD-HHMMSS 前缀（在
  #    复制/rsync 时稳定）。文件系统 mtime 会漂移，不具有权威性。
  # 2. 在 macOS 上，`find ... | xargs ls -1t` 在没有结果时会回退到
  #    列出 cwd。`sort -r` 在空输入时干净地返回空。
  # 限制最多 20 个最近的文件：拥有 10k 个保存文件的用户不应该仅仅
  # 因为列出它们就消耗上下文窗口。/context-save list 处理分页。
  FILES=$(find "$CHECKPOINT_DIR" -maxdepth 1 -name "*.md" -type f 2>/dev/null | sort -r | head -20)
  if [ -z "$FILES" ]; then
    echo "NO_CHECKPOINTS"
  else
    echo "$FILES"
  fi
fi
```

**候选集包含目录中的所有 `.md` 文件，无论分支如何**
（分支记录在 frontmatter 中，不用于此处过滤）。这
支持 Conductor 工作区交接。

### 步骤 2：加载正确的文件

- 如果用户指定了标题片段或编号：在候选集中找到匹配的文件。
- 否则：加载 **上方 `sort -r` 返回的第一个文件** — 那是
  最新的 `YYYYMMDD-HHMMSS` 前缀，即规范的"最近"。

读取选定的文件并呈现摘要：

```
RESUMING CONTEXT
════════════════════════════════════════
标题：       {title}
分支：       {来自 frontmatter 的分支}
保存时间：   {timestamp，可读格式}
时长：       上次会话为 {格式化的时长}（如果有）
状态：       {status}
════════════════════════════════════════

### Summary
{来自保存文件的摘要}

### Remaining Work
{剩余工作项}

### Notes
{备注}
```

如果当前分支与保存上下文的分支不同，标注这一点：
"此上下文保存在分支 `{branch}` 上。你当前在
`{current branch}` 上。你可能需要在继续之前切换分支。"

### 步骤 3：提供下一步

呈现后，通过 AskUserQuestion 询问：

- A) 继续处理剩余项目
- B) 显示完整的保存文件
- C) 只需要上下文，谢谢

如果选择 A，摘要第一个剩余工作项并建议从那里开始。

---

## 如果没有已保存的上下文

如果步骤 1 打印了 `NO_CHECKPOINTS`，告诉用户：

"还没有保存的上下文。先运行 `/context-save` 保存你当前的工作
状态，然后 `/context-restore` 就能找到它。"

---

## 重要规则

- **永远不要修改代码。** 此技能仅读取已保存的文件并呈现它们。
- **默认始终跨所有分支搜索。** 跨分支恢复是
  全部意义所在。仅在用户通过特定标题片段匹配明确要求时按分支过滤。
- **"最近"意味着文件名 `YYYYMMDD-HHMMSS` 前缀**，而非
  `ls -1t`（文件系统 mtime）。文件名在文件系统
  操作中是稳定的；mtime 不是。
- **这是 gstack 技能，不是 Claude Code 内置的。** 当用户输入
  `/context-restore` 时，通过 Skill 工具调用此技能。
