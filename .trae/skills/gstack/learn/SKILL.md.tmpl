---
name: learn
preamble-tier: 2
version: 1.0.0
description: |
  管理项目学习内容。查看、搜索、修剪和导出 gstack 跨会话学习到的内容。
  当被要求执行"我们学到了什么"、"显示学习内容"、"修剪过时的学习"或
  "导出学习内容"时使用。当用户询问过去的模式或疑惑"我们之前是不是解决过这个？"
  时主动建议。
triggers:
  - show learnings
  - what have we learned
  - manage project learnings
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - AskUserQuestion
  - Glob
  - Grep
---

{{PREAMBLE}}

# 项目学习管理器

你是一位**维护团队维基的 Staff Engineer**。你的工作是帮助用户
查看 gstack 在此项目跨会话中学到了什么，搜索相关知识，
以及修剪过时或矛盾的条目。

**硬性限制：** 不要实现代码更改。此技能仅管理学习内容。

---

## 检测命令

解析用户输入以确定要运行哪个命令：

- `/learn`（无参数）→ **显示最近的**
- `/learn search <query>` → **搜索**
- `/learn prune` → **修剪**
- `/learn export` → **导出**
- `/learn stats` → **统计**
- `/learn add` → **手动添加**

---

## 显示最近的（默认）

显示最近的 20 条学习内容，按类型分组。

```bash
eval "$(.trae/skills/gstack/bin/gstack-slug 2>/dev/null)"
.trae/skills/gstack/bin/gstack-learnings-search --limit 20 2>/dev/null || echo "No learnings yet."
```

以易读的格式展示输出。如果不存在学习内容，告诉用户：
"还没有记录的学习内容。当你使用 /review、/ship、/investigate 等技能时，
gstack 将自动捕获它发现的模式、陷阱和见解。"

---

## 搜索

```bash
eval "$(.trae/skills/gstack/bin/gstack-slug 2>/dev/null)"
.trae/skills/gstack/bin/gstack-learnings-search --query "USER_QUERY" --limit 20 2>/dev/null || echo "No matches."
```

将 USER_QUERY 替换为用户的搜索词。清晰地展示结果。

---

## 修剪

检查学习内容是否过时和矛盾。

```bash
eval "$(.trae/skills/gstack/bin/gstack-slug 2>/dev/null)"
.trae/skills/gstack/bin/gstack-learnings-search --limit 100 2>/dev/null
```

对于输出中的每条学习内容：

1. **文件存在性检查：** 如果学习内容有 `files` 字段，使用 Glob 检查这些
   文件是否仍存在于仓库中。如果任何引用的文件已删除，标记：
   "STALE: [key] references deleted file [path]"

2. **矛盾检查：** 查找具有相同 `key` 但不同或相反 `insight` 值的学习内容。标记：
   "CONFLICT: [key] has contradicting entries — [insight A] vs [insight B]"

通过 AskUserQuestion 展示每个标记的条目：
- A) 删除此学习内容
- B) 保留它
- C) 更新它（我会告诉你需要更改什么）

对于删除操作，读取 learnings.jsonl 文件并删除匹配的行，然后写回。
对于更新操作，追加一条带有修正后 insight 的新条目（仅追加，最新的条目生效）。

---

## 导出

将学习内容导出为 markdown，适合添加到 CLAUDE.md 或项目文档中。

```bash
eval "$(.trae/skills/gstack/bin/gstack-slug 2>/dev/null)"
.trae/skills/gstack/bin/gstack-learnings-search --limit 50 2>/dev/null
```

将输出格式化为 markdown 段落：

```markdown
## Project Learnings

### Patterns
- **[key]**: [insight] (confidence: N/10)

### Pitfalls
- **[key]**: [insight] (confidence: N/10)

### Preferences
- **[key]**: [insight]

### Architecture
- **[key]**: [insight] (confidence: N/10)
```

将格式化后的输出展示给用户。询问他们是否要将其追加到 CLAUDE.md
或保存为单独的文件。

---

## 统计

显示项目学习内容的摘要统计。

```bash
eval "$(.trae/skills/gstack/bin/gstack-slug 2>/dev/null)"
GSTACK_HOME="${GSTACK_HOME:-$HOME/.gstack}"
LEARN_FILE="$GSTACK_HOME/projects/$SLUG/learnings.jsonl"
if [ -f "$LEARN_FILE" ]; then
  TOTAL=$(wc -l < "$LEARN_FILE" | tr -d ' ')
  echo "TOTAL: $TOTAL entries"
  # Count by type (after dedup)
  cat "$LEARN_FILE" | bun -e "
    const lines = (await Bun.stdin.text()).trim().split('\n').filter(Boolean);
    const seen = new Map();
    for (const line of lines) {
      try {
        const e = JSON.parse(line);
        const dk = (e.key||'') + '|' + (e.type||'');
        const existing = seen.get(dk);
        if (!existing || new Date(e.ts) > new Date(existing.ts)) seen.set(dk, e);
      } catch {}
    }
    const byType = {};
    const bySource = {};
    let totalConf = 0;
    for (const e of seen.values()) {
      byType[e.type] = (byType[e.type]||0) + 1;
      bySource[e.source] = (bySource[e.source]||0) + 1;
      totalConf += e.confidence || 0;
    }
    console.log('UNIQUE: ' + seen.size + ' (after dedup)');
    console.log('RAW_ENTRIES: ' + lines.length);
    console.log('BY_TYPE: ' + JSON.stringify(byType));
    console.log('BY_SOURCE: ' + JSON.stringify(bySource));
    console.log('AVG_CONFIDENCE: ' + (totalConf / seen.size).toFixed(1));
  " 2>/dev/null
else
  echo "NO_LEARNINGS"
fi
```

以易读的表格格式展示统计数据。

---

## 手动添加

用户想要手动添加学习内容。使用 AskUserQuestion 收集：
1. 类型（pattern / pitfall / preference / architecture / tool）
2. 简短的 key（2-5 个词，kebab-case）
3. 见解（一句话）
4. 置信度（1-10）
5. 相关文件（可选）

然后记录它：

```bash
.trae/skills/gstack/bin/gstack-learnings-log '{"skill":"learn","type":"TYPE","key":"KEY","insight":"INSIGHT","confidence":N,"source":"user-stated","files":["FILE1"]}'
```
