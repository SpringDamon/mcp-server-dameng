---
name: open-gstack-browser
version: 0.2.0
description: |
  启动 GStack 浏览器——AI 控制的 Chromium，内置侧边栏扩展。
  打开一个可见的浏览器窗口，你可以实时观看每个操作。
  侧边栏显示实时活动流和聊天。内置反机器人隐蔽性。
  当被要求"打开 gstack 浏览器"、"启动浏览器"、"连接 chrome"、
  "打开 chrome"、"真实浏览器"、"启动 chrome"、"侧面板"或
  "控制我的浏览器"时使用。
voice-triggers:
  - "show me the browser"
triggers:
  - open gstack browser
  - launch chromium
  - show me the browser
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion

---

{{PREAMBLE}}

# /open-gstack-browser — 启动 GStack 浏览器

启动 GStack 浏览器——AI 控制的 Chromium，带有侧边栏扩展、
反机器人隐蔽性和自定义品牌。你可以实时看到每个操作。

{{BROWSE_SETUP}}

## 步骤 0：飞行前清理

在连接之前，清理任何过期的浏览服务器和清理可能从崩溃中
遗留的锁定文件。这防止"已连接"的误报和 Chromium 配置文件锁定冲突。

```bash
# 杀死任何现有的浏览服务器
if [ -f "$(git rev-parse --show-toplevel 2>/dev/null)/.gstack/browse.json" ]; then
  _OLD_PID=$(cat "$(git rev-parse --show-toplevel)/.gstack/browse.json" 2>/dev/null | grep -o '"pid":[0-9]*' | grep -o '[0-9]*')
  [ -n "$_OLD_PID" ] && kill "$_OLD_PID" 2>/dev/null || true
  sleep 1
  [ -n "$_OLD_PID" ] && kill -9 "$_OLD_PID" 2>/dev/null || true
  rm -f "$(git rev-parse --show-toplevel)/.gstack/browse.json"
fi
# 清理 Chromium 配置文件锁定（崩溃后可能残留）
_PROFILE_DIR="$HOME/.gstack/chromium-profile"
for _LF in SingletonLock SingletonSocket SingletonCookie; do
  rm -f "$_PROFILE_DIR/$_LF" 2>/dev/null || true
done
echo "Pre-flight cleanup done"
```

## 步骤 1：连接

```bash
$B connect
```

这将以有头模式启动 GStack 浏览器（重新品牌化的 Chromium）：
- 一个你可以观看的可见窗口（不是你常规的 Chrome——它保持不受影响）
- 通过 `launchPersistentContext` 自动加载 gstack 侧边栏扩展
- 反机器人隐蔽性补丁（Google 和 NYTimes 等网站无需验证码即可工作）
- 自定义用户代理和 Dock/菜单栏中的 GStack 浏览器品牌
- 用于聊天命令的侧边栏代理进程

`connect` 命令从 gstack 安装目录自动发现扩展。它始终使用端口
**34567**，以便扩展可以自动连接。

连接后，向用户打印完整输出。确认你在输出中看到 `Mode: headed`。

如果输出显示错误或模式不是 `headed`，在继续之前运行 `$B status`
并与用户共享输出。

## 步骤 2：验证

```bash
$B status
```

确认输出显示 `Mode: headed`。从状态文件读取端口：

```bash
cat "$(git rev-parse --show-toplevel 2>/dev/null)/.gstack/browse.json" 2>/dev/null | grep -o '"port":[0-9]*' | grep -o '[0-9]*'
```

端口应该是 **34567**。如果不同，记录它——用户可能需要它来使用侧面板。

同时找到扩展路径，以便在用户需要手动加载时可以提供帮助：

```bash
_EXT_PATH=""
_ROOT=$(git rev-parse --show-toplevel 2>/dev/null)
[ -n "$_ROOT" ] && [ -f "$_ROOT/.trae/skills/gstack/extension/manifest.json" ] && _EXT_PATH="$_ROOT/.trae/skills/gstack/extension"
[ -z "$_EXT_PATH" ] && [ -f "$HOME/.trae/skills/gstack/extension/manifest.json" ] && _EXT_PATH="$HOME/.trae/skills/gstack/extension"
echo "EXTENSION_PATH: ${_EXT_PATH:-NOT FOUND}"
```

## 步骤 3：引导用户到侧面板

使用 AskUserQuestion：

> Chrome 已启动并受 gstack 控制。你应该看到 Playwright 的 Chromium
> （不是你常规的 Chrome），页面顶部有一条金色的微光线条。
>
> 侧面板扩展应该已自动加载。要打开它：
> 1. 在工具栏中查找**拼图图标**（扩展）——如果扩展成功加载，
>    它可能已经显示 gstack 图标
> 2. 点击**拼图图标** → 找到 **gstack browse** → 点击**图钉图标**
> 3. 点击工具栏中已固定的 **gstack 图标**
> 4. 侧面板应该在右侧打开，显示实时活动流
>
> **端口：** 34567（自动检测——扩展在 Playwright 控制的 Chrome 中
> 自动连接）。

选项：
- A) 我可以看到侧面板——开始吧！
- B) 我可以看到 Chrome 但找不到扩展
- C) 出错了

如果 B：告诉用户：

> 扩展在启动时加载到 Playwright 的 Chromium 中，但
> 有时它不会立即出现。尝试这些步骤：
>
> 1. 在地址栏中输入 `chrome://extensions`
> 2. 查找 **"gstack browse"**——它应该已列出并启用
> 3. 如果它在那里但没有固定，回到任何页面，点击拼图图标
>    并固定它
> 4. 如果完全没有列出，点击 **"加载已解压的扩展"** 并导航到：
>    - 在文件选择器对话框中按 **Cmd+Shift+G**
>    - 粘贴此路径：`{EXTENSION_PATH}`（使用步骤 2 中的路径）
>    - 点击 **选择**
>
> 加载后，固定它并点击图标打开侧面板。
>
> 如果侧面板徽章保持灰色（未连接），点击 gstack 图标
> 并手动输入端口 **34567**。

如果 C：

1. 运行 `$B status` 并显示输出
2. 如果服务器不健康，重新运行步骤 0 清理 + 步骤 1 连接
3. 如果服务器健康但浏览器不可见，尝试 `$B focus`
4. 如果仍然失败，询问用户他们看到了什么（错误消息、空白屏幕等）

## 步骤 4：演示

在用户确认侧面板正常工作后，运行快速演示：

```bash
$B goto https://news.ycombinator.com
```

等待 2 秒，然后：

```bash
$B snapshot -i
```

告诉用户："检查侧面板——你应该看到 `goto` 和 `snapshot`
命令出现在活动流中。Claude 运行的每个命令都会在这里
实时显示。"

## 步骤 5：侧边栏聊天

在活动流演示后，告诉用户关于侧边栏聊天：

> 侧面板还有一个**聊天标签页**。尝试输入消息如"截图并描述此页面"。
> 侧边栏代理（子 Claude 实例）在浏览器中执行你的请求——你会
> 看到命令在活动流中出现。
>
> 侧边栏代理可以导航页面、点击按钮、填写表单和读取内容。
> 每个任务最多 5 分钟。它在隔离会话中运行，所以不会干扰
> 这个 Claude Code 窗口。

## 步骤 6：接下来

告诉用户：

> 你已经准备好了！以下是你可以用连接的 Chrome 做的事情：
>
> **实时观看 Claude 工作：**
> - 运行任何 gstack 技能（`/qa`、`/design-review`、`/benchmark`）并观看
>   每个操作在可见的 Chrome 窗口 + 侧面板流中发生
> - 无需导入 cookie——Playwright 浏览器共享其自己的会话
>
> **直接控制浏览器：**
> - **侧边栏聊天**——在侧面板中输入自然语言，侧边栏
>   代理执行它（例如"填写登录表单并提交"）
> - **浏览命令**——`$B goto <url>`、`$B click <sel>`、`$B fill <sel> <val>`、
>   `$B snapshot -i`——全部在 Chrome + 侧面板中可见
>
> **窗口管理：**
> - `$B focus`——随时将 Chrome 带到前台
> - `$B disconnect`——关闭有头 Chrome 并返回无头模式
>
> **有头模式中的技能看起来像什么：**
> - `/qa` 在可见浏览器中运行其完整测试套件——你看到每个页面
>   加载、每次点击、每个断言
> - `/design-review` 在真实浏览器中截图——与你看到的像素相同
> - `/benchmark` 在有头浏览器中测量性能

然后继续用户要求做的事情。如果他们没有指定任务，
询问他们想要测试或浏览什么。
