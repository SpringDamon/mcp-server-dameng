---
name: benchmark
preamble-tier: 1
version: 1.0.0
description: |
  在仓库中生成基准套件骨架。运行 `benchmark-setup`。
  当被要求"生成基准"或"基准套件"时使用。(gstack)
allowed-tools: [Bash, Read, Write, Edit, Grep, Glob, AskUserQuestion]
triggers:
  - generate benchmarks
  - benchmark setup
  - create benchmark suite
---

{{PREAMBLE}}

# /benchmark — 基准生成

## 0. 预生成扫描

```bash
echo "=== 项目检测 ==="
ls package.json 2>/dev/null && echo "Node.js 项目"
ls next.config.* 2>/dev/null && echo "Next.js"
ls Cargo.toml 2>/dev/null && echo "Rust"
ls go.mod 2>/dev/null && echo "Go"
ls requirements.txt pyproject.toml 2>/dev/null && echo "Python"
```

识别：框架、测试文件（`**/*.test.*`、`**/*_test.*`）、API 路由、数据库模型。

AskUserQuestion："为 {N} 个模块生成基准？
A) 全部 {N} 个
B) 仅关键的（API 端点、数据库查询）
C) 让我选择"

**停止。** 在用户响应之前不要继续。

## 1. 基准骨架

```bash
mkdir -p benchmarks/
```

对于每个目标模块，生成：
1. **API 基准：** 带延迟、吞吐量、负载模式的负载测试
2. **数据库基准：** 查询性能、连接池、索引
3. **渲染基准：** TTFB、渲染时间、Lighthouse
4. **单元基准：** 函数执行时间、内存使用

AskUserQuestion："包括哪些基准类型？
A) 全部 4 种类型
B) 仅 API + 数据库
C) 让我选择"

## 2. 基准文件

对于每个基准，生成：
- 可重复的设置/拆解
- 可配置的负载模式（50、100、500、1000 用户）
- 明确的阈值（P50、P95、P99 延迟）
- 带趋势可视化的结果输出（JSON → 图表）

## 3. CI 集成

```bash
mkdir -p .github/workflows/
```

生成 `benchmarks.yml`：
- 在 PR 上触发
- 将结果与基线比较
- 如果性能下降 > 10% 则失败

## 4. 完成摘要

```
+====================================================================+
|                    基准生成摘要                                |
+====================================================================+
| 模块            | 基准数  | 基线延迟 | 阈值     |
|-----------------|-----------|-------------|----------|
| [模块]     | N         | P95: Xms    | < Yms    |
+--------------------------------------------------------------------+
```

"基准生成在 benchmarks/ 中。运行 `npm run benchmark` 执行所有基准。"
