---
name: plan-tune
preamble-tier: 1
version: 1.0.0
description: |
  调整 GStack 计划生成参数。通过交互式 UI 配置计划大小、
  优先级、质量门限和其他计划生成参数。当被要求 "tune planning"（调整计划）、
  "adjust plan settings"（调整计划设置）、"plan configuration"（计划配置）
  或 "optimize planning"（优化计划）时使用此技能。
  当用户抱怨计划太大或太小时，主动调用此技能。（gstack）
triggers:
  - tune planning
  - adjust plan settings
  - plan configuration
allowed-tools:
  - Read
  - Write
  - Edit
  - AskUserQuestion

---

{{PREAMBLE}}

# plan-tune：计划参数调谐器

调整影响 GStack 计划生成（/plan-eng-review、/plan-ceo-review、/plan-tuning）行为的参数。

## 当前配置

首先读取并显示当前的计划调优配置：

```bash
cat ~/.gstack/plan-tune.json 2>/dev/null || echo "未找到配置——使用默认值"
```

如果配置不存在，创建默认配置。

## 可调参数

### 1. 计划大小（plan_size）

控制在 /plan-eng-review 中生成的计划项数量。

| 预设 | 项数 | 适用场景 |
|-----------|-------|----------|
| 精益 | 3-5 | 小型变更、bug 修复、简单功能 |
| 标准 | 6-10 | 中等功能、重构、多组件变更 |
| 综合 | 11-15 | 大型功能、架构变更、新模块 |
| 自定义 | 手动设置 | 你有特定数字 |

**默认：** 标准

### 2. 优先级分布（priority_distribution）

控制计划项中 P0/P1/P2 项的百分比。

| 预设 | P0（关键） | P1（高） | P2（中） | 适用场景 |
|---------|-------------|----------|----------|----------|
| 聚焦 | 60% | 30% | 10% | MVP、严格截止日期、关键路径 |
| 平衡 | 40% | 40% | 20% | 标准开发，推荐 |
| 探索性 | 20% | 30% | 50% | 调研、技术可行性、原型 |

**默认：** 平衡

### 3. 质量门限（quality_threshold）

控制在计划完成度审计中应用的严格程度。

| 预设 | 行为 | 适用场景 |
|---------|----------|----------|
| 宽松 | 仅门控 P0 项。P1/P2 可以延后而不阻断发布。 | 快速迭代、内部工具、非生产用途 |
| 标准 | 门控 P0 + P1。P2 可以延后。 | 标准开发流程，推荐 |
| 严格 | 门控所有 P0/P1/P2。不允许延后。 | 生产发布、客户可见的变更 |

**默认：** 标准

### 4. 粒度级别（granularity）

控制计划项的详细程度。

| 预设 | 每个计划项的行数 | 适用场景 |
|-----------|-----------|----------|
| 高层 | 1-2 | 高管审查、利益相关者沟通 |
| 标准 | 3-4 | 工程团队、日常计划 |
| 详细 | 5+ | 复杂系统、新手开发者 |

**默认：** 标准

### 5. 测试策略（test_strategy）

控制为计划项生成的测试指导级别。

| 预设 | 行为 |
|-----------|----------|
| 最小 | 仅为 P0 项生成测试要求 |
| 标准 | 为 P0/P1 项生成测试要求 |
| 全面 | 为所有项生成测试要求，包含边界情况 |

**默认：** 标准

## 交互式调谐

使用 AskUserQuestion 引导用户完成配置：

```
你想调整哪些计划参数？

当前配置：
  计划大小：{plan_size}
  优先级分布：{priority_distribution}
  质量门限：{quality_threshold}
  粒度：{granularity}
  测试策略：{test_strategy}

选择要调整的参数：
A) 计划大小（生成的计划项数量）
B) 优先级分布（P0/P1/P2 百分比）
C) 质量门限（发布门控严格程度）
D) 粒度（计划项详细程度）
E) 测试策略（测试要求级别）
F) 重置为默认值
```

对于每个选定的参数，呈现预设选项或接受自定义值。

## 配置文件格式

将配置保存到 `~/.gstack/plan-tune.json`：

```json
{
  "plan_size": "standard",
  "plan_size_custom": null,
  "priority_distribution": "balanced",
  "quality_threshold": "standard",
  "granularity": "standard",
  "test_strategy": "standard",
  "updated_at": "2026-01-15T10:30:00Z"
}
```

## 预设快速切换

提供快速切换预设，无需交互式 UI：

```bash
# 快速模式——用于快速迭代
plan-tune --preset lean

# 生产模式——用于生产发布
plan-tune --preset production

# 研究模式——用于调研
plan-tune --preset exploratory

# 重置
plan-tune --reset
```

**快速切换预设：**

| 预设 | 计划大小 | 优先级 | 质量门限 | 粒度 | 测试策略 |
|---------|-----------|----------|-----------|--------|-----------|
| lean | 精益 | 聚焦 | 宽松 | 高层 | 最小 |
| production | 综合 | 平衡 | 严格 | 详细 | 全面 |
| exploratory | 精益 | 探索性 | 宽松 | 标准 | 最小 |
| default | 标准 | 平衡 | 标准 | 标准 | 标准 |

## 生效时间

配置变更在下次运行以下命令时生效：
- `/plan-eng-review`
- `/plan-ceo-review`
- `/plan-tuning`

当前正在进行的计划会话不受影响。

## 验证

更改配置后，验证它：

1. 读取配置文件确认更改已保存
2. 显示新旧配置对比
3. 建议运行 `/plan-eng-review` 以新设置测试

## 规则

- **始终显示当前配置**在进行任何变更之前
- **始终提供预设选项**用于常见场景
- **始终允许自定义值**如果预设不符合需求
- **始终验证保存的配置**在写入之后
- **永远不要修改正在进行的计划会话**——变更仅适用于未来的计划
- **如果配置文件损坏**，重置为默认值并通知用户
