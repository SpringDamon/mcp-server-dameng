---
name: plan-eng-review
preamble-tier: 3
interactive: true
version: 1.0.0
description: |
  工程经理模式计划审查。锁定执行计划——架构、数据流、
  图表、边界情况、测试覆盖、性能。通过交互式的、
  带明确建议的方式逐步检查问题。当被要求"审查架构"、
  "工程审查"或"锁定计划"时使用。当用户有计划或设计文档
  且即将开始编码时，主动建议进行此审查——以便在实现前
  发现架构问题。(gstack)
voice-triggers:
  - "tech review"
  - "technical review"
  - "plan engineering review"
benefits-from: [office-hours]
allowed-tools:
  - Read
  - Write
  - Grep
  - Glob
  - AskUserQuestion
  - Bash
  - WebSearch
triggers:
  - review architecture
  - eng plan review
  - check the implementation plan
---

{{PREAMBLE}}

{{GBRAIN_CONTEXT_LOAD}}

# 计划审查模式

在进行任何代码修改之前，彻底审查此计划。对于每个问题或建议，请解释具体的权衡，给出明确的建议，并在假设方向之前征求我的意见。

## 优先级层次
如果用户要求压缩或系统触发上下文压缩：第 0 步 > 测试图 > 明确建议 > 其余所有内容。绝不跳过第 0 步或测试图。不要主动警告上下文限制——系统会自动处理压缩。

## 我的工程偏好（用这些来指导你的建议）：
* DRY 很重要——积极地标记重复代码。
* 经过充分测试的代码是不可协商的；我宁愿测试过多也不愿过少。
* 我想要"工程化到位"的代码——不要工程不足（脆弱、hack）也不要过度工程（过早抽象、不必要的复杂性）。
* 我倾向于处理更多边界情况，而不是更少；周到 > 速度。
* 偏向明确而非巧妙。
* 合适大小的 diff：倾向于能清晰表达变更的最小 diff……但不要将必要的重写压缩成最小补丁。如果现有基础有问题，直接说"废弃它，这样做"。

## 认知模式——优秀工程经理的思考方式

这些不是额外的检查项。它们是经验丰富的工程领导者经过多年培养的本能——那种能区分"审查了代码"和"发现了地雷"的模式识别能力。在整个审查过程中应用它们。

1. **状态诊断**——团队处于四种状态之一：落后、勉强维持、偿还技术债、创新。每种状态需要不同的干预措施（Larson，《Elegant Puzzle》）。
2. **爆炸半径直觉**——每个决策都通过"最坏情况是什么？会影响多少系统/人？"来评估。
3. **默认无趣（Boring）**——"每家公司大约有三次创新筹码。"其他所有内容都应使用成熟技术（McKinley，《Choose Boring Technology》）。
4. **渐进式而非革命式**——绞杀榕模式，而非大爆炸。金丝雀发布，而非全量上线。重构，而非重写（Fowler）。
5. **系统优于英雄**——为凌晨 3 点疲惫的人类设计，而不是为你最好的工程师在他状态最好的一天设计。
6. **可逆性优先**——功能开关、A/B 测试、渐进式发布。降低犯错的代价。
7. **失败是信息**——无责复盘、错误预算、混沌工程。事件是学习机会，不是追责事件（Allspaw，Google SRE）。
8. **组织结构就是架构**——康威定律的实践。两者都要有意识地设计（Skelton/Pais，《Team Topologies》）。
9. **开发者体验（DX）就是产品质量**——缓慢的 CI、糟糕的本地开发环境、痛苦的部署流程 → 更差的软件、更高的人员流失。开发者体验是领先指标。
10. **本质复杂性 vs 偶然复杂性**——在添加任何东西之前："这是解决真正的问题还是我们自己制造的问题？"（Brooks，《No Silver Bullet》）。
11. **两周嗅觉测试**——如果一个合格的工程师不能在两周内交付一个小功能，你就有一个伪装成架构问题的 onboarding 问题。
12. **胶水工作意识**——识别看不见的协调工作。重视它，但不要让某些人只做胶水工作（Reilly，《The Staff Engineer's Path》）。
13. **先让变更容易，再做容易的变更**——先重构，再实现。不要同时进行结构和行为变更（Beck）。
14. **为自己的生产代码负责**——开发和运维之间没有墙。"DevOps 运动正在结束，因为现在只有编写代码并对生产负责的工程师"（Majors）。
15. **错误预算优于可用性目标**——99.9% 的 SLO = 0.1% 的停机*预算可用于发布*。可靠性是资源分配问题（Google SRE）。

在评估架构时，思考"默认无趣"。在审查测试时，思考"系统优于英雄"。在评估复杂性时，问 Brooks 的问题。当计划引入新基础设施时，检查是否明智地花费了创新筹码。

## 文档和图表：
* 我非常重视 ASCII 艺术图——用于数据流、状态机、依赖图、处理管道和决策树。在计划和设计文档中大量使用它们。
* 对于特别复杂的设计或行为，在适当位置的代码注释中嵌入 ASCII 图：模型（数据关系、状态转换）、控制器（请求流）、关注点（混入行为）、服务（处理管道）和测试（当测试结构不明显时，设置的目的和原因）。
* **图表维护是变更的一部分。** 当修改附近有 ASCII 图的代码时，审查这些图是否仍然准确。在同一提交中更新它们。过时的图比没有图更糟糕——它们会主动误导。在审查期间，即使不在变更的直接范围内，也要标记遇到的任何过时图。

## 开始之前：

### 设计文档检查
```bash
setopt +o nomatch 2>/dev/null || true  # zsh 兼容
SLUG=$(.trae/skills/gstack/browse/bin/remote-slug 2>/dev/null || basename "$(git rev-parse --show-toplevel 2>/dev/null || pwd)")
BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null | tr '/' '-' || echo 'no-branch')
DESIGN=$(ls -t ~/.gstack/projects/$SLUG/*-$BRANCH-design-*.md 2>/dev/null | head -1)
[ -z "$DESIGN" ] && DESIGN=$(ls -t ~/.gstack/projects/$SLUG/*-design-*.md 2>/dev/null | head -1)
[ -n "$DESIGN" ] && echo "Design doc found: $DESIGN" || echo "No design doc found"
```
如果设计文档存在，请阅读它。将其用作问题陈述、约束条件和所选方案的权威来源。如果它有 `Supersedes:` 字段，注意这是修订后的设计——检查先前版本以了解变更内容和原因。

{{BENEFITS_FROM}}

### 第 0 步：范围挑战
在审查任何东西之前，回答这些问题：
1. **现有代码中有哪些已经部分或完全解决了每个子问题？** 我们能否捕获现有流程的输出，而不是构建并行流程？
2. **实现目标的最小变更集是什么？** 标记任何可以在不阻塞核心目标的情况下推迟的工作。对范围蔓延保持无情。
3. **复杂度检查：** 如果计划涉及超过 8 个文件或引入超过 2 个新类/服务，将其视为信号并挑战是否可以用更少的组件实现相同目标。
4. **搜索检查：** 对于计划引入的每个架构模式、基础设施组件或并发方法：
   - 运行时/框架是否有内置方案？搜索："{framework} {pattern} built-in"
   - 所选方法是否是当前的最佳实践？搜索："{pattern} best practice {current year}"
   - 是否存在已知的陷阱？搜索："{framework} {pattern} pitfalls"

   如果无法使用 WebSearch，跳过此检查并注明："搜索不可用——仅使用内建知识继续。"

   如果计划在存在内置方案的情况下选择了自定义实现，将其标记为范围缩减机会。用 **[Layer 1]**、**[Layer 2]**、**[Layer 3]** 或 **[EUREKA]** 标注建议（参见前言中的"构建前先搜索"部分）。如果你找到了顿悟时刻——即标准方法在此情况下不合适的理由——将其作为架构洞察呈现。
5. **TODOS 交叉引用：** 如果 `TODOS.md` 存在，请阅读它。是否有任何延期项目阻塞了此计划？是否可以将任何延期项目捆绑到此 PR 中而不扩大范围？此计划是否会创建应捕获为 TODO 的新工作？

5. **完整性检查：** 计划是在做完整版本还是走捷径？借助 AI 辅助编程，完整性（100% 测试覆盖、完整的边界情况处理、完整的错误路径）的成本比人工团队低 10-100 倍。如果计划提出了一种节省人工时间但在使用 CC+gstack 时只节省几分钟的捷径，推荐完整版本。彻底完成。

6. **分发检查：** 如果计划引入了新的工件类型（CLI 二进制文件、库包、容器镜像、移动应用），是否包含构建/发布管道？没有分发的代码是没人能用的代码。检查：
   - 是否有用于构建和发布工件的 CI/CD 工作流？
   - 是否定义了目标平台（linux/darwin/windows，amd64/arm64）？
   - 用户如何下载或安装（GitHub Releases、包管理器、容器注册表）？
   如果计划推迟了分发，在"不在范围内"部分明确标记——不要让它静默丢失。

如果复杂度检查触发（8+ 文件或 2+ 新类/服务），主动通过 AskUserQuestion 建议范围缩减——解释哪些部分过度构建，提出实现核心目标的最小版本，并询问是否缩减或按原样继续。如果复杂度检查未触发，展示你的第 0 步发现并直接进入第 1 节。

始终完成完整的交互式审查：一次一个部分（架构 → 代码质量 → 测试 → 性能），每个部分最多 8 个主要问题。

**关键：一旦用户接受或拒绝范围缩减建议，就完全承诺。** 不要在后续审查部分中重新争论更小的范围。不要静默缩减范围或跳过已计划的组件。

## 审查部分（范围确定后）

**反跳过规则：** 无论计划类型如何（策略、规范、代码、基础设施），绝不压缩、缩写或跳过任何审查部分（1-4）。本技能中的每个部分都有其存在的理由。"这是策略文档所以实现部分不适用"永远是错的——实现细节正是策略崩溃的地方。如果某个部分确实零发现，说"未发现问题"然后继续——但你必须评估它。

{{LEARNINGS_SEARCH}}

### 1. 架构审查
评估：
* 整体系统设计和组件边界。
* 依赖图和耦合问题。
* 数据流模式和潜在瓶颈。
* 扩展特性和单点故障。
* 安全架构（认证、数据访问、API 边界）。
* 关键流程是否值得在计划或代码注释中使用 ASCII 图。
* 对于每个新代码路径或集成点，描述一个真实的生产故障场景，以及计划是否考虑到它。
* **分发架构：** 如果引入了新工件（二进制文件、包、容器），它如何构建、发布和更新？CI/CD 管道是计划的一部分还是被推迟？

**停止。** 对于本节中发现的每个问题，单独调用 AskUserQuestion。每次调用一个问题。展示选项，说明你的建议，解释原因。不要将多个问题合并到一个 AskUserQuestion 中。只有在本节中所有问题都解决后才能进入下一节。

{{CONFIDENCE_CALIBRATION}}

### 2. 代码质量审查
评估：
* 代码组织和模块结构。
* DRY 违规——在这里要积极。
* 错误处理模式和缺失的边界情况（明确标出）。
* 技术债热点。
* 相对于我的偏好过度工程或工程不足的区域。
* 已触及文件中的现有 ASCII 图——变更后是否仍然准确？

**停止。** 对于本节中发现的每个问题，单独调用 AskUserQuestion。每次调用一个问题。展示选项，说明你的建议，解释原因。不要将多个问题合并到一个 AskUserQuestion 中。只有在本节中所有问题都解决后才能进入下一节。

### 3. 测试审查

{{TEST_COVERAGE_AUDIT_PLAN}}

对于 LLM/提示词变更：检查 CLAUDE.md 中列出的"Prompt/LLM changes"文件模式。如果此计划涉及这些模式中的任何一个，说明必须运行哪些评估套件，应添加哪些用例，以及与哪些基线进行比较。然后使用 AskUserQuestion 与用户确认评估范围。

**停止。** 对于本节中发现的每个问题，单独调用 AskUserQuestion。每次调用一个问题。展示选项，说明你的建议，解释原因。不要将多个问题合并到一个 AskUserQuestion 中。只有在本节中所有问题都解决后才能进入下一节。

### 4. 性能审查
评估：
* N+1 查询和数据库访问模式。
* 内存使用问题。
* 缓存机会。
* 缓慢或高复杂度的代码路径。

**停止。** 对于本节中发现的每个问题，单独调用 AskUserQuestion。每次调用一个问题。展示选项，说明你的建议，解释原因。不要将多个问题合并到一个 AskUserQuestion 中。只有在本节中所有问题都解决后才能进入下一节。

{{CODEX_PLAN_REVIEW}}

### 外部声音集成规则

外部声音的发现是信息性的，直到用户明确批准每个发现。
在通过 AskUserQuestion 呈现每个发现并获得明确批准之前，
不要将外部声音的建议纳入计划。即使你同意外部声音也适用。
跨模型共识是强烈信号——这样呈现它——但用户做决定。

## 关键规则——如何提问
遵循上方前言中的 AskUserQuestion 格式。计划审查的额外规则：
* **一个问题 = 一次 AskUserQuestion 调用。** 绝不将多个问题合并到一个问题中。
* 具体描述问题，包含文件和行引用。
* 提供 2-3 个选项，包括"不做任何事"（如果合理）。
* 对于每个选项，用一行说明：工作量（人工：约 X / CC：约 Y）、风险和维护负担。如果在使用 CC 时完整选项仅比捷径多一点点工作量，推荐完整选项。
* **将推理映射到我的工程偏好。** 用一句话将你的建议连接到特定偏好（DRY、明确 > 巧妙、最小 diff 等）。
* 用问题编号 + 选项字母标注（例如，"3A"、"3B"）。
* **覆盖度 vs 种类：** 对于你在本次审查中提出的每个问题的 AskUserQuestion，决定选项是覆盖度不同还是种类不同。如果覆盖度不同（例如更多测试 vs 更少测试、完整错误处理 vs 仅快乐路径、完整边界情况覆盖 vs 捷径），在每个选项上包含 `Completeness: N/10`。如果种类不同（例如两种不同系统之间的架构选择、姿态对姿态、A/B/C 每种都是不同类型的事物），跳过评分并添加一行：`注意：选项在种类上不同，而非覆盖度——不提供完整性分数。` 不要在种类不同的问题上编造分数——填充分数比没有分数更糟糕。
* **逃生舱（收紧）：** 如果某部分零发现，声明"无问题，继续"并继续。如果有发现，对每个发现使用 AskUserQuestion——有"明显修复"的发现仍然是发现，在变更进入计划之前仍需要用户批准。仅在决定真正微不足道（例如拼写错误修复）且没有有意义的替代方案时才跳过 AskUserQuestion。如有疑问，提问。

## 必需输出

### "不在范围内"部分
每个计划审查都**必须**产生一个"不在范围内"部分，列出已考虑并明确推迟的工作，每个项目一行理由。

### "已存在内容"部分
列出已经部分解决此计划中子问题的现有代码/流程，以及计划是复用它们还是不必要的重建。

### TODOS.md 更新
所有审查部分完成后，将每个潜在 TODO 作为单独的 AskUserQuestion 呈现。绝不批量处理 TODO——每个问题一个。绝不静默跳过此步骤。遵循 `.claude/skills/review/TODOS-format.md` 中的格式。

对于每个 TODO，描述：
* **是什么：** 一行描述。
* **为什么：** 它解决的具体问题或解锁的价值。
* **优点：** 做这项工作能获得什么。
* **缺点：** 成本、复杂性或风险。
* **上下文：** 足够的细节，让 3 个月后接手的人理解动机、当前状态和从哪里开始。
* **依赖/阻塞：** 任何前置条件或排序约束。

然后提供选项：**A）** 添加到 TODOS.md **B）** 跳过——价值不够 **C）** 在此 PR 中现在构建，而不是推迟。

不要只是追加模糊的要点。没有上下文的 TODO 比没有 TODO 更糟糕——它会创建一种想法已被捕获的虚假信心，而实际上丢失了推理。

### 图表
计划本身应对任何非平凡的数据流、状态机或处理管道使用 ASCII 图。此外，识别实现中哪些文件应获得内联 ASCII 图注释——特别是具有复杂状态转换的模型、具有多步骤管道的服务以及具有不明显混入行为的关注点。

### 故障模式
对于在测试审查图中识别的每个新代码路径，列出一种它在生产中可能失败的真实方式（超时、nil 引用、竞争条件、过期数据等），以及是否：
1. 有测试覆盖该故障
2. 有错误处理
3. 用户会看到清晰的错误还是静默失败

如果任何故障模式没有测试**且**没有错误处理**且**会是静默的，将其标记为**关键缺口**。

### 工作树并行化策略

分析计划的实现步骤以寻找并行执行机会。这有助于用户在 git 工作树之间拆分工作（通过 Claude Code 的 Agent 工具使用 `isolation: "worktree"` 或并行工作区）。

**跳过条件：** 所有步骤都触及相同的主模块，或计划的独立工作流少于 2 个。在这种情况下，写："顺序实现，无并行化机会。"

**否则，生成：**

1. **依赖表**——对于每个实现步骤/工作流：

| 步骤 | 触及的模块 | 依赖于 |
|------|-----------|--------|
| （步骤名） | （目录/模块，不是具体文件） | （其他步骤，或 —） |

在模块/目录级别工作，而不是文件级别。计划描述意图（"添加 API 端点"），而不是具体文件。模块级别（"controllers/, models/"）是可靠的；文件级别是猜测。

2. **并行通道**——将步骤分组到通道：
   - 没有共享模块且没有依赖的步骤放在不同的通道（并行）
   - 共享模块目录的步骤放在同一通道（顺序）
   - 依赖其他步骤的步骤放在后续通道

格式：`通道 A: step1 → step2（顺序，共享 models/）` / `通道 B: step3（独立）`

3. **执行顺序**——哪些通道并行启动，哪些等待。示例："在并行工作树中启动 A + B。合并两者。然后 C。"

4. **冲突标记**——如果两个并行通道触及相同的模块目录，标记："通道 X 和 Y 都触及 module/——可能的合并冲突。考虑顺序执行或仔细协调。"

### 完成摘要
在审查结束时，填写并显示此摘要，以便用户一目了然地看到所有发现：
- 第 0 步：范围挑战——___（范围按原样接受 / 根据建议缩减范围）
- 架构审查：发现 ___ 个问题
- 代码质量审查：发现 ___ 个问题
- 测试审查：已生成图，识别 ___ 个缺口
- 性能审查：发现 ___ 个问题
- 不在范围内：已编写
- 已存在内容：已编写
- TODOS.md 更新：向用户提议了 ___ 个项目
- 故障模式：标记了 ___ 个关键缺口
- 外部声音：已运行（codex/claude）/ 已跳过
- 并行化：___ 个通道，___ 个并行 / ___ 个顺序
- 湖泊评分：X/Y 个建议选择了完整选项

## 回顾性学习
检查此分支的 git 日志。如果有先前的提交表明之前的审查周期（例如，审查驱动的重构、回滚的变更），注意变更了什么以及当前计划是否触及相同的区域。对于之前有问题的区域，审查时要更积极。

## 格式规则
* 用数字标记问题（1、2、3...），用字母标记选项（A、B、C...）。
* 用数字 + 字母标注（例如，"3A"、"3B"）。
* 每个选项最多一句话。5 秒内选择。
* 在每个审查部分之后，暂停并征求反馈后再继续。

## 审查日志

在生成上述完成摘要后，持久化审查结果。

**计划模式例外——始终运行：** 此命令将审查元数据写入
`~/.gstack/`（用户配置目录，不是项目文件）。技能前言
已经写入 `~/.gstack/sessions/` 和 `~/.gstack/analytics/`——这是
相同的模式。审查仪表板依赖此数据。跳过此命令会
破坏 /ship 中的审查就绪仪表板。

```bash
.trae/skills/gstack/bin/gstack-review-log '{"skill":"plan-eng-review","timestamp":"TIMESTAMP","status":"STATUS","unresolved":N,"critical_gaps":N,"issues_found":N,"mode":"MODE","commit":"COMMIT"}'
```

从完成摘要中替换值：
- **TIMESTAMP**：当前 ISO 8601 日期时间
- **STATUS**：如果 0 个未解决决定**且**0 个关键缺口则为 "clean"；否则为 "issues_open"
- **unresolved**："未解决决定"的数量
- **critical_gaps**："故障模式：标记了 ___ 个关键缺口"中的数量
- **issues_found**：所有审查部分中发现的问题总数（架构 + 代码质量 + 性能 + 测试缺口）
- **MODE**：FULL_REVIEW / SCOPE_REDUCED
- **COMMIT**：`git rev-parse --short HEAD` 的输出

{{REVIEW_DASHBOARD}}

{{PLAN_FILE_REVIEW_REPORT}}

{{LEARNINGS_LOG}}

{{GBRAIN_SAVE_RESULTS}}

## 下一步——审查链

在显示审查就绪仪表板后，检查是否有额外审查有价值。阅读仪表板输出以查看哪些审查已运行以及是否过期。

**如果存在 UI 变更且未运行设计审查，建议 /plan-design-review**——从测试图、架构审查或触及前端组件、CSS、视图或面向用户的交互流的任何部分检测。如果现有设计审查的提交哈希显示它早于此工程审查中的重大变更，注明它可能已过期。

**如果是重大产品变更且不存在 CEO 审查，提及 /plan-ceo-review**——这是一个软性建议，不是推动。CEO 审查是可选的。仅在计划引入新的面向用户功能、改变产品方向或大幅扩大范围时才提及。

**注明过期**：如果此工程审查发现了与现有 CEO 或设计审查相矛盾的假设，或提交哈希显示重大偏离，则注明它们。

**如果不需要额外审查**（或仪表板配置中 `skip_eng_review` 为 `true`，表示此工程审查是可选的）：声明"所有相关审查已完成。准备好时运行 /ship。"

使用 AskUserQuestion，仅包含适用的选项：
- **A）** 运行 /plan-design-review（仅在检测到 UI 范围且不存在设计审查时）
- **B）** 运行 /plan-ceo-review（仅在重大产品变更且不存在 CEO 审查时）
- **C）** 准备实现——完成后运行 /ship

## 未解决的决定
如果用户没有响应 AskUserQuestion 或中断以继续，注意哪些决定未解决。在审查结束时，将这些列为"可能以后会困扰你的未解决决定"——绝不静默默认为某个选项。
