---
name: qa
preamble-tier: 4
version: 2.0.0
description: |
  系统化 QA 测试 Web 应用程序并修复发现的 bug。运行 QA 测试，
  然后迭代修复源代码中的 bug，每次原子性地提交每个修复并
  重新验证。当被要求 "qa"、"QA"、"test this site"（测试这个站点）、
  "find bugs"（查找 bug）、"test and fix"（测试并修复）或
  "fix what's broken"（修复损坏的东西）时使用此技能。
  当用户说功能已准备好进行测试或问 "这能用吗？" 时，
  主动建议执行此技能。三个级别：Quick（仅关键/高）、
  Standard（+ 中）、Exhaustive（+ 外观）。生成前后健康评分、
  修复证据和发布就绪摘要。仅报告模式使用 /qa-only。（gstack）
voice-triggers:
  - "quality check"
  - "test the app"
  - "run QA"
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - AskUserQuestion
  - WebSearch
triggers:
  - qa test this
  - find bugs on site
  - test the site
---

{{PREAMBLE}}

{{BASE_BRANCH_DETECT}}

{{GBRAIN_CONTEXT_LOAD}}

# /qa：测试 → 修复 → 验证

你既是 QA 工程师也是 bug 修复工程师。像真实用户一样测试 Web 应用程序——点击所有内容、填写每个表单、检查每个状态。当你发现 bug 时，在源代码中使用原子提交修复它们，然后重新验证。生成带有前后证据的结构化报告。

## 设置

**从用户的请求中解析以下参数：**

| 参数 | 默认值 | 覆盖示例 |
|-----------|---------|-----------------:|
| 目标 URL | （自动检测或必填） | `https://myapp.com`、`http://localhost:3000` |
| 级别 | Standard | `--quick`、`--exhaustive` |
| 模式 | full | `--regression .gstack/qa-reports/baseline.json` |
| 输出目录 | `.gstack/qa-reports/` | `Output to /tmp/qa` |
| 范围 | 完整应用（或 diff 范围） | `Focus on the billing page` |
| 认证 | 无 | `Sign in to user@example.com`、`Import cookies from cookies.json` |

**级别决定修复哪些问题：**
- **Quick：** 仅修复 critical + high 严重性
- **Standard：** + medium 严重性（默认）
- **Exhaustive：** + low/cosmetic 严重性

**如果没有给定 URL 且你在功能分支上：** 自动进入 **diff 感知模式**（见下方模式）。这是最常见的情况——用户刚刚在分支上发布了代码并想验证它能正常工作。

**CDP 模式检测：** 在开始之前，检查 browse 服务器是否连接到用户的真实浏览器：
```bash
$B status 2>/dev/null | grep -q "Mode: cdp" && echo "CDP_MODE=true" || echo "CDP_MODE=false"
```
如果 `CDP_MODE=true`：跳过 cookie 导入提示（真实浏览器已经有 cookie）、跳过用户代理覆盖（真实浏览器有真实用户代理）、跳过无头检测变通方法。用户的真实认证会话已经可用。

**检查干净的工作树：**

```bash
git status --porcelain
```

如果输出非空（工作树有未提交变更），**停止** 并使用 AskUserQuestion：

"你的工作树有未提交的变更。/qa 需要干净的树，这样每个 bug 修复都能有自己的原子提交。"

- A) 提交我的变更——用描述性消息提交所有当前变更，然后开始 QA
- B) 暂存我的变更——暂存，运行 QA，之后弹出暂存
- C) 中止——我会手动清理

推荐：选择 A，因为未提交的工作应该在 QA 添加自己的修复提交之前保存为提交。

用户选择后，执行他们的选择（提交或暂存），然后继续设置。

**查找 browse 二进制文件：**

{{BROWSE_SETUP}}

**检查测试框架（如果需要则引导）：**

{{TEST_BOOTSTRAP}}

**创建输出目录：**

```bash
mkdir -p .gstack/qa-reports/screenshots
```

---

{{LEARNINGS_SEARCH}}

## 测试计划上下文

在回退到 git diff 启发式之前，检查更丰富的测试计划来源：

1. **项目范围的测试计划：** 检查 `~/.gstack/projects/` 中此仓库的近期 `*-test-plan-*.md` 文件
   ```bash
   setopt +o nomatch 2>/dev/null || true  # zsh 兼容
   {{SLUG_EVAL}}
   ls -t ~/.gstack/projects/$SLUG/*-test-plan-*.md 2>/dev/null | head -1
   ```
2. **对话上下文：** 检查此对话中先前的 `/plan-eng-review` 或 `/plan-ceo-review` 是否生成了测试计划输出
3. **使用更丰富的来源。** 只有当两者都不可用时，才回退到 git diff 分析。

---

## 阶段 1-6：QA 基线

{{QA_METHODOLOGY}}

在阶段 6 结束时记录基线健康评分。

---

## 输出结构

```
.gstack/qa-reports/
├── qa-report-{domain}-{YYYY-MM-DD}.md    # 结构化报告
├── screenshots/
│   ├── initial.png                        # 着陆页带注释的截图
│   ├── issue-001-step-1.png               # 每个问题的证据
│   ├── issue-001-result.png
│   ├── issue-001-before.png               # 修复前（如果已修复）
│   ├── issue-001-after.png                # 修复后（如果已修复）
│   └── ...
└── baseline.json                          # 用于回归模式
```

报告文件名使用域名和日期：`qa-report-myapp-com-2026-03-12.md`

---

## 阶段 7：分类

按严重性排序所有发现的问题，然后根据选择的级别决定修复哪些：

- **Quick：** 仅修复 critical + high。将 medium/low 标记为 "deferred"（延后）。
- **Standard：** 修复 critical + high + medium。将 low 标记为 "deferred"。
- **Exhaustive：** 修复所有，包括 cosmetic/low 严重性。

标记无法从源代码修复的问题（例如，第三方组件 bug、基础设施问题），无论级别如何都标记为 "deferred"。

---

## 阶段 8：修复循环

对于每个可修复的问题，按严重性顺序：

### 8a. 定位源代码

```bash
# Grep 搜索错误消息、组件名称、路由定义
# Glob 搜索匹配受影响页面的文件模式
```

- 查找负责此 bug 的源代码文件
- **仅**修改与此问题直接相关的文件

### 8b. 修复

- 读取源代码，理解上下文
- 进行**最小修复**——解决问题的最小变更
- **不要**重构周围的代码、添加功能或"改进"无关的内容

### 8c. 提交

```bash
git add <only-changed-files>
git commit -m "fix(qa): ISSUE-NNN — 简短描述"
```

- 每个修复一次提交。永远不要将多个修复捆绑在一起。
- 消息格式：`fix(qa): ISSUE-NNN — 简短描述`

### 8d. 重新测试

- 导航回受影响的页面
- 拍摄**前后截图对**
- 检查控制台是否有错误
- 使用 `snapshot -D` 验证变更产生了预期效果

```bash
$B goto <affected-url>
$B screenshot "$REPORT_DIR/screenshots/issue-NNN-after.png"
$B console --errors
$B snapshot -D
```

### 8e. 分类

- **verified（已验证）：** 重新测试确认修复有效，没有引入新错误
- **best-effort（尽力而为）：** 已应用修复但无法完全验证（例如，需要认证状态、外部服务）
- **reverted（已回滚）：** 检测到回归 → `git revert HEAD` → 将问题标记为 "deferred"

### 8e.5. 回归测试

如果以下情况则跳过：分类不是 "verified"，或修复纯粹是视觉/CSS 且没有 JS 行为，或未检测到测试框架且用户拒绝引导。

**1. 研究项目现有的测试模式：**

读取最接近修复的 2-3 个测试文件（同一目录、同一代码类型）。精确匹配：
- 文件命名、导入、断言风格、describe/it 嵌套、设置/清理模式
回归测试必须看起来像是同一个开发者编写的。

**2. 追踪 bug 的代码路径，然后编写回归测试：**

在编写测试之前，追踪你刚修复的代码中的数据流：
- 什么输入/状态触发了 bug？（确切的前提条件）
- 它遵循了什么代码路径？（哪些分支、哪些函数调用）
- 它在哪里崩溃了？（确切失败的行/条件）
- 还有什么其他输入可能命中相同的代码路径？（修复周围的边界情况）

测试**必须**：
- 设置触发 bug 的前提条件（使其崩溃的确切状态）
- 执行暴露 bug 的操作
- 断言正确的行为（**不是** "它渲染了" 或 "它没有抛出"）
- 如果你在追踪时发现了相邻的边界情况，也测试它们（例如，null 输入、空数组、边界值）
- 包含完整的归属注释：
  ```
  // 回归测试：ISSUE-NNN — {什么崩溃了}
  // 由 /qa 于 {YYYY-MM-DD} 发现
  // 报告：.gstack/qa-reports/qa-report-{domain}-{date}.md
  ```

测试类型决策：
- 控制台错误 / JS 异常 / 逻辑 bug → 单元或集成测试
- 损坏的表单 / API 失败 / 数据流 bug → 带有请求/响应的集成测试
- 带有 JS 行为的视觉 bug（损坏的下拉菜单、动画） → 组件测试
- 纯 CSS → 跳过（由 QA 重新运行捕获）

生成单元测试。模拟所有外部依赖（数据库、API、Redis、文件系统）。

使用自动递增的名称以避免冲突：检查现有的 `{name}.regression-*.test.{ext}` 文件，取最大编号 + 1。

**3. 仅运行新的测试文件：**

```bash
{检测到的测试命令} {新测试文件}
```

**4. 评估：**
- 通过 → 提交：`git commit -m "test(qa): regression test for ISSUE-NNN — {描述}"`
- 失败 → 修复测试一次。仍然失败 → 删除测试，延后。
- 探索超过 2 分钟 → 跳过并延后。

**5. WTF 可能性排除：** 测试提交不计入该启发式。

### 8f. 自我调节（停止并评估）

每 5 次修复后（或任何回滚后），计算 WTF 可能性：

```
WTF 可能性：
  从 0% 开始
  每次回滚：                     +15%
  每次修复触及超过 3 个文件：     +5%
  第 15 次修复后：                每次额外修复 +1%
  剩余所有 Low 严重性：           +10%
  触及无关文件：                  +20%
```

**如果 WTF > 20%：** 立即停止。向用户展示你到目前为止做了什么。询问是否继续。

**硬上限：50 次修复。** 50 次修复后，无论剩余问题如何都停止。

---

## 阶段 9：最终 QA

应用所有修复后：

1. 在所有受影响页面上重新运行 QA
2. 计算最终健康评分
3. **如果最终评分比基线更差：** 显著警告——某些东西回归了

---

## 阶段 10：报告

将报告写入本地和项目范围的两个位置：

**本地：** `.gstack/qa-reports/qa-report-{domain}-{YYYY-MM-DD}.md`

**项目范围：** 写入测试产出工件以进行跨会话上下文：
```bash
{{SLUG_SETUP}}
```
写入到 `~/.gstack/projects/{slug}/{user}-{branch}-test-outcome-{datetime}.md`

**每个问题的附加内容**（超出标准报告模板）：
- 修复状态：verified / best-effort / reverted / deferred
- 提交 SHA（如果已修复）
- 变更的文件（如果已修复）
- 前后截图（如果已修复）

**摘要部分：**
- 发现的问题总数
- 应用的修复（已验证：X，尽力而为：Y，回滚：Z）
- 延后的问题
- 健康评分变化：基线 → 最终

**PR 摘要：** 包含一行摘要，适用于 PR 描述：
> "QA 发现 N 个问题，修复 M 个，健康评分 X → Y。"

---

## 阶段 11：TODOS.md 更新

如果仓库有 `TODOS.md`：

1. **新延后的 bug** → 作为 TODO 添加，包含严重性、类别和复现步骤
2. **TODOS.md 中已修复的 bug** → 标注 "由 /qa 于 {branch}，{date} 修复"

---

{{LEARNINGS_LOG}}

{{GBRAIN_SAVE_RESULTS}}

## 附加规则（qa 特有）

11. **需要干净的工作树。** 如果有未提交变更，使用 AskUserQuestion 提供提交/暂存/中止选项，然后继续。
12. **每个修复一次提交。** 永远不要将多个修复捆绑到一次提交中。
13. **仅在阶段 8e.5 生成回归测试时修改测试。** 永远不要修改 CI 配置。永远不要修改现有测试——只创建新的测试文件。
14. **回归时回滚。** 如果修复使情况更糟，立即 `git revert HEAD`。
15. **自我调节。** 遵循 WTF 可能性启发式。如有疑问，停止并询问。
