---
name: investigate
preamble-tier: 2
version: 1.0.0
description: |
  系统化调试与根因调查。四个阶段：调查、分析、假设、实施。
  铁律：没有根因就不修复。当被要求 "debug this"（调试这个）、"fix this bug"
  （修复这个 bug）、"why is this broken"（为什么坏了）、"investigate this error"
  （调查这个错误）或 "root cause analysis"（根因分析）时使用此技能。
  当用户报告错误、500 错误、堆栈跟踪、意外行为、"昨天还能用"，
  或在排查为什么某个功能停止工作时，请主动调用此技能
  （不要直接调试）。（gstack）
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Grep
  - Glob
  - AskUserQuestion
  - WebSearch
triggers:
  - debug this
  - fix this bug
  - why is this broken
  - root cause analysis
  - investigate this error
hooks:
  PreToolUse:
    - matcher: "Edit"
      hooks:
        - type: command
          command: "bash ${CLAUDE_SKILL_DIR}/../freeze/bin/check-freeze.sh"
          statusMessage: "检查调试范围边界..."
    - matcher: "Write"
      hooks:
        - type: command
          command: "bash ${CLAUDE_SKILL_DIR}/../freeze/bin/check-freeze.sh"
          statusMessage: "检查调试范围边界..."
---

{{PREAMBLE}}

# 系统化调试

## 铁律

**在根因调查之前绝不修复。**

修复症状只会导致打地鼠式的调试。每个不解决根因的修复都会让下一个 bug 更难找到。找到根因，然后修复它。

---

{{GBRAIN_CONTEXT_LOAD}}

## 阶段 1：根因调查

在形成任何假设之前先收集上下文。

1. **收集症状：** 读取错误消息、堆栈跟踪和复现步骤。如果用户没有提供足够的上下文，通过 AskUserQuestion 每次只问**一个问题**。

2. **读取代码：** 从症状回溯到潜在原因，追踪代码路径。使用 Grep 查找所有引用，使用 Read 理解逻辑。

3. **检查最近的变更：**
   ```bash
   git log --oneline -20 -- <affected-files>
   ```
   之前能用吗？什么变了？回归意味着根因就在 diff 中。

4. **复现：** 你能确定性地触发这个 bug 吗？如果不能，在继续之前收集更多证据。

5. **检查调查历史：** 在过往经验中搜索相同文件的调查。同一区域的反复出现的 bug 是架构异味的信号。如果存在先前的调查，注意模式并检查根因是否是结构性的。

{{LEARNINGS_SEARCH}}

输出：**"根因假设：..."**——关于什么地方出了问题以及为什么的具体的、可验证的声明。

---

## 范围锁定

在形成根因假设之后，将编辑限制在受影响的模块，以防止范围蔓延。

```bash
[ -x "${CLAUDE_SKILL_DIR}/../freeze/bin/check-freeze.sh" ] && echo "FREEZE_AVAILABLE" || echo "FREEZE_UNAVAILABLE"
```

**如果 FREEZE_AVAILABLE：** 识别包含受影响文件的最窄目录。将其写入冻结状态文件：

```bash
STATE_DIR="${CLAUDE_PLUGIN_DATA:-$HOME/.gstack}"
mkdir -p "$STATE_DIR"
echo "<detected-directory>/" > "$STATE_DIR/freeze-dir.txt"
echo "调试范围已锁定到：<detected-directory>/"
```

将 `<detected-directory>` 替换为实际的目录路径（例如 `src/auth/`）。告诉用户："在此调试会话中，编辑限制在 `<dir>/`。这可以防止对无关代码的变更。运行 `/unfreeze` 可移除此限制。"

如果 bug 跨越整个仓库或范围确实不清楚，跳过锁定并注明原因。

**如果 FREEZE_UNAVAILABLE：** 跳过范围锁定。编辑不受限制。

---

## 阶段 2：模式分析

检查此 bug 是否匹配已知模式：

| 模式 | 特征 | 在哪里查找 |
|---------|-----------|---------------|
| 竞态条件 | 间歇性、依赖时序 | 对共享状态的并发访问 |
| 空值传播 | NoMethodError、TypeError | 可选值上缺少保护 |
| 状态损坏 | 数据不一致、部分更新 | 事务、回调、钩子 |
| 集成失败 | 超时、意外响应 | 外部 API 调用、服务边界 |
| 配置漂移 | 本地能用、在预发/生产失败 | 环境变量、功能开关、数据库状态 |
| 缓存过期 | 显示旧数据、清除缓存后修复 | Redis、CDN、浏览器缓存、Turbo |

同时检查：
- `TODOS.md` 中相关的已知问题
- `git log` 中同一区域的先前修复——**同一文件中反复出现的 bug 是架构异味**，不是巧合

**外部模式搜索：** 如果 bug 不匹配上述已知模式，使用 WebSearch 搜索：
- "{框架} {通用错误类型}"——**先消毒：** 剥离主机名、IP、文件路径、SQL、客户数据。搜索错误类别，而不是原始消息。
- "{库} {组件} 已知问题"

如果 WebSearch 不可用，跳过此搜索并继续假设测试。如果发现了文档化的解决方案或已知的依赖 bug，将其作为阶段 3 的候选假设呈现。

---

## 阶段 3：假设测试

在编写**任何**修复之前，验证你的假设。

1. **确认假设：** 在怀疑的根因处添加临时日志语句、断言或调试输出。运行复现。证据是否匹配？

2. **如果假设错误：** 在形成下一个假设之前，考虑搜索该错误。**先消毒**——剥离主机名、IP、文件路径、SQL 片段、客户标识符和任何内部/专有数据。只搜索通用错误类型和框架上下文："{组件} {消毒后的错误类型} {框架版本}"。如果错误消息太具体而无法安全消毒，跳过搜索。如果 WebSearch 不可用，跳过并继续。然后返回阶段 1。收集更多证据。不要猜测。

3. **三次失败规则：** 如果 3 个假设都失败了，**停止**。使用 AskUserQuestion：
   ```
   已测试 3 个假设，都不匹配。这可能是架构问题
   而不是简单的 bug。

   A) 继续调查——我有一个新假设：[描述]
   B) 升级给人工审查——这需要了解系统的人
   C) 添加日志并等待——对该区域进行插桩，下次捕获它
   ```

**红旗**——如果你看到以下任何情况，放慢速度：
- "先快速修复一下"——没有"暂时"这回事。要么正确修复，要么升级。
- 在追踪数据流之前提出修复——你在猜测。
- 每个修复都在其他地方揭示新问题——是层级错了，不是代码错了。

---

## 阶段 4：实施

确认根因后：

1. **修复根因，而不是症状。** 消除实际问题的最小变更。

2. **最小化 diff：** 触及最少的文件，改动最少的行。抵制重构相邻代码的冲动。

3. **编写回归测试，该测试：**
   - 在没有修复时**失败**（证明测试是有意义的）
   - 在修复后**通过**（证明修复有效）

4. **运行完整的测试套件。** 粘贴输出。不允许回归。

5. **如果修复涉及超过 5 个文件：** 使用 AskUserQuestion 标记影响范围：
   ```
   此修复涉及 N 个文件。对于一个 bug 修复来说，影响范围很大。
   A) 继续——根因确实跨越了这些文件
   B) 拆分——先修复关键路径，延后其余部分
   C) 重新思考——可能有更有针对性的方法
   ```

---

## 阶段 5：验证与报告

**新鲜验证：** 复现原始 bug 场景并确认它已修复。这不是可选的。

运行测试套件并粘贴输出。

输出结构化的调试报告：
```
调试报告
════════════════════════════════════════
症状：         [用户观察到的现象]
根因：         [实际上什么地方出了问题]
修复：         [做了什么变更，附带 file:line 引用]
证据：         [测试输出、复现尝试显示修复有效]
回归测试：     [新测试的 file:line]
相关：         [TODOS.md 项、同一区域的先前 bug、架构说明]
状态：         DONE | DONE_WITH_CONCERNS | BLOCKED
════════════════════════════════════════
```

将调查记录为经验，供未来会话使用。使用 `type: "investigation"` 并包含受影响的文件，以便未来对同一区域的调查可以找到此记录：

```bash
.trae/skills/gstack/bin/gstack-learnings-log '{"skill":"investigate","type":"investigation","key":"ROOT_CAUSE_KEY","insight":"ROOT_CAUSE_SUMMARY","confidence":9,"source":"observed","files":["affected/file1.ts","affected/file2.ts"]}'
```

{{LEARNINGS_LOG}}

{{GBRAIN_SAVE_RESULTS}}

---

## 重要规则

- **3+ 次失败的修复尝试 → 停止并质疑架构。** 是架构错了，不是假设失败。
- **永远不要应用你无法验证的修复。** 如果你无法复现和确认，不要发布它。
- **永远不要说"这应该能修复它。"** 验证并证明它。运行测试。
- **如果修复涉及超过 5 个文件 → 在继续之前使用 AskUserQuestion 询问影响范围。**
- **完成状态：**
  - DONE——找到根因，应用修复，编写回归测试，所有测试通过
  - DONE_WITH_CONCERNS——已修复但无法完全验证（例如，间歇性 bug、需要预发环境）
  - BLOCKED——调查后根因不明确，已升级
