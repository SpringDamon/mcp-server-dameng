---
name: retro
preamble-tier: 2
version: 2.0.0
description: |
  每周工程回顾。分析提交历史、工作模式和
  代码质量指标，带有持久历史和趋势跟踪。
  团队感知：分解每个人的贡献，包括赞扬和成长空间。
  当被要求执行"每周回顾"、"我们交付了什么"或
  "工程回顾"时使用。
  在工作周或冲刺结束时主动建议。(gstack)
allowed-tools:
  - Bash
  - Read
  - Write
  - Glob
  - AskUserQuestion
triggers:
  - weekly retro
  - what did we ship
  - engineering retrospective
---

{{PREAMBLE}}

{{BASE_BRANCH_DETECT}}

# /retro — 每周工程回顾

生成全面的工程回顾，分析提交历史、工作模式和代码质量指标。
团队感知：识别运行命令的用户，然后分析每位贡献者，
包括每个人的赞扬和成长机会。专为使用 Claude Code 作为
力量倍增器的高级 IC/CTO 级构建者设计。

## 用户调用
当用户输入 `/retro` 时，运行此技能。

## 参数
- `/retro` — 默认：过去 7 天
- `/retro 24h` — 过去 24 小时
- `/retro 14d` — 过去 14 天
- `/retro 30d` — 过去 30 天
- `/retro compare` — 比较当前窗口与之前的相同长度窗口
- `/retro compare 14d` — 使用显式窗口比较
- `/retro global` — 跨所有 AI 编码工具的跨项目回顾（默认 7 天）
- `/retro global 14d` — 带显式窗口的跨项目回顾

{{GBRAIN_CONTEXT_LOAD}}

## 指令

解析参数以确定时间窗口。如果未提供参数，默认为 7 天。
所有时间应使用用户的**本地时区**报告（使用系统默认——不要设置 `TZ`）。

**午夜对齐窗口：** 对于天（`d`）和周（`w`）单位，计算当地午夜的绝对开始日期，
而不是相对字符串。例如，如果今天是 2026-03-18 且窗口是 7 天：
开始日期是 2026-03-11。使用 `--since="2026-03-11T00:00:00"` 进行 git log 查询——
显式的 `T00:00:00` 后缀确保 git 从午夜开始。没有它，git 使用当前挂钟时间
（例如 `--since="2026-03-11"` 在晚上 11 点意味着晚上 11 点，不是午夜）。
对于周单位，乘以 7 得到天数（例如 `2w` = 14 天前）。
对于小时（`h`）单位，使用 `--since="N hours ago"`，因为午夜对齐不适用于子天窗口。

**参数验证：** 如果参数不匹配数字后跟 `d`、`h` 或 `w`、
单词 `compare`（可选后跟窗口）或单词 `global`（可选后跟窗口），
显示此用法并停止：
```
Usage: /retro [window | compare | global]
  /retro              — last 7 days (default)
  /retro 24h          — last 24 hours
  /retro 14d          — last 14 days
  /retro 30d          — last 30 days
  /retro compare      — compare this period vs prior period
  /retro compare 14d  — compare with explicit window
  /retro global       — cross-project retro across all AI tools (7d default)
  /retro global 14d   — cross-project retro with explicit window
```

**如果第一个参数是 `global`：** 跳过正常的仓库范围回顾（步骤 1-14）。
相反，按照本文档末尾的**全局回顾**流程。
第二个可选参数是时间窗口（默认 7 天）。此模式不需要在 git 仓库内。

{{LEARNINGS_SEARCH}}

### 非 git 上下文（可选）

检查应包含在回顾中的非 git 上下文：

```bash
[ -f ~/.gstack/retro-context.md ] && echo "RETRO_CONTEXT_FOUND" || echo "NO_RETRO_CONTEXT"
```

如果 `RETRO_CONTEXT_FOUND`：读取 `~/.gstack/retro-context.md`。
此文件是用户撰写的，可能包含会议记录、日历事件、决策和其他
不会出现在 git 历史中的上下文。在相关时将此上下文整合到回顾叙述中。

### 步骤 1：获取原始数据

首先，获取 origin 并识别当前用户：
```bash
git fetch origin <default> --quiet
# 识别谁在运行回顾
git config user.name
git config user.email
```

`git config user.name` 返回的名称是**"你"**——阅读此回顾的人。
所有其他作者是队友。使用它来定位叙述："你的"提交 vs 队友贡献。

并行运行所有这些 git 命令（它们是独立的）：

```bash
# 1. 窗口内的所有提交，带有时间戳、主题、哈希、作者、更改的文件、插入、删除
git log origin/<default> --since="<window>" --format="%H|%aN|%ae|%ai|%s" --shortstat

# 2. 每个提交的测试 vs 总 LOC 分解，带作者
#    每个提交块以 COMMIT:<hash>|<author> 开头，后跟 numstat 行。
#    将测试文件（匹配 test/|spec/|__tests__/）与生产文件分开。
git log origin/<default> --since="<window>" --format="COMMIT:%H|%aN" --numstat

# 3. 提交时间戳用于会话检测和每小时分布（带作者）
git log origin/<default> --since="<window>" --format="%at|%aN|%ai|%s" | sort -n

# 4. 最频繁更改的文件（热点分析）
git log origin/<default> --since="<window>" --format="" --name-only | grep -v '^$' | sort | uniq -c | sort -rn

# 5. 来自提交消息的 PR/MR 编号（GitHub #NNN，GitLab !NNN）
git log origin/<default> --since="<window>" --format="%s" | grep -oE '[#!][0-9]+' | sort -t'#' -k1 | uniq

# 6. 每个作者的文件热点（谁接触什么）
git log origin/<default> --since="<window>" --format="AUTHOR:%aN" --name-only

# 7. 每个作者的提交计数（快速摘要）
git shortlog origin/<default> --since="<window>" -sn --no-merges

# 8. Greptile 分类历史（如果可用）
cat ~/.gstack/greptile-history.md 2>/dev/null || true

# 9. TODOS.md 待办事项（如果可用）
cat TODOS.md 2>/dev/null || true

# 10. 测试文件计数
find . -name '*.test.*' -o -name '*.spec.*' -o -name '*_test.*' -o -name '*_spec.*' 2>/dev/null | grep -v node_modules | wc -l

# 11. 窗口内的回归测试提交
git log origin/<default> --since="<window>" --oneline --grep="test(qa):" --grep="test(design):" --grep="test: coverage"

# 12. gstack 技能使用遥测（如果可用）
cat ~/.gstack/analytics/skill-usage.jsonl 2>/dev/null || true

# 12. 窗口内更改的测试文件
git log origin/<default> --since="<window>" --format="" --name-only | grep -E '\.(test|spec)\.' | sort -u | wc -l
```

### 步骤 2：计算指标

在摘要表中计算并展示这些指标：

| 指标 | 值 |
|--------|-------|
| **已交付功能**（来自 CHANGELOG + 合并的 PR 标题）| N |
| 对 main 的提交 | N |
| 加权提交（提交数 × 平均接触文件数，每个提交上限 20）| N |
| 贡献者 | N |
| 合并的 PR | N |
| **逻辑 SLOC 添加**（非空行、非注释——主要代码量指标）| N |
| 原始 LOC：插入 | N |
| 原始 LOC：删除 | N |
| 原始 LOC：净增 | N |
| 测试 LOC（插入）| N |
| 测试 LOC 比例 | N% |
| 版本范围 | vX.Y.Z.W → vX.Y.Z.W |
| 活跃天数 | N |
| 检测到的会话 | N |
| 平均每会话小时原始 LOC | N |
| Greptile 信号 | N%（Y 次捕获，Z 次误报）|
| 测试健康 | N 个总测试 · 本期间添加 M 个 · K 个回归测试 |

**指标顺序理由（V1）：** 已交付功能领先——用户得到了什么。
提交和加权提交反映交付意图。逻辑 SLOC 添加反映真正的新功能。
原始 LOC 被降级为上下文，因为 AI 会膨胀它；十行好的修复不比
一万行脚手架少交付。见 docs/designs/PLAN_TUNING_V1.md §Workstream C。

然后立即在下方显示**每个作者的排行榜**：

```
Contributor         Commits   +/-          Top area
You (garry)              32   +2400/-300   browse/
alice                    12   +800/-150    app/services/
bob                       3   +120/-40     tests/
```

按提交数降序排序。当前用户（来自 `git config user.name`）始终出现在第一位，
标记为 "You (name)"。

**Greptile 信号（如果历史存在）：** 读取 `~/.gstack/greptile-history.md`
（步骤 1 中获取，命令 8）。按日期过滤回顾时间窗口内的条目。
按类型计数：`fix`、`fp`、`already-fixed`。计算信号比例：
`(fix + already-fixed) / (fix + already-fixed + fp)`。如果窗口内没有条目
或文件不存在，跳过 Greptile 指标行。静默跳过无法解析的行。

**待办事项健康（如果 TODOS.md 存在）：** 读取 `TODOS.md`（步骤 1 中获取，命令 9）。计算：
- 总开放待办事项（排除 `## Completed` 部分中的项目）
- P0/P1 计数（紧急/关键项目）
- P2 计数（重要项目）
- 本期间完成的项目（已完成部分中日期在回顾窗口内的项目）
- 本期间添加的项目（交叉引用 git log 中窗口内修改 TODOS.md 的提交）

包含在指标表中：
```
| Backlog Health | N open (X P0/P1, Y P2) · Z completed this period |
```

如果 TODOS.md 不存在，跳过待办事项健康行。

**技能使用（如果分析存在）：** 如果存在，读取 `~/.gstack/analytics/skill-usage.jsonl`。
按 `ts` 字段过滤回顾时间窗口内的条目。将技能激活（无 `event` 字段）
与钩子触发（`event: "hook_fire"`）分开。按技能名称聚合。展示为：

```
| Skill Usage | /ship(12) /qa(8) /review(5) · 3 safety hook fires |
```

如果 JSONL 文件不存在或窗口内没有条目，跳过技能使用行。

**顿悟时刻（如果已记录）：** 如果存在，读取 `~/.gstack/analytics/eureka.jsonl`。
按 `ts` 字段过滤回顾时间窗口内的条目。对于每个顿悟时刻，显示标记它的技能、
分支和洞察的一行摘要。展示为：

```
| Eureka Moments | 2 this period |
```

如果存在时刻，列出它们：
```
  EUREKA /office-hours (branch: garrytan/auth-rethink): "Session tokens don't need server storage — browser crypto API makes client-side JWT validation viable"
  EUREKA /plan-eng-review (branch: garrytan/cache-layer): "Redis isn't needed here — Bun's built-in LRU cache handles this workload"
```

如果 JSONL 文件不存在或窗口内没有条目，跳过顿悟时刻行。

### 步骤 3：提交时间分布

使用条形图显示本地时间的每小时直方图：

```
Hour  Commits  ████████████████
 00:    4      ████
 07:    5      █████
 ...
```

识别并指出：
- 高峰小时
- 死区
- 模式是否为双峰（早上/晚上）或连续
- 深夜编码集群（晚上 10 点后）

### 步骤 4：工作会话检测

使用**45 分钟间隔**阈值检测连续提交之间的会话。对于每个会话报告：
- 开始/结束时间（太平洋时间）
- 提交数
- 持续时间（分钟）

分类会话：
- **深度会话**（50+ 分钟）
- **中等会话**（20-50 分钟）
- **微会话**（<20 分钟，通常是单次提交的即发即忘）

计算：
- 总活跃编码时间（会话持续时间总和）
- 平均会话长度
- 活跃时间每小时 LOC

### 步骤 5：提交类型分解

按约定提交前缀分类（feat/fix/refactor/test/chore/docs）。显示为百分比条：

```
feat:     20  (40%)  ████████████████████
fix:      27  (54%)  ███████████████████████████
refactor:  2  ( 4%)  ██
```

如果 fix 比例超过 50% 则标记——这可能表示"快速交付，快速修复"模式，
表明审查存在差距。

### 步骤 6：热点分析

显示前 10 个最常更改的文件。标记：
- 更改 5+ 次的文件（ churn 热点）
- 热点列表中的测试文件 vs 生产文件
- VERSION/CHANGELOG 频率（版本纪律指标）

### 步骤 7：PR 大小分布

从提交差异中估算 PR 大小并分组：
- **小**（<100 LOC）
- **中**（100-500 LOC）
- **大**（500-1500 LOC）
- **XL**（1500+ LOC）

### 步骤 8：专注分数 + 本周交付

**专注分数：** 计算接触单个最常更改的顶级目录的提交百分比
（例如 `app/services/`、`app/views/`）。分数越高 = 越深入专注的工作。
分数越低 = 分散的上下文切换。报告为："Focus score: 62% (app/services/)"

**本周交付：** 自动识别窗口中单个最高 LOC 的 PR。突出显示：
- PR 编号和标题
- LOC 更改
- 为什么重要（从提交消息和接触的文件推断）

### 步骤 9：团队成员分析

对于每位贡献者（包括当前用户），计算：

1. **提交和 LOC**——总提交数、插入、删除、净 LOC
2. **专注领域**——他们接触最多的目录/文件（前 3 个）
3. **提交类型混合**——他们个人的 feat/fix/refactor/test 分解
4. **会话模式**——他们何时编码（他们的高峰小时）、会话计数
5. **测试纪律**——他们个人的测试 LOC 比例
6. **最大交付**——他们在窗口中单个影响最大的提交或 PR

**对于当前用户（"你"）：** 此部分获得最深入的对待。包括来自单独回顾的所有细节——
会话分析、时间模式、专注分数。用第一人称框架："你的高峰小时..."、
"你的最大交付..."

**对于每位队友：** 写 2-3 句关于他们工作和模式的内容。然后：

- **赞扬**（1-2 件具体的事）：锚定在实际提交中。不是"做得好"——
  准确说出什么好。示例："在 3 个专注的会话中以 45% 测试覆盖率交付了整个
  认证中间件重写"、"每个 PR 低于 200 LOC——纪律分解。"
- **成长机会**（1 件具体的事）：框架为升级建议，不是批评。
  锚定在实际数据中。示例："本周测试比例为 12%——在支付模块变得更复杂
  之前添加测试覆盖率会有回报"、"同一文件上有 5 个 fix 提交表明原始 PR
  可能需要审查。"

**如果只有一位贡献者（单独仓库）：** 跳过团队分解并按之前继续——
回顾是个人的。

**如果有 Co-Authored-By 尾注：** 解析提交消息中的 `Co-Authored-By:` 行。
将这些作者与主要作者一起计入提交。注意 AI 共同作者
（例如 `noreply@anthropic.com`）但不要把作为团队成员包括——
而是将"AI 辅助提交"跟踪为单独的指标。

{{LEARNINGS_LOG}}

{{GBRAIN_SAVE_RESULTS}}

### 步骤 10：周复一周趋势（如果窗口 >= 14d）

如果时间窗口是 14 天或更长，拆分为每周桶并显示趋势：
- 每周提交（总计和每个作者）
- 每周 LOC
- 每周测试比例
- 每周 fix 比例
- 每周会话计数

### 步骤 11：连续跟踪

计算至少有 1 次提交到 origin/<default> 的连续天数，从今天往回。
跟踪团队连续和个人连续：

```bash
# 团队连续：所有唯一提交日期（本地时间）——无硬截止
git log origin/<default> --format="%ad" --date=format:"%Y-%m-%d" | sort -u

# 个人连续：仅当前用户的提交
git log origin/<default> --author="<user_name>" --format="%ad" --date=format:"%Y-%m-%d" | sort -u
```

从今天往回数——有多少连续天至少有一次提交？这查询完整历史，
因此任何长度的连续都能准确报告。显示两者：
- "Team shipping streak: 47 consecutive days"
- "Your shipping streak: 32 consecutive days"

### 步骤 12：加载历史与比较

在保存新快照之前，检查先前的回顾历史：

```bash
setopt +o nomatch 2>/dev/null || true  # zsh 兼容
ls -t .context/retros/*.json 2>/dev/null
```

**如果存在先前回顾：** 使用 Read 工具加载最近的一个。计算关键指标的差异
并包含**与上次回顾的趋势**部分：
```
                    Last        Now         Delta
Test ratio:         22%    →    41%         ↑19pp
Sessions:           10     →    14          ↑4
LOC/hour:           200    →    350         ↑75%
Fix ratio:          54%    →    30%         ↓24pp (improving)
Commits:            32     →    47          ↑47%
Deep sessions:      3      →    5           ↑2
```

**如果不存在先前回顾：** 跳过比较部分并附加："First retro recorded — run again next week to see trends."

### 步骤 13：保存回顾历史

在计算所有指标（包括连续）并加载任何先前历史进行比较后，保存 JSON 快照：

```bash
mkdir -p .context/retros
```

确定今天的下一个序列号（用实际日期替换 `$(date +%Y-%m-%d)`）：
```bash
setopt +o nomatch 2>/dev/null || true  # zsh 兼容
# 计算今天的现有回顾数以获取下一个序列号
today=$(date +%Y-%m-%d)
existing=$(ls .context/retros/${today}-*.json 2>/dev/null | wc -l | tr -d ' ')
next=$((existing + 1))
# 保存为 .context/retros/${today}-${next}.json
```

使用 Write 工具保存具有以下 schema 的 JSON 文件：
```json
{
  "date": "2026-03-08",
  "window": "7d",
  "metrics": {
    "commits": 47,
    "contributors": 3,
    "prs_merged": 12,
    "insertions": 3200,
    "deletions": 800,
    "net_loc": 2400,
    "test_loc": 1300,
    "test_ratio": 0.41,
    "active_days": 6,
    "sessions": 14,
    "deep_sessions": 5,
    "avg_session_minutes": 42,
    "loc_per_session_hour": 350,
    "feat_pct": 0.40,
    "fix_pct": 0.30,
    "peak_hour": 22,
    "ai_assisted_commits": 32
  },
  "authors": {
    "Garry Tan": { "commits": 32, "insertions": 2400, "deletions": 300, "test_ratio": 0.41, "top_area": "browse/" },
    "Alice": { "commits": 12, "insertions": 800, "deletions": 150, "test_ratio": 0.35, "top_area": "app/services/" }
  },
  "version_range": ["1.16.0.0", "1.16.1.0"],
  "tweetable": "Week of Mar 1: 47 commits (3 contributors), 3.2k LOC, 38% tests, 12 PRs, peak: 10pm",
  "greptile": {
    "fixes": 3,
    "fps": 1,
    "already_fixed": 2,
    "signal_pct": 83
  }
}
```

**注意：** 仅当 `~/.gstack/greptile-history.md` 存在且时间窗口内有条目时才包含 `greptile` 字段。
仅当 `TODOS.md` 存在时才包含 `backlog` 字段。仅当找到测试文件时才包含 `test_health` 字段
（命令 10 返回 > 0）。如果任何没有数据，完全省略该字段。

当测试文件存在时，在 JSON 中包含测试健康数据：
```json
  "test_health": {
    "total_test_files": 47,
    "tests_added_this_period": 5,
    "regression_test_commits": 3,
    "test_files_changed": 8
  }
```

当 TODOS.md 存在时，在 JSON 中包含待办事项数据：
```json
  "backlog": {
    "total_open": 28,
    "p0_p1": 2,
    "p2": 8,
    "completed_this_period": 3,
    "added_this_period": 1
  }
```

### 步骤 14：编写叙述

将输出结构化为：

---

**可推文摘要**（第一行，在所有内容之前）：
```
Week of Mar 1: 47 commits (3 contributors), 3.2k LOC, 38% tests, 12 PRs, peak: 10pm | Streak: 47d
```

## Engineering Retro: [date range]

### Summary Table
（来自步骤 2）

### Trends vs Last Retro
（来自步骤 11，在保存之前加载——如果是第一次回顾则跳过）

### Time & Session Patterns
（来自步骤 3-4）

叙述解释团队范围模式意味着什么：
- 最高效的小时是什么时候以及什么驱动它们
- 会话是否随时间变长或变短
- 估计每天活跃编码的小时数（团队聚合）
- 显著模式：团队成员是否同时编码或轮班？

### Shipping Velocity
（来自步骤 5-7）

叙述覆盖：
- 提交类型混合及其揭示的内容
- PR 大小分布及其揭示的交付节奏
- 修复链检测（同一子系统上的 fix 提交序列）
- 版本升级纪律

### Code Quality Signals
- 测试 LOC 比例趋势
- 热点分析（相同的文件是否在 churn？）
- Greptile 信号比例和趋势（如果历史存在）："Greptile: X% signal (Y valid catches, Z false positives)"

### Test Health
- 总测试文件：N（来自命令 10）
- 本期间添加的测试：M（来自命令 12——更改的测试文件）
- 回归测试提交：列出命令 11 中的 `test(qa):`、`test(design):` 和 `test: coverage` 提交
- 如果先前回顾存在且有 `test_health`：显示差异 "Test count: {last} → {now} (+{delta})"
- 如果测试比例 < 20%：标记为成长区域——"100% 测试覆盖率是目标。测试让 vibe coding 安全。"

### Plan Completion
从本期间 /ship 运行的 review JSONL 日志中检查计划完成数据：

```bash
setopt +o nomatch 2>/dev/null || true  # zsh 兼容
eval "$(.trae/skills/gstack/bin/gstack-slug 2>/dev/null)"
cat ~/.gstack/projects/$SLUG/*-reviews.jsonl 2>/dev/null | grep '"skill":"ship"' | grep '"plan_items_total"' || echo "NO_PLAN_DATA"
```

如果回顾时间窗口内存在计划完成数据：
- 计数有计划交付的分支（具有 `plan_items_total` > 0 的条目）
- 计算平均完成：`plan_items_done` 总和 / `plan_items_total` 总和
- 如果数据支持，识别最常跳过的项目类别

输出：
```
Plan Completion This Period:
  {N} branches shipped with plans
  Average completion: {X}% ({done}/{total} items)
```

如果不存在计划数据，静默跳过此部分。

### Focus & Highlights
（来自步骤 8）
- 专注分数及解读
- 本周交付亮点

### Your Week（个人深入分析）
（来自步骤 9，仅针对当前用户）

这是用户最关心的部分。包括：
- 他们的个人提交计数、LOC、测试比例
- 他们的会话模式和高峰小时
- 他们的专注领域
- 他们的最大交付
- **你做得好的地方**（2-3 件锚定在提交中的具体事情）
- **在哪里升级**（1-2 个具体的、可操作的建议）

### Team Breakdown
（来自步骤 9，针对每位队友——如果是单独仓库则跳过）

对于每位队友（按提交数降序排序），写一个部分：

#### [Name]
- **他们交付了什么**：2-3 句关于他们的贡献、专注领域和提交模式
- **赞扬**：1-2 件他们做得好的具体事情，锚定在实际提交中。
  真诚——你会在 1:1 中实际说什么？示例：
  - "在 3 个小型、可审查的 PR 中清理了整个认证模块——教科书式分解"
  - "为每个新端点添加了集成测试，不仅是快乐路径"
  - "修复了导致仪表板 2 秒加载时间的 N+1 查询"
- **成长机会**：1 个具体的、建设性的建议。框架为投资，不是批评。示例：
  - "支付模块的测试覆盖率为 8%——值得在下一个功能落在它上面之前投资"
  - "大多数提交在一次爆发中着陆——将工作分散在一天中可以减少上下文切换疲劳"
  - "所有提交都在凌晨 1-4 点之间着陆——可持续的节奏对长期代码质量很重要"

**AI 协作者注：** 如果许多提交有 `Co-Authored-By` AI 尾注（例如 Claude、Copilot），
将 AI 辅助提交百分比作为团队指标记录。中性框架——"N% 的提交是 AI 辅助的"——
不做判断。

### Top 3 Team Wins
识别窗口中整个团队交付的 3 个最高影响的事情。对于每个：
- 是什么
- 谁交付的
- 为什么重要（产品/架构影响）

### 3 Things to Improve
具体的、可操作的、锚定在实际提交中。混合个人和团队级别的建议。
措辞为"为了变得更好，团队可以..."

### 3 Habits for Next Week
小的、实用的、现实的。每个必须是采用时间 <5 分钟的事情。
至少一个应该是面向团队的（例如"当天审查彼此的 PR"）。

### Week-over-Week Trends
（如果适用，来自步骤 10）

---

## 全局回顾模式

当用户运行 `/retro global`（或 `/retro global 14d`）时，遵循此流程而不是
仓库范围的步骤 1-14。此模式适用于任何目录——它不需要在 git 仓库内。

### 全局步骤 1：计算时间窗口

与常规回顾相同的午夜对齐逻辑。默认 7 天。`global` 后的第二个参数是窗口
（例如 `14d`、`30d`、`24h`）。

### 全局步骤 2：运行发现

使用此回退链定位并运行发现脚本：

```bash
DISCOVER_BIN=""
[ -x .trae/skills/gstack/bin/gstack-global-discover ] && DISCOVER_BIN=.trae/skills/gstack/bin/gstack-global-discover
[ -z "$DISCOVER_BIN" ] && [ -x .trae/skills/gstack/bin/gstack-global-discover ] && DISCOVER_BIN=.trae/skills/gstack/bin/gstack-global-discover
[ -z "$DISCOVER_BIN" ] && which gstack-global-discover >/dev/null 2>&1 && DISCOVER_BIN=$(which gstack-global-discover)
[ -z "$DISCOVER_BIN" ] && [ -f bin/gstack-global-discover.ts ] && DISCOVER_BIN="bun run bin/gstack-global-discover.ts"
echo "DISCOVER_BIN: $DISCOVER_BIN"
```

如果未找到二进制文件，告诉用户："Discovery script not found. Run `bun run build` in the gstack directory to compile it." 并停止。

运行发现：
```bash
$DISCOVER_BIN --since "<window>" --format json 2>/tmp/gstack-discover-stderr
```

从 `/tmp/gstack-discover-stderr` 读取 stderr 输出以获取诊断信息。
从 stdout 解析 JSON 输出。

如果 `total_sessions` 为 0，说："No AI coding sessions found in the last <window>. Try a longer window: `/retro global 30d`" 并停止。

### 全局步骤 3：对每个发现的仓库运行 git log

对于发现 JSON 的 `repos` 数组中的每个仓库，在 `paths[]` 中找到第一个有效路径
（目录存在且带有 `.git/`）。如果不存在有效路径，跳过仓库并注明。

**对于本地仓库**（`remote` 以 `local:` 开头）：跳过 `git fetch` 并使用本地默认分支。
使用 `git log HEAD` 而不是 `git log origin/$DEFAULT`。

**对于有远程的仓库：**

```bash
git -C <path> fetch origin --quiet 2>/dev/null
```

检测每个仓库的默认分支：首先尝试 `git symbolic-ref refs/remotes/origin/HEAD`，
然后检查常见分支名称（`main`、`master`），然后回退到
`git rev-parse --abbrev-ref HEAD`。使用检测到的分支作为以下命令中的 `<default>`。

```bash
# 提交带统计
git -C <path> log origin/$DEFAULT --since="<start_date>T00:00:00" --format="%H|%aN|%ai|%s" --shortstat

# 提交时间戳用于会话检测、连续和上下文切换
git -C <path> log origin/$DEFAULT --since="<start_date>T00:00:00" --format="%at|%aN|%ai|%s" | sort -n

# 每个作者提交计数
git -C <path> shortlog origin/$DEFAULT --since="<start_date>T00:00:00" -sn --no-merges

# 来自提交消息的 PR/MR 编号（GitHub #NNN，GitLab !NNN）
git -C <path> log origin/$DEFAULT --since="<start_date>T00:00:00" --format="%s" | grep -oE '[#!][0-9]+' | sort -t'#' -k1 | uniq
```

对于失败的仓库（删除的路径、网络错误）：跳过并注明 "N repos could not be reached."

### 全局步骤 4：计算全局交付连续

对于每个仓库，获取提交日期（上限 365 天）：

```bash
git -C <path> log origin/$DEFAULT --since="365 days ago" --format="%ad" --date=format:"%Y-%m-%d" | sort -u
```

联合所有仓库的所有日期。从今天往回数——有多少连续天至少有一次提交到任何仓库？
如果连续达到 365 天，显示为 "365+ days"。

### 全局步骤 5：计算上下文切换指标

从步骤 3 收集的提交时间戳中，按日期分组。对于每个日期，计算当天有多少
不同的仓库有提交。报告：
- 平均每天仓库数
- 最大每天仓库数
- 哪些天是专注的（1 个仓库）vs. 碎片化的（3+ 仓库）

### 全局步骤 6：每个工具的生产力模式

从发现 JSON 中，分析工具使用模式：
- 哪个 AI 工具用于哪些仓库（独占 vs. 共享）
- 每个工具的会话计数
- 行为模式（例如，"Codex 专用于 myapp，Claude Code 用于其他一切"）

### 全局步骤 7：聚合并生成叙述

将输出结构化为**可分享的个人卡片优先**，然后是完整的
团队/项目分解。个人卡片设计为适合截图分享
——某人想在 X/Twitter 上分享的所有内容都在一个干净的块中。

---

**可推文摘要**（第一行，在所有内容之前）：
```
Week of Mar 14: 5 projects, 138 commits, 250k LOC across 5 repos | 48 AI sessions | Streak: 52d 🔥
```

## 🚀 Your Week: [user name] — [date range]

此部分是**可分享的个人卡片**。它仅包含当前用户的
统计数据——没有团队数据、没有项目分解。设计用于截图和发布。

使用 `git config user.name` 的用户身份过滤每个仓库的 git 数据。
跨所有仓库聚合以计算个人总计。

渲染为单个视觉上干净的块。仅左边框——没有右边框（LLM
无法可靠地对齐右边框）。将仓库名填充到最长的名称，以便列
对齐干净。永远不要截断项目名称。

```
╔═══════════════════════════════════════════════════════════════
║  [USER NAME] — Week of [date]
╠═══════════════════════════════════════════════════════════════
║
║  [N] commits across [M] projects
║  +[X]k LOC added · [Y]k LOC deleted · [Z]k net
║  [N] AI coding sessions (CC: X, Codex: Y, Gemini: Z)
║  [N]-day shipping streak 🔥
║
║  PROJECTS
║  ─────────────────────────────────────────────────────────
║  [repo_name_full]        [N] commits    +[X]k LOC    [solo/team]
║  [repo_name_full]        [N] commits    +[X]k LOC    [solo/team]
║  [repo_name_full]        [N] commits    +[X]k LOC    [solo/team]
║
║  SHIP OF THE WEEK
║  [PR title] — [LOC] lines across [N] files
║
║  TOP WORK
║  • [1-line description of biggest theme]
║  • [1-line description of second theme]
║  • [1-line description of third theme]
║
║  Powered by gstack
╚═══════════════════════════════════════════════════════════════
```

**个人卡片规则：**
- 仅显示用户有提交的仓库。跳过 0 提交的仓库。
- 按用户的提交数降序排序仓库。
- **永远不要截断仓库名称。** 使用完整的仓库名称（例如 `analyze_transcripts`
  不是 `analyze_trans`）。将名称列填充到最长的仓库名称，以便所有列
  对齐。如果名称很长，加宽框——框宽度适应内容。
- 对于 LOC，使用 "k" 格式表示千（例如 "+64.0k" 不是 "+64010"）。
- 角色：如果用户是唯一贡献者则为 "solo"，如果其他人贡献则为 "team"。
- 本周交付：用户跨所有仓库的单个最高 LOC 的 PR。
- 顶部工作：3 个要点总结用户的主要主题，从提交消息推断。
  不是单个提交——综合为主题。
  例如 "Built /retro global — cross-project retrospective with AI session discovery"
  不是 "feat: gstack-global-discover" + "feat: /retro global template"。
- 卡片必须是自包含的。看到此块的人应该理解
  用户的周，无需任何周围上下文。
- 不要在此处包括团队成员、项目总数或上下文切换数据。

**个人连续：** 使用用户跨所有仓库的自己的提交（通过 `--author` 过滤）
计算个人连续，与团队连续分开。

---

## Global Engineering Retro: [date range]

以下是完整分析——团队数据、项目分解、模式。
这是跟随可分享卡片的"深入分析"。

### All Projects Overview
| Metric | Value |
|--------|-------|
| Projects active | N |
| Total commits (all repos, all contributors) | N |
| Total LOC | +N / -N |
| AI coding sessions | N (CC: X, Codex: Y, Gemini: Z) |
| Active days | N |
| Global shipping streak (any contributor, any repo) | N consecutive days |
| Context switches/day | N avg (max: M) |

### Per-Project Breakdown
对于每个仓库（按提交数降序排序）：
- 仓库名称（占总提交的 %）
- 提交、LOC、合并的 PR、主要贡献者
- 关键工作（从提交消息推断）
- 每个工具的 AI 会话

**你的贡献**（每个项目中的子部分）：
对于每个项目，添加一个"你的贡献"块，显示当前用户在
该仓库中的个人统计数据。使用 `git config user.name` 的用户身份过滤。包括：
- 你的提交 / 总提交（带 %）
- 你的 LOC（+插入 / -删除）
- 你的关键工作（仅从你的提交消息推断）
- 你的提交类型混合（feat/fix/refactor/chore/docs 分解）
- 你在此仓库中的最大交付（最高 LOC 的提交或 PR）

如果用户是唯一贡献者，说 "Solo project — all commits are yours."
如果用户在仓库中有 0 提交（本期间未接触的团队项目），
说 "No commits this period — [N] AI sessions only." 并跳过分述。

格式：
```
**Your contributions:** 47/244 commits (19%), +4.2k/-0.3k LOC
  Key work: Writer Chat, email blocking, security hardening
  Biggest ship: PR #605 — Writer Chat eats the admin bar (2,457 ins, 46 files)
  Mix: feat(3) fix(2) chore(1)
```

### Cross-Project Patterns
- 跨项目的时间分配（% 分解，使用你的提交而不是总数）
- 跨所有仓库聚合的高峰生产力小时
- 专注 vs. 碎片化的天数
- 上下文切换趋势

### Tool Usage Analysis
每个工具的分解与行为模式：
- Claude Code：N 个会话跨 M 个仓库——观察到的模式
- Codex：N 个会话跨 M 个仓库——观察到的模式
- Gemini：N 个会话跨 M 个仓库——观察到的模式

### Ship of the Week (Global)
跨所有项目的最高影响 PR。通过 LOC 和提交消息识别。

### 3 Cross-Project Insights
全局视图揭示了什么单个仓库回顾无法显示的内容。

### 3 Habits for Next Week
考虑完整的跨项目图景。

---

### 全局步骤 8：加载历史与比较

```bash
setopt +o nomatch 2>/dev/null || true  # zsh 兼容
ls -t ~/.gstack/retros/global-*.json 2>/dev/null | head -5
```

**仅与具有相同 `window` 值的先前回顾进行比较**（例如 7d vs 7d）。
如果最近的先前回顾有不同的窗口，跳过比较并注明：
"Prior global retro used a different window — skipping comparison."

如果存在匹配的先前回顾，使用 Read 工具加载它。
显示**与上次全局回顾的趋势**表，包含关键指标的差异：
总提交、LOC、会话、连续、每天上下文切换。

如果不存在先前的全局回顾，附加："First global retro recorded — run again next week to see trends."

### 全局步骤 9：保存快照

```bash
mkdir -p ~/.gstack/retros
```

确定今天的下一个序列号：
```bash
setopt +o nomatch 2>/dev/null || true  # zsh 兼容
today=$(date +%Y-%m-%d)
existing=$(ls ~/.gstack/retros/global-${today}-*.json 2>/dev/null | wc -l | tr -d ' ')
next=$((existing + 1))
```

使用 Write 工具将 JSON 保存到 `~/.gstack/retros/global-${today}-${next}.json`：

```json
{
  "type": "global",
  "date": "2026-03-21",
  "window": "7d",
  "projects": [
    {
      "name": "gstack",
      "remote": "<detected from git remote get-url origin, normalized to HTTPS>",
      "commits": 47,
      "insertions": 3200,
      "deletions": 800,
      "sessions": { "claude_code": 15, "codex": 3, "gemini": 0 }
    }
  ],
  "totals": {
    "commits": 182,
    "insertions": 15300,
    "deletions": 4200,
    "projects": 5,
    "active_days": 6,
    "sessions": { "claude_code": 48, "codex": 8, "gemini": 3 },
    "global_streak_days": 52,
    "avg_context_switches_per_day": 2.1
  },
  "tweetable": "Week of Mar 14: 5 projects, 182 commits, 15.3k LOC | CC: 48, Codex: 8, Gemini: 3 | Focus: gstack (58%) | Streak: 52d"
}
```

---

## 比较模式

当用户运行 `/retro compare`（或 `/retro compare 14d`）时：

1. 使用午夜对齐的开始日期计算当前窗口的指标（默认 7d）
   （与主回顾相同的逻辑——例如，如果今天是 2026-03-18 且窗口是 7d，
   使用 `--since="2026-03-11T00:00:00"`）
2. 使用 `--since` 和 `--until` 与午夜对齐的日期计算先前相同长度窗口的指标，
   以避免重叠（例如，对于 2026-03-11 开始的 7d 窗口：先前窗口是
   `--since="2026-03-04T00:00:00" --until="2026-03-11T00:00:00"`）
3. 显示带有差异和箭头的并排比较表
4. 编写简短叙述，突出最大的改进和回退
5. 仅将当前窗口快照保存到 `.context/retros/`（与正常回顾运行相同）；
   **不要**持久化先前窗口指标。

## 语气

- 鼓励但坦诚，不溺爱
- 具体和具体——始终锚定在实际提交/代码中
- 跳过通用赞美（"做得好！"）——准确说出什么好以及为什么
- 将改进框架为升级，不是批评
- **赞扬应该感觉像你在 1:1 中实际说的话**——具体的、应得的、真诚的
- **成长建议应该感觉像投资建议**——"这值得你的时间因为..."不是"你失败在..."
- 永远不要消极地将队友相互比较。每个人的部分都独立存在。
- 保持总输出约 3000-4500 字（稍长以容纳团队部分）
- 使用 markdown 表和代码块表示数据，散文表示叙述
- 直接输出到对话——不要写入文件系统（除了 `.context/retros/` JSON 快照）

## 重要规则

- 所有叙述输出直接在对话中给用户。唯一写入的文件是 `.context/retros/` JSON 快照。
- 对所有 git 查询使用 `origin/<default>`（不是可能过时的本地 main）
- 以用户的本地时区显示所有时间戳（不要覆盖 `TZ`）
- 如果窗口有零提交，这么说并建议不同的窗口
- 将 LOC/小时舍入到最接近的 50
- 将合并提交视为 PR 边界
- 不要读取 CLAUDE.md 或其他文档——此技能是自包含的
- 在首次运行（无先前回顾）时，优雅地跳过比较部分
- **全局模式：** 不需要在 git 仓库内。将快照保存到 `~/.gstack/retros/`
  （不是 `.context/retros/`）。优雅地跳过未安装的 AI 工具。仅与具有相同窗口值的
  先前全局回顾进行比较。如果连续达到 365 天上限，显示为 "365+ days"。
