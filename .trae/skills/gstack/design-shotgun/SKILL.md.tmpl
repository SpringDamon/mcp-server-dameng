---
name: design-shotgun
preamble-tier: 3
version: 1.0.0
description: |
  从设计参考或自然语言提示生成前端组件样机。
  将提示转化为精美的 React + Tailwind 组件，带有适当的语义 HTML、
  响应式设计和可访问性。当被要求 "generate mockup"（生成样机）、
  "design component"（设计组件）、"create UI from screenshot"（从截图创建 UI）、
  "build frontend"（构建前端）或 "design-shotgun"（设计散弹枪）时使用此技能。
  当用户展示设计参考并要求"做成这样"时，主动调用此技能。（gstack）
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - AskUserQuestion
triggers:
  - generate mockup
  - design component from
  - create UI from screenshot
  - build this frontend
---

{{PREAMBLE}}

# design-shotgun：设计 → 代码

将设计参考或自然语言提示转化为精美的前端组件样机。

## 输入

接受以下任一输入：
- 截图或设计参考文件（图片路径或 URL）
- 自然语言提示（"一个带有头像、用户名和推文的推文卡片"）
- 对现有组件的引用（"让设置页面看起来像 Linear 的"）

## 输出

生成以下工件：
- React 组件文件（`components/{name}.tsx`）
- 组件截图（`designs/{name}-{timestamp}.png`）
- 可选：如果用户要求，包含使用示例

## 工作流

### 阶段 1：理解参考

**如果有截图或设计文件：**
1. 读取图片文件
2. 分析视觉结构：布局、层次、间距、颜色、排版
3. 识别组件：卡片、按钮、导航、表单元素
4. 记录响应式行为（如果是多尺寸截图）

**如果是自然语言提示：**
1. 解析提示以识别组件类型和关键特征
2. 如果提示不清楚，向用户澄清（"你指的是哪种卡片？信息卡片？产品卡片？"）
3. 如果有参考风格，提及它（"Linear 风格的设置页面"）

### 阶段 2：生成组件

**遵循这些规则：**
- 使用 React + TypeScript
- 使用 Tailwind CSS 进行样式设计
- 语义 HTML（`<nav>`、`<main>`、`<section>`、`<article>`）
- 响应式设计（移动端优先，使用 Tailwind 断点）
- 可访问性（ARIA 标签、alt 文本、键盘导航、焦点状态）
- 适当的默认 props 和 TypeScript 接口
- 模拟数据作为 props 默认值或单独的 mock 文件
- 如果组件有状态，使用 `useState` 进行基本交互
- 导出为默认导出，以便轻松导入

**组件结构：**
```tsx
import React from 'react';

interface {ComponentName}Props {
  // 根据设计定义 props
}

export default function {ComponentName}({ ...props }: {ComponentName}Props) {
  return (
    // 语义 HTML，带有 Tailwind 类
  );
}
```

### 阶段 3：截图验证

如果 gstack 设计器可用：
```bash
$D generate --component "path/to/component.tsx" --output "designs/{name}-{timestamp}.png"
```

如果设计器不可用：
- 生成组件后，告知用户组件已创建
- 提供将其集成到应用中的说明

### 阶段 4：迭代

如果用户要求变更：
1. 读取当前组件
2. 应用请求的变更
3. 重新截图（如果设计器可用）
4. 展示前后对比

## 设计原则

在生成组件时遵循这些原则：
- **层次：** 使用尺寸、颜色和粗细建立清晰的视觉层次
- **间距：** 使用一致的间距比例（4px/8px 网格）
- **颜色：** 使用语义颜色——主色用于行动，灰色用于次要内容，红色用于错误
- **排版：** 限制为 2 种字体，使用字重区分，而不是字号
- **留白：** 慷慨使用留白——拥挤是 AI 生成 UI 的标志
- **状态：** 为悬停、聚焦、活动、禁用、加载和错误状态设计

## 常见模式

### 卡片
- 圆角（rounded-lg 或 rounded-xl）
- 微妙的阴影或边框
- 内边距一致（p-4 到 p-6）
- 如果有操作，底部对齐

### 导航
- 固定或粘性定位，如果有滚动内容
- 当前项的活跃状态指示器
- 移动端的折叠菜单

### 表单
- 每个输入关联标签
- 清晰的验证错误消息
- 禁用状态，表单无效时禁用提交
- 加载状态，提交期间禁用按钮

### 列表
- 交替的行背景色（如果需要可读性）
- 悬停高亮
- 空状态，当没有数据时
- 加载骨架屏

## 规则

- **始终生成 TypeScript。** 不使用纯 JavaScript。
- **始终包含类型定义。** 不使用 `any`。
- **始终使用 Tailwind。** 不使用内联样式或单独的 CSS 文件。
- **始终使响应式。** 至少支持移动端和桌面。
- **始终包含 ARIA 属性。** 当语义 HTML 不够时。
- **始终导出组件。** 不使用仅用于演示的独立文件。
- **永远不要引入新依赖。** 仅使用 React 和 Tailwind。
- **永远不要生成后端代码。** 这是纯前端样机。
- **永远不要硬编码业务逻辑。** 使用 mock 数据，通过 props 注入真实数据。
- **如果参考不清楚，询问澄清。** 不要猜测设计意图。
