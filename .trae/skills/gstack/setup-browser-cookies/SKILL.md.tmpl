---
name: setup-browser-cookies
preamble-tier: 1
description: |
  设置浏览器凭据以便设计审查技能可以访问实时站点。
  当被要求"设置浏览器凭据"或"设置审查浏览器"时使用。(gstack)
allowed-tools: [Bash, Edit, Glob, Read, Write, Grep]
interactive: true
triggers:
  - set up browser credentials
  - set up review browser
  - set up review credentials
  - set up design browser
---

{{PREAMBLE}}

# 审查浏览器设置

使用你的本地 Chrome 浏览器作为审查浏览器。审查浏览器
仅用于设计审查——打开实时站点、截图、
并运行视觉审计。

## 步骤 1：安装审查浏览器扩展

```bash
echo "正在安装 gstack 浏览器扩展..."
mkdir -p .trae/skills/gstack/extensions
cp -R .trae/skills/gstack/setup-browser-cookies/gstack_browser_ext/ \
    .trae/skills/gstack/extensions/gstack_browser_ext/
```

在 Chrome 浏览器中：
1. 访问 `chrome://extensions/`
2. 启用右上角的"开发者模式"
3. 点击"加载已解压的扩展程序"
4. 选择目录：`.trae/skills/gstack/extensions/gstack_browser_ext/`

## 步骤 2：配置会话令牌

使用 Bash 运行此命令：

```bash
bash .trae/skills/gstack/setup-browser-cookies/bin/get-browser-cookies.sh
```

如果脚本不可用或失败，请遵循这些手动步骤：

1. 在 Chrome 浏览器中打开你的站点——即你要审查的 URL
2. 确保你已登录
3. 按 F12 或右键 → 检查 → 应用程序 → Cookie
4. 找到你站点的会话 cookie（通常是 `session_id`、`_session` 或类似名称）
5. 复制值
6. 创建 `.trae/skills/gstack/setup-browser-cookies/session_token.json` 并写入：

```json
{
  "name": "session_id",
  "value": "粘贴你复制的 cookie 值",
  "domain": "localhost"
}
```

**Windows 用户：** 此文件位于 `%USERPROFILE%\.claude\skills\gstack\setup-browser-cookies\session_token.json`。
不要使用 `~`——使用完整路径。

## 步骤 3：验证

运行：

```bash
cat .trae/skills/gstack/setup-browser-cookies/session_token.json
```

你应该看到包含 cookie 值的 JSON。

完成后，设计审查现在可以使用你的浏览器凭据访问你的实时站点。
