---
name: document-release
preamble-tier: 2
version: 1.0.0
description: |
  发布后文档更新。读取所有项目文档，交叉引用
  diff，更新 README/ARCHITECTURE/CONTRIBUTING/CLAUDE.md 以匹配已发布的内容，
  润色 CHANGELOG 语气，清理 TODOS，并可选择更新 VERSION。使用场景：
  "更新文档"、"同步文档"或"发布后文档"。
  在 PR 合并或代码发布后主动建议。(gstack)
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Grep
  - Glob
  - AskUserQuestion
triggers:
  - update docs after ship
  - document what changed
  - post-ship docs
---

{{PREAMBLE}}

{{BASE_BRANCH_DETECT}}

# Document Release：发布后文档更新

你正在运行 `/document-release` 工作流。这在 `/ship` **之后**（代码已提交，PR
存在或即将存在）但在 **PR 合并之前**运行。你的工作：确保项目中的每个文档文件
都是准确的、最新的，并以友好的、用户优先的语气编写。

你大部分是自动化的。直接进行明显的事实更新。只在有风险或
主观决策时停下来询问。

**仅在以下情况停止：**
- 有风险/有疑问的文档更改（叙述、哲学、安全、删除、大规模重写）
- VERSION 更新决策（如果尚未更新）
- 需要添加的新 TODOS 项目
- 叙述性（非事实性）的跨文档矛盾

**永远不停止：**
- 明显来自 diff 的事实更正
- 向表格/列表添加项目
- 更新路径、计数、版本号
- 修复过时的交叉引用
- CHANGELOG 语气润色（小幅措辞调整）
- 标记 TODOS 为完成
- 跨文档的事实不一致（例如，版本号不匹配）

**永远不做：**
- 覆盖、替换或重新生成 CHANGELOG 条目 — 仅润色措辞，保留所有内容
- 不询问就更新 VERSION — 始终使用 AskUserQuestion 进行版本更改
- 对 CHANGELOG.md 使用 `Write` 工具 — 始终使用 `Edit` 和精确的 `old_string` 匹配

---

## 步骤 1：预检 & Diff 分析

1. 检查当前分支。如果在基础分支上，**中止**："你在基础分支上。请从功能分支运行。"

2. 收集关于更改的上下文：

```bash
git diff <base>...HEAD --stat
```

```bash
git log <base>..HEAD --oneline
```

```bash
git diff <base>...HEAD --name-only
```

3. 发现仓库中的所有文档文件：

```bash
find . -maxdepth 2 -name "*.md" -not -path "./.git/*" -not -path "./node_modules/*" -not -path "./.gstack/*" -not -path "./.context/*" | sort
```

4. 将更改分类为与文档相关的类别：
   - **新功能** — 新文件、新命令、新技能、新能力
   - **行为更改** — 修改的服务、更新的 API、配置更改
   - **移除的功能** — 删除的文件、移除的命令
   - **基础设施** — 构建系统、测试基础设施、CI

5. 输出简短摘要："正在分析跨越 M 次提交的 N 个文件更改。发现 K 个需要审查的文档文件。"

---

## 步骤 2：逐文件文档审计

读取每个文档文件并交叉引用 diff。使用以下通用启发式方法
（适应你所在的任何项目 — 这些不是 gstack 特定的）：

**README.md：**
- 是否描述了 diff 中可见的所有功能和能力？
- 安装/设置说明是否与更改一致？
- 示例、演示和使用说明是否仍然有效？
- 故障排除步骤是否仍然准确？

**ARCHITECTURE.md：**
- ASCII 图和组件描述是否与当前代码匹配？
- 设计决策和"为什么"的解释是否仍然准确？
- 保守操作 — 仅更新被 diff 明确矛盾的内容。架构文档
  描述不太可能频繁更改的内容。

**CONTRIBUTING.md — 新贡献者冒烟测试：**
- 好像你是一个全新的贡献者一样，逐步完成设置说明。
- 列出的命令是否准确？每个步骤能成功吗？
- 测试层级描述是否与当前测试基础设施匹配？
- 工作流描述（开发设置、运营经验等）是否最新？
- 标记任何会让首次贡献者失败或困惑的内容。

**CLAUDE.md / 项目说明：**
- 项目结构部分是否与实际文件树匹配？
- 列出的命令和脚本是否准确？
- 构建/测试说明是否与 package.json（或等效文件）中的内容匹配？

**任何其他 .md 文件：**
- 读取文件，确定其目的和受众。
- 交叉引用 diff，检查是否与文件中的任何内容矛盾。

对于每个文件，将所需的更新分类为：

- **自动更新** — diff 明确 warrant 的事实更正：向
  表格添加项目、更新文件路径、修复计数、更新项目结构树。
- **询问用户** — 叙述性更改、章节删除、安全模型更改、大规模重写
  （一个章节中超过 ~10 行）、模糊的相关性、添加全新章节。

---

## 步骤 3：应用自动更新

使用 Edit 工具直接进行清晰的事实更新。

对于每个修改的文件，输出一行摘要，描述**具体更改了什么** — 不是
仅仅"更新了 README.md"，而是"README.md：在技能表中添加了 /new-skill，技能计数
从 9 更新到 10。"

**永远不自动更新：**
- README 介绍或项目定位
- ARCHITECTURE 哲学或设计原理
- 安全模型描述
- 不要从任何文档中删除整个章节

---

## 步骤 4：询问有风险/有疑问的更改

对于步骤 2 中识别的每个有风险/有疑问的更新，使用 AskUserQuestion：
- 背景信息：项目名称、分支、哪个文档文件、我们正在审查什么
- 具体的文档决策
- `RECOMMENDATION: Choose [X] because [单行原因]`
- 选项包括 C) Skip — 保持原样

在每个答案后立即应用已批准的更改。

---

## 步骤 5：CHANGELOG 语气润色

**关键 — 永远不要覆盖 CHANGELOG 条目。**

此步骤仅润色语气。它**不**重写、替换或重新生成 CHANGELOG 内容。

曾发生过一起事件：一个代理在它应该
保留现有 CHANGELOG 条目时替换了它们。此技能绝不能这样做。

**规则：**
1. 首先读取整个 CHANGELOG.md。理解已有什么。
2. 仅修改现有条目中的措辞。永远不要删除、重新排序或替换条目。
3. 永远不要从头开始重新生成 CHANGELOG 条目。条目是由 `/ship` 从实际的
   diff 和提交历史编写的。它是事实的来源。你是在润色散文，不是
   重写历史。
4. 如果条目看起来错误或 incomplete，使用 AskUserQuestion — **不要**默默地修复它。
5. 使用 Edit 工具和精确的 `old_string` 匹配 — 永远不要使用 Write 覆盖 CHANGELOG.md。

**如果 CHANGELOG 在此分支中未被修改：** 跳过此步骤。

**如果 CHANGELOG 在此分支中被修改**，审查条目的语气：

- **销售测试：** 用户阅读每个要点时会想"哦不错，我想试试那个"吗？如果不是，
  重写措辞（而非内容）。
- 以用户现在可以**做**的事情为先 — 而非实现细节。
- "你现在可以..."而非"重构了..."
- 标记并重写任何读起来像提交消息的条目。
- 内部/贡献者更改属于单独的 "### For contributors" 子章节。
- 自动修复小幅语气调整。如果重写会改变含义，使用 AskUserQuestion。

---

## 步骤 6：跨文档一致性 & 可发现性检查

在单独审计每个文件之后，进行跨文档一致性检查：

1. README 的功能/能力列表是否与 CLAUDE.md（或项目说明）描述的匹配？
2. ARCHITECTURE 的组件列表是否与 CONTRIBUTING 的项目结构描述匹配？
3. CHANGELOG 的最新版本是否与 VERSION 文件匹配？
4. **可发现性：** 每个文档文件是否可以从 README.md 或 CLAUDE.md 访问？如果
   ARCHITECTURE.md 存在但 README 和 CLAUDE.md 都没有链接到它，标记它。每个文档
   都应该可以从两个入口点文件之一发现。
5. 标记文档之间的任何矛盾。自动修复清晰的事实不一致（例如，
   版本不匹配）。对叙述性矛盾使用 AskUserQuestion。

---

## 步骤 7：TODOS.md 清理

这是对 `/ship` 步骤 5.5 的补充。读取 `review/TODOS-format.md`（如果
可用）获取规范的 TODO 项目格式。

如果 TODOS.md 不存在，跳过此步骤。

1. **已完成但未标记的项目：** 交叉引用 diff 与开放的 TODO 项目。如果
   TODO 被此分支中的更改明确完成，将其移动到 Completed 部分，
   附带 `**Completed:** vX.Y.Z.W (YYYY-MM-DD)`。保守操作 — 仅标记在 diff 中有清晰
   证据的项目。

2. **需要描述更新的项目：** 如果 TODO 引用了被
   大幅更改的文件或组件，其描述可能已过时。使用 AskUserQuestion 确认
   TODO 是否应更新、完成或保持原样。

3. **新的延期工作：** 检查 diff 中的 `TODO`、`FIXME`、`HACK` 和 `XXX` 注释。对于
   每个代表有意义的延期工作的注释（不是简单的内联注释），使用
   AskUserQuestion 询问是否应捕获到 TODOS.md 中。

---

## 步骤 8：VERSION 更新问题

**关键 — 永远不要在未询问的情况下更新 VERSION。**

1. **如果 VERSION 不存在：** 静默跳过。

2. 检查 VERSION 是否已在此分支上修改：

```bash
git diff <base>...HEAD -- VERSION
```

3. **如果 VERSION 未被更新：** 使用 AskUserQuestion：
   - RECOMMENDATION：选择 C（跳过），因为纯文档更改很少 warrant 版本更新
   - A) 更新 PATCH（X.Y.Z+1） — 如果文档更改与代码更改一起发布
   - B) 更新 MINOR（X.Y+1.0） — 如果这是一个重大的独立发布
   - C) 跳过 — 不需要版本更新

4. **如果 VERSION 已经被更新：** 不要静默跳过。相反，检查更新
   是否仍然涵盖此分支上更改的全部范围：

   a. 读取当前 VERSION 的 CHANGELOG 条目。它描述了哪些功能？
   b. 读取完整的 diff（`git diff <base>...HEAD --stat` 和 `git diff <base>...HEAD --name-only`）。
      是否有未在当前版本的 CHANGELOG 条目中提及的重大更改（新功能、新技能、新命令、重大重构）？
   c. **如果 CHANGELOG 条目涵盖了所有内容：** 跳过 — 输出 "VERSION：已更新到
      vX.Y.Z，涵盖所有更改。"
   d. **如果有未涵盖的重大更改：** 使用 AskUserQuestion 解释当前版本涵盖的内容与新内容的对比，并询问：
      - RECOMMENDATION：选择 A，因为新更改 warrant 它们自己的版本
      - A) 更新到下一个 patch（X.Y.Z+1） — 给新更改它们自己的版本
      - B) 保持当前版本 — 将新更改添加到现有的 CHANGELOG 条目
      - C) 跳过 — 保持版本不变，稍后处理

   关键见解：为"功能 A"设置的 VERSION 更新不应默默地吸收"功能 B"，
   如果功能 B 足够重要而值得拥有自己的版本条目。

---

## 步骤 9：提交 & 输出

**首先检查是否为空：** 运行 `git status`（永远不要使用 `-uall`）。如果任何前序步骤都没有
修改文档文件，输出"所有文档都是最新的。"并在不提交的情况下退出。

**提交：**

1. 按名称暂存修改的文档文件（永远不要 `git add -A` 或 `git add .`）。
2. 创建单个提交：

```bash
git commit -m "$(cat <<'EOF'
docs: update project documentation for vX.Y.Z.W

{{CO_AUTHOR_TRAILER}}
EOF
)"
```

3. 推送到当前分支：

```bash
git push
```

**PR/MR 正文更新（幂等、防竞态）：**

1. 读取现有的 PR/MR 正文到 PID 唯一的临时文件（使用步骤 0 中检测到的平台）：

**如果是 GitHub：**
```bash
gh pr view --json body -q .body > /tmp/gstack-pr-body-$$.md
```

**如果是 GitLab：**
```bash
glab mr view -F json 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin).get('description',''))" > /tmp/gstack-pr-body-$$.md
```

2. 如果临时文件已包含 `## Documentation` 部分，用
   更新后的内容替换该部分。如果不包含，在末尾追加一个 `## Documentation` 部分。

3. Documentation 部分应包括 **文档 diff 预览** — 对于每个修改的文件，
   描述具体更改了什么（例如，"README.md：在技能表中添加了 /document-release，技能计数从 9 更新到 10"）。

4. 将更新后的正文写回：

**如果是 GitHub：**
```bash
gh pr edit --body-file /tmp/gstack-pr-body-$$.md
```

**如果是 GitLab：**
使用 Read 工具读取 `/tmp/gstack-pr-body-$$.md` 的内容，然后通过 heredoc 传递给 `glab mr update` 以避免 shell 元字符问题：
```bash
glab mr update -d "$(cat <<'MRBODY'
<在此粘贴文件内容>
MRBODY
)"
```

5. 清理临时文件：

```bash
rm -f /tmp/gstack-pr-body-$$.md
```

6. 如果 `gh pr view` / `glab mr view` 失败（不存在 PR/MR）：跳过并显示消息 "未找到 PR/MR — 跳过正文更新。"
7. 如果 `gh pr edit` / `glab mr update` 失败：警告 "无法更新 PR/MR 正文 — 文档更改已在
   提交中。"并继续。

**结构化的文档健康摘要（最终输出）：**

输出一个可扫描的摘要，显示每个文档文件的状态：

```
Documentation health:
  README.md       [status] ([details])
  ARCHITECTURE.md [status] ([details])
  CONTRIBUTING.md [status] ([details])
  CHANGELOG.md    [status] ([details])
  TODOS.md        [status] ([details])
  VERSION         [status] ([details])
```

其中 status 是以下之一：
- Updated — 附带更改描述
- Current — 不需要更改
- Voice polished — 措辞调整
- Not bumped — 用户选择跳过
- Already bumped — 版本已由 /ship 设置
- Skipped — 文件不存在

---

## 重要规则

- **编辑前读取。** 始终在修改之前读取文件的完整内容。
- **永远不要覆盖 CHANGELOG。** 仅润色措辞。永远不要删除、替换或重新生成条目。
- **永远不要静默更新 VERSION。** 始终询问。即使已经更新，也要检查它是否涵盖更改的全部范围。
- **明确说明更改了什么。** 每次编辑都有一行摘要。
- **通用启发式方法，而非项目特定的。** 审计检查适用于任何仓库。
- **可发现性很重要。** 每个文档文件都应该可以从 README 或 CLAUDE.md 访问。
- **语气：友好的、用户优先的、不晦涩的。** 写起来就像你在向一个没见过代码的聪明人解释。
