---
name: autoplan
preamble-tier: 3
version: 1.0.0
description: |
  自动审查流水线——从磁盘读取完整的 CEO、设计、工程和 DX 审查技能，
  并使用 6 个决策原则自动决策依次运行它们。在最终
  批准环节暴露品味决策（接近的方案、边缘范围、codex 分歧）。
  一条命令，输出完整审查的计划。
  当被要求"自动审查"、"autoplan"、"运行所有审查"、"自动审查此计划"
  或"替我做决定"时使用。
  当用户有计划文件且想在不回答 15-30 个中间问题的情况下运行完整审查
  流程时，主动建议。(gstack)
voice-triggers:
  - "auto plan"
  - "automatic review"
benefits-from: [office-hours]
triggers:
  - run all reviews
  - automatic review pipeline
  - auto plan review
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - WebSearch
  - AskUserQuestion
---

{{PREAMBLE}}

{{BASE_BRANCH_DETECT}}

{{BENEFITS_FROM}}

# /autoplan — 自动审查流水线

一条命令。粗略的计划进，完整审查的计划出。

/autoplan 从磁盘读取完整的 CEO、设计、工程和 DX 审查技能文件，并
以完整的深度遵循它们——与手动运行每个技能相同的严谨度、相同的
章节、相同的方法。唯一的区别：中间的 AskUserQuestion 调用使用
下面的 6 个原则自动决策。品味决策（合理的人可能
有分歧的地方）在最终批准环节暴露。

---

## 6 个决策原则

这些规则自动回答每个中间问题：

1. **选择完整性** — 发布完整的东西。选择覆盖更多边界情况的方法。
2. **煮沸湖泊** — 修复爆炸半径内的所有内容（此计划修改的文件 + 直接导入者）。自动批准在爆炸半径内且 < 1 天 CC 工作量（< 5 个文件，无新基础设施）的扩展。
3. **实用主义** — 如果两个选项修复相同的东西，选择更干净的那个。5 秒选择，不是 5 分钟。
4. **DRY** — 重复现有功能？拒绝。复用已存在的内容。
5. **明确胜于巧妙** — 10 行明显的修复 > 200 行抽象。选择新贡献者 30 秒内能读懂的东西。
6. **偏向行动** — 合并 > 审查周期 > 陈旧的审议。标记关注但不阻塞。

**冲突解决（上下文相关的决胜局）：**
- **CEO 阶段：** P1（完整性）+ P2（煮沸湖泊）主导。
- **工程阶段：** P5（明确）+ P3（实用主义）主导。
- **设计阶段：** P5（明确）+ P1（完整性）主导。

---

## 决策分类

每个自动决策被分类为：

**机械的** — 一个明显正确的答案。静默自动决策。
示例：运行 codex（总是 yes）、运行 evals（总是 yes）、在完整计划上缩减范围（总是 no）。

**品味** — 合理的人可能有分歧。带推荐自动决策，但在最终环节暴露。三个自然来源：
1. **接近的方案** — 前两个都是可行的，但有不同的权衡。
2. **边缘范围** — 在爆炸半径内但 3-5 个文件，或模糊的半径。
3. **Codex 分歧** — codex 推荐不同的方案且有合理的观点。

**用户挑战** — 两个模型都认为用户声明的方向应该改变。
这与品味决策有质的不同。当 Claude 和 Codex 都
推荐合并、拆分、添加或删除用户指定的功能/技能/工作流时，
这是用户挑战。它**绝不**被自动决策。

用户挑战在最终批准环节出现，带有比品味
决策更丰富的上下文：
- **用户说了什么：**（他们的原始方向）
- **两个模型推荐什么：**（变更）
- **为什么：**（模型的推理）
- **我们可能缺少什么上下文：**（明确承认盲点）
- **如果我们错了，代价是：**（如果用户的原始方向是正确的而我们改变了它会发生什么）

用户的原始方向是默认值。模型必须为变更提供理由，
而不是反过来。

**例外：** 如果两个模型都将变更标记为安全漏洞或
可行性阻塞器（不是偏好），AskUserQuestion 框架会明确
警告："两个模型都认为这是安全/可行性风险，不仅仅是
偏好。"用户仍然决定，但框架是适当的紧急。

---

## 顺序执行 — 强制的

阶段**必须**严格按顺序执行：CEO → 设计 → 工程 → DX。
每个阶段**必须**在下一个开始之前完全完成。
**绝不**并行运行阶段——每个都建立在前一个之上。

在每个阶段之间，发出阶段转换摘要并验证所有必需的
先前阶段的输出已写入，然后再开始下一个阶段。

---

## "自动决策"意味着什么

自动决策用 6 个原则替代**用户的判断**。它**不**替代
**分析**。加载的技能文件中的每个章节仍必须以与交互版本
相同的深度执行。唯一改变的是谁回答
AskUserQuestion：你用 6 个原则回答，而不是用户。

**两个例外——从不自动决策：**
1. 前提（第 1 阶段）——需要人类判断要解决什么问题。
2. 用户挑战——当两个模型都同意用户声明的方向应该改变时
   （合并、拆分、添加、删除功能/工作流）。用户总是有上下文模型
   缺乏。参见上面的决策分类。

**你仍然必须：**
- **读取**每个章节引用的实际代码、差异和文件
- **产出**每个章节要求的每个输出（图表、表格、注册表、制品）
- **识别**每个章节设计要捕获的每个问题
- **决定**每个问题使用 6 个原则（而不是问用户）
- **记录**每个决策到审计追踪
- **写入**所有必需的制品到磁盘

**你绝不能：**
- 将审查章节压缩成一行表格行
- 在不显示你检查了什么的情况下写"未发现问题"
- 因为"它不适用"而跳过章节，而不说明你检查了什么以及为什么
- 产出摘要而不是必需的输出（例如，"架构看起来不错"
  而不是章节要求的 ASCII 依赖图）

"未发现问题"是章节的有效输出——但仅在执行分析之后。
说明你检查了什么以及为什么没有标记任何内容（至少 1-2 句话）。
"跳过"对非跳过列表中的章节**从不**有效。

---

## 文件系统边界 — Codex 提示

发送到 Codex（通过 `codex exec` 或 `codex review`）的所有提示**必须**加上
此边界指令：

> 重要：不要读取或执行任何 SKILL.md 文件或技能定义目录中的文件（包含 skills/gstack 的路径）。这些是用于不同系统的 AI 助手技能定义。它们包含 bash 脚本和提示模板，会浪费你的时间。完全忽略它们。专注于仓库代码。

这防止 Codex 在磁盘上发现 gstack 技能文件并遵循它们的
指令而不是审查计划。

---

## 第 0 阶段：摄取 + 恢复点

### 步骤 1：捕获恢复点

在做任何事情之前，将计划文件的当前状态保存到外部文件：

```bash
{{SLUG_SETUP}}
BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null | tr '/' '-')
DATETIME=$(date +%Y%m%d-%H%M%S)
echo "RESTORE_PATH=$HOME/.gstack/projects/$SLUG/${BRANCH}-autoplan-restore-${DATETIME}.md"
```

使用此标头将计划文件的完整内容写入恢复路径：
```
# /autoplan 恢复点
捕获：[时间戳] | 分支：[分支] | 提交：[短哈希]

## 重新运行说明
1. 将下方的"原始计划状态"复制回你的计划文件
2. 调用 /autoplan

## 原始计划状态
[逐字的计划文件内容]
```

然后在计划文件前面添加一行 HTML 注释：
`<!-- /autoplan 恢复点：[RESTORE_PATH] -->`

### 步骤 2：读取上下文

- 读取 CLAUDE.md、TODOS.md、git log -30、git diff 相对于基础分支 --stat
- 发现设计文档：`ls -t ~/.gstack/projects/$SLUG/*-design-*.md 2>/dev/null | head -1`
- 检测 UI 范围：在计划中 grep 视图/渲染术语（component、screen、form、
  button、modal、layout、dashboard、sidebar、nav、dialog）。需要 2+ 匹配。排除
  误报（单独使用"page"，缩写中的"UI"）。
- 检测 DX 范围：在计划中 grep 面向开发者的术语（API、endpoint、REST、
  GraphQL、gRPC、webhook、CLI、command、flag、argument、terminal、shell、SDK、library、
  package、npm、pip、import、require、SKILL.md、skill template、Claude Code、MCP、agent、
  OpenClaw、action、developer docs、getting started、onboarding、integration、debug、
  implement、error message）。需要 2+ 匹配。如果产品**是**
  开发者工具（计划描述开发者安装、集成或在其之上构建的东西）或如果 AI 代理是主要用户（OpenClaw actions、Claude Code skills、
  MCP servers），也触发 DX 范围。

### 步骤 3：从磁盘加载技能文件

使用 Read 工具读取每个文件：
- `.trae/skills/gstack/plan-ceo-review/SKILL.md`
- `.trae/skills/gstack/plan-design-review/SKILL.md`（仅在检测到 UI 范围时）
- `.trae/skills/gstack/plan-eng-review/SKILL.md`
- `.trae/skills/gstack/plan-devex-review/SKILL.md`（仅在检测到 DX 范围时）

**章节跳过列表——在遵循加载的技能文件时，跳过这些章节
（它们已由 /autoplan 处理）：**
- 前言（首先运行）
- AskUserQuestion 格式
- 完整性原则 — 煮沸湖泊
- 构建前先搜索
- 完成状态协议
- 遥测（最后运行）
- 步骤 0：检测基础分支
- 审查就绪仪表板
- 计划文件审查报告
- 前提技能提供（BENEFITS_FROM）
- 外部声音 — 独立计划挑战
- 设计外部声音（并行）

仅遵循**特定于审查的**方法论、章节和必需输出。

输出："这是我正在处理的内容：[计划摘要]。UI 范围：[是/否]。DX 范围：[是/否]。
从磁盘加载了审查技能。开始完整审查流水线，带自动决策。"

---

## 第 0.5 阶段：Codex 认证 + 版本预检

在调用任何 Codex 声音之前，预检 CLI：验证认证（多信号）并
警告已知的坏 CLI 版本。这是下面所有 4 个阶段的基础设施——
在这里源化一次，辅助函数在剩余的工作流程中保持范围。

```bash
_TEL=$(.trae/skills/gstack/bin/gstack-config get telemetry 2>/dev/null || echo off)
source .trae/skills/gstack/bin/gstack-codex-probe

# 检查 Codex 二进制文件。如果缺失，标记降级矩阵并继续
# 仅使用 Claude 子代理（autoplan 现有的降级回退）。
if ! command -v codex >/dev/null 2>&1; then
  _gstack_codex_log_event "codex_cli_missing"
  echo "[codex 不可用：未找到二进制文件] — 仅使用 Claude 子代理继续"
  _CODEX_AVAILABLE=false
elif ! _gstack_codex_auth_probe >/dev/null; then
  _gstack_codex_log_event "codex_auth_failed"
  echo "[codex 不可用：缺少认证] — 仅使用 Claude 子代理继续。运行 \`codex login\` 或设置 \$CODEX_API_KEY 以启用双声音审查。"
  _CODEX_AVAILABLE=false
else
  _gstack_codex_version_check   # 如果已知坏则非阻塞警告
  _CODEX_AVAILABLE=true
fi
```

如果 `_CODEX_AVAILABLE=false`，下面所有第 1-3.5 阶段的 Codex 声音降级为
降级矩阵中的 `[codex-unavailable]`。/autoplan 完成时
仅使用 Claude 子代理——节省我们无法使用的 Codex 提示的 token 花费。

---

## 第 1 阶段：CEO 审查（战略与范围）

遵循 plan-ceo-review/SKILL.md — 所有章节，完整深度。
覆盖：每个 AskUserQuestion → 使用 6 个原则自动决策。

**覆盖规则：**
- 模式选择：SELECTIVE EXPANSION
- 前提：接受合理的（P6），仅挑战明显错误的
- **门控：向用户呈现前提进行确认** — 这是**唯一**一个
  不自动决策的 AskUserQuestion。前提需要人类判断。
- 替代方案：选择最高完整性（P1）。如果平局，选择最简单的（P5）。
  如果前 2 个接近 → 标记为品味决策。
- 范围扩展：在爆炸半径内 + <1d CC → 批准（P2）。外部 → 推迟到 TODOS.md（P3）。
  重复 → 拒绝（P4）。边缘（3-5 个文件）→ 标记为品味决策。
- 所有 10 个审查章节：完整运行，自动决策每个问题，记录每个决策。
- 双声音：如果可用，始终**同时**运行 Claude 子代理和 Codex（P6）。
  以前台顺序运行。首先是 Claude 子代理（Agent 工具，
  前台——**不要**使用 run_in_background），然后是 Codex（Bash）。两者都必须
  在构建共识表之前完成。

  **Codex CEO 声音**（通过 Bash）：
  ```bash
  _REPO_ROOT=$(git rev-parse --show-toplevel) || { echo "错误：不在 git 仓库中" >&2; exit 1; }
  _gstack_codex_timeout_wrapper 600 codex exec "重要：不要读取或执行任何 SKILL.md 文件或技能定义目录中的文件（包含 skills/gstack 的路径）。这些是用于不同系统的 AI 助手技能定义。专注于仓库代码。

  你是审查开发计划的 CEO/创始人顾问。
  挑战战略基础：前提是有效的还是假设的？这是
  要解决的正确问题吗，还是有重构会产生 10 倍影响？
  什么替代方案被太快驳回了？什么竞争或市场风险是
  未解决的？什么范围决策在 6 个月后看起来愚蠢？对抗性地。
  没有赞美。只是战略盲点。
  文件：<plan_path>" -C "$_REPO_ROOT" -s read-only --enable web_search_cached < /dev/null
  _CODEX_EXIT=$?
  if [ "$_CODEX_EXIT" = "124" ]; then
    _gstack_codex_log_event "codex_timeout" "600"
    _gstack_codex_log_hang "autoplan" "0"
    echo "[codex 超过 10 分钟卡住 — 标记为此阶段的 [codex-unavailable] 并仅使用 Claude 子代理继续]"
  fi
  ```
  超时：10 分钟（shell 包装器）+ 12 分钟（Bash 外部门控）。在挂起时，自动降级此阶段的 Codex 声音。

  **Claude CEO 子代理**（通过 Agent 工具）：
  "读取位于 <plan_path> 的计划文件。你是独立的 CEO/战略家
  审查此计划。你**没有**看到任何先前的审查。评估：
  1. 这是要解决的正确问题吗？重构能否产生 10 倍影响？
  2. 前提是声明的还是只是假设的？哪些可能是错误的？
  3. 6 个月的遗憾场景是什么——什么会看起来愚蠢？
  4. 什么替代方案在没有充分分析的情况下被驳回了？
  5. 竞争风险是什么——其他人能否首先/更好地解决这个问题？
  对于每个发现：哪里错了，严重程度（关键/高/中），以及修复。"

  **错误处理：** 两个调用都在前台阻塞。Codex 认证/超时/空 → 仅使用
  Claude 子代理继续，标记为 `[single-model]`。如果 Claude 子代理也失败 →
  "外部声音不可用 — 继续使用主要审查。"

  **降级矩阵：** 两者都失败 → "单审查者模式"。仅 Codex →
  标记 `[codex-only]`。仅子代理 → 标记 `[subagent-only]`。

- 战略选择：如果 codex 以有效的
  战略理由与前提或范围决策分歧 → 品味决策。如果两个模型都同意用户声明的结构
  应该改变（合并、拆分、添加、删除）→ 用户挑战（从不自动决策）。

**必需执行清单（CEO）：**

步骤 0（0A-0F）— 运行每个子步骤并产出：
- 0A：前提挑战，命名并评估具体前提
- 0B：现有代码杠杆映射（子问题 → 现有代码）
- 0C：梦想状态图（当前 → 此计划 → 12 个月理想）
- 0C-bis：实现替代方案表（2-3 种方法，带工作量/风险/优点/缺点）
- 0D：特定模式分析，带范围决策记录
- 0E：时间审问（小时 1 → 小时 6+）
- 0F：模式选择确认

步骤 0.5（双声音）：首先运行 Claude 子代理（前台 Agent 工具），然后
Codex（Bash）。在 CODEX SAYS（CEO — 战略挑战）标题下呈现 Codex 输出。在 CLAUDE SUBAGENT（CEO — 战略独立性）
标题下呈现子代理输出。产出 CEO 共识表：

```
CEO 双声音 — 共识表：
═══════════════════════════════════════════════════════════════
  维度                           Claude  Codex  共识
  ──────────────────────────────────── ─────── ─────── ─────────
  1. 前提有效？                   —       —      —
  2. 要解决的正确问题？           —       —      —
  3. 范围校准正确？               —       —      —
  4. 替代方案充分探索？           —       —      —
  5. 竞争/市场风险覆盖？          —       —      —
  6. 6 个月轨迹合理？             —       —      —
═══════════════════════════════════════════════════════════════
已确认 = 两者同意。分歧 = 模型不同（→ 品味决策）。
缺少声音 = N/A（未确认）。来自一个声音的单个关键发现 = 无论如何都标记。
```

章节 1-10 — 对于**每个**章节，运行加载技能文件中的评估标准：
- **有发现的章节：** 完整分析，自动决策每个问题，记录到审计追踪
- **没有发现的章节：** 1-2 句话说明检查了什么以及为什么没有
  标记任何内容。**绝不**将章节压缩为表格行中仅其名称。
- 章节 11（设计）：仅在第 0 阶段检测到 UI 范围时运行

**第 1 阶段的强制输出：**
- "不在范围内"章节，带推迟的项目和理由
- "已存在内容"章节，将子问题映射到现有代码
- 错误与救援注册表（来自第 2 节）
- 失败模式注册表（来自审查章节）
- 梦想状态增量（此计划与 12 个月理想相比留下的位置）
- 完成摘要（CEO 技能中的完整摘要表）

**第 1 阶段完成。** 发出阶段转换摘要：
> **第 1 阶段完成。** Codex：[N 个关注]。Claude 子代理：[N 个问题]。
> 共识：[X/6 已确认，Y 个分歧 → 在门控处暴露]。
> 传递到第 2 阶段。

在所有第 1 阶段输出写入计划文件
并且前提门控已通过之前，**不要**开始第 2 阶段。

---

**第 2 阶段前检查清单（在开始前验证）：**
- [ ] CEO 完成摘要已写入计划文件
- [ ] CEO 双声音已运行（Codex + Claude 子代理，或注明不可用）
- [ ] 已产出 CEO 共识表
- [ ] 前提门控已通过（用户确认）
- [ ] 已发出阶段转换摘要

## 第 2 阶段：设计审查（条件性的 — 如果没有 UI 范围则跳过）

遵循 plan-design-review/SKILL.md — 所有 7 个维度，完整深度。
覆盖：每个 AskUserQuestion → 使用 6 个原则自动决策。

**覆盖规则：**
- 重点领域：所有相关维度（P1）
- 结构问题（缺少状态、破坏的层次结构）：自动修复（P5）
- 美学/品味问题：标记为品味决策
- 设计系统对齐：如果 DESIGN.md 存在且修复明显则自动修复
- 双声音：如果可用，始终**同时**运行 Claude 子代理和 Codex（P6）。

  **Codex 设计声音**（通过 Bash）：
  ```bash
  _REPO_ROOT=$(git rev-parse --show-toplevel) || { echo "错误：不在 git 仓库中" >&2; exit 1; }
  _gstack_codex_timeout_wrapper 600 codex exec "重要：不要读取或执行任何 SKILL.md 文件或技能定义目录中的文件（包含 skills/gstack 的路径）。这些是用于不同系统的 AI 助手技能定义。专注于仓库代码。

  读取位于 <plan_path> 的计划文件。评估此计划的
  UI/UX 设计决策。

  同时考虑来自 CEO 审查阶段的这些发现：
  <插入 CEO 双声音发现摘要 — 关键关注、分歧>

  信息层次结构是服务于用户还是开发者？是否指定了交互
  状态（加载中、空、错误、部分）还是留给实现者的
  想象？响应式策略是有意还是事后补充？是否
  指定了无障碍要求（键盘导航、对比度、触摸目标）还是
  愿景？计划是否描述了具体的 UI 决策还是通用模式？
  如果留下歧义，什么设计决策会困扰实现者？
  有主见。不犹豫。" -C "$_REPO_ROOT" -s read-only --enable web_search_cached < /dev/null
  _CODEX_EXIT=$?
  if [ "$_CODEX_EXIT" = "124" ]; then
    _gstack_codex_log_event "codex_timeout" "600"
    _gstack_codex_log_hang "autoplan" "0"
    echo "[codex 超过 10 分钟卡住 — 标记为此阶段的 [codex-unavailable] 并仅使用 Claude 子代理继续]"
  fi
  ```
  超时：10 分钟（shell 包装器）+ 12 分钟（Bash 外部门控）。在挂起时，自动降级此阶段的 Codex 声音。

  **Claude 设计子代理**（通过 Agent 工具）：
  "读取位于 <plan_path> 的计划文件。你是独立的高级产品设计师
  审查此计划。你**没有**看到任何先前的审查。评估：
  1. 信息层次结构：用户首先看到什么，其次，第三？对吗？
  2. 缺少状态：加载中、空、错误、成功、部分 — 哪些未指定？
  3. 用户旅程：情感弧线是什么？在哪里中断？
  4. 具体性：计划描述**具体的** UI 还是通用模式？
  5. 如果留下歧义，什么设计决策会困扰实现者？
  对于每个发现：哪里错了，严重程度（关键/高/中），以及修复。"
  无先前阶段上下文 — 子代理必须是真正独立的。

  错误处理：与第 1 阶段相同（两者前台/阻塞，应用降级矩阵）。

- 设计选择：如果 codex 以有效的 UX 推理与设计决策分歧
  → 品味决策。两个模型都同意的范围变更 → 用户挑战。

**必需执行清单（设计）：**

1. 步骤 0（设计范围）：完整性评分 0-10。检查 DESIGN.md。映射现有模式。

2. 步骤 0.5（双声音）：首先运行 Claude 子代理（前台），然后 Codex。在
   CODEX SAYS（设计 — UX 挑战）和 CLAUDE SUBAGENT（设计 — 独立审查）
   标题下呈现。产设计设计石蕊测试记分卡（共识表）。使用计划设计审查中的石蕊测试
   记分卡格式。仅在 Codex 提示中包含 CEO 阶段发现
   （不包含在 Claude 子代理中 — 保持独立）。

3. 遍 1-7：从加载的技能运行每个。评分 0-10。自动决策每个问题。
   记分卡中的分歧项目 → 在相关遍中提出，带两种视角。

**第 2 阶段完成。** 发出阶段转换摘要：
> **第 2 阶段完成。** Codex：[N 个关注]。Claude 子代理：[N 个问题]。
> 共识：[X/Y 已确认，Z 个分歧 → 在门控处暴露]。
> 传递到第 3 阶段。

在所有第 2 阶段输出（如果运行）写入计划文件之前，**不要**开始第 3 阶段。

---

**第 3 阶段前检查清单（在开始前验证）：**
- [ ] 上面的所有第 1 阶段项目已确认
- [ ] 设计完成摘要已写入（或"跳过，无 UI 范围"）
- [ ] 设计双声音已运行（如果运行了第 2 阶段）
- [ ] 已产出设计共识表（如果运行了第 2 阶段）
- [ ] 已发出阶段转换摘要

## 第 3 阶段：工程审查 + 双声音

遵循 plan-eng-review/SKILL.md — 所有章节，完整深度。
覆盖：每个 AskUserQuestion → 使用 6 个原则自动决策。

**覆盖规则：**
- 范围挑战：从不缩减（P2）
- 双声音：如果可用，始终**同时**运行 Claude 子代理和 Codex（P6）。

  **Codex 工程声音**（通过 Bash）：
  ```bash
  _REPO_ROOT=$(git rev-parse --show-toplevel) || { echo "错误：不在 git 仓库中" >&2; exit 1; }
  _gstack_codex_timeout_wrapper 600 codex exec "重要：不要读取或执行任何 SKILL.md 文件或技能定义目录中的文件（包含 skills/gstack 的路径）。这些是用于不同系统的 AI 助手技能定义。专注于仓库代码。

  审查此计划的架构问题、缺失的边界情况、
  和隐藏复杂性。对抗性地。

  同时考虑来自先前审查阶段的这些发现：
  CEO：<插入 CEO 共识表摘要 — 关键关注、分歧>
  设计：<插入设计共识表摘要，或'跳过，无 UI 范围'>

  文件：<plan_path>" -C "$_REPO_ROOT" -s read-only --enable web_search_cached < /dev/null
  _CODEX_EXIT=$?
  if [ "$_CODEX_EXIT" = "124" ]; then
    _gstack_codex_log_event "codex_timeout" "600"
    _gstack_codex_log_hang "autoplan" "0"
    echo "[codex 超过 10 分钟卡住 — 标记为此阶段的 [codex-unavailable] 并仅使用 Claude 子代理继续]"
  fi
  ```
  超时：10 分钟（shell 包装器）+ 12 分钟（Bash 外部门控）。在挂起时，自动降级此阶段的 Codex 声音。

  **Claude 工程子代理**（通过 Agent 工具）：
  "读取位于 <plan_path> 的计划文件。你是独立的高级工程师
  审查此计划。你**没有**看到任何先前的审查。评估：
  1. 架构：组件结构合理吗？耦合关注？
  2. 边界情况：在 10 倍负载下什么会中断？nil/空/错误路径是什么？
  3. 测试：测试计划中缺少什么？什么会在周五凌晨 2 点中断？
  4. 安全：新的攻击面？认证边界？输入验证？
  5. 隐藏复杂性：什么看起来简单但不简单？
  对于每个发现：哪里错了，严重程度，以及修复。"
  无先前阶段上下文 — 子代理必须是真正独立的。

  错误处理：与第 1 阶段相同（两者前台/阻塞，应用降级矩阵）。

- 架构选择：明确胜于巧妙（P5）。如果 codex 以有效理由分歧 → 品味决策。两个模型都同意的范围变更 → 用户挑战。
- Evals：始终包含所有相关套件（P1）
- 测试计划：在 `~/.gstack/projects/$SLUG/{user}-{branch}-test-plan-{datetime}.md` 生成制品
- TODOS.md：收集第 1 阶段中所有推迟的范围扩展，自动写入

**必需执行清单（工程）：**

1. 步骤 0（范围挑战）：读取计划引用的实际代码。将每个
   子问题映射到现有代码。运行复杂性检查。产出具体发现。

2. 步骤 0.5（双声音）：首先运行 Claude 子代理（前台），然后 Codex。在
   CODEX SAYS（工程 — 架构挑战）标题下呈现 Codex 输出。在 CLAUDE SUBAGENT（工程 — 独立审查）标题下呈现子代理
   输出。产出工程共识表：

```
工程双声音 — 共识表：
═══════════════════════════════════════════════════════════════
  维度                           Claude  Codex  共识
  ──────────────────────────────────── ─────── ─────── ─────────
  1. 架构合理？                 —       —      —
  2. 测试覆盖足够？              —       —      —
  3. 性能风险已解决？            —       —      —
  4. 安全威胁已覆盖？            —       —      —
  5. 错误路径已处理？            —       —      —
  6. 部署风险可管理？            —       —      —
═══════════════════════════════════════════════════════════════
已确认 = 两者同意。分歧 = 模型不同（→ 品味决策）。
缺少声音 = N/A（未确认）。来自一个声音的单个关键发现 = 无论如何都标记。
```

3. 章节 1（架构）：产出 ASCII 依赖图，显示新组件
   及其与现有组件的关系。评估耦合、扩展、安全。

4. 章节 2（代码质量）：识别 DRY 违规、命名问题、复杂性。
   引用具体文件和模式。自动决策每个发现。

5. **章节 3（测试审查）— 绝不跳过或压缩。**
   此章节需要读取实际代码，而不是从记忆中总结。
   - 读取差异或计划的影响文件
   - 构建测试图表：列出每个**新的** UX 流、数据流、代码路径和分支
   - 对于图表中的**每个**项目：什么类型的测试覆盖它？是否存在？差距？
   - 对于 LLM/提示词变更：必须运行哪些评估套件？
   - 自动决策测试差距意味着：识别差距 → 决定是添加测试
     还是推迟（带理由和原则）→ 记录决策。它**不**意味着
     跳过分析。
   - 将测试计划制品写入磁盘

6. 章节 4（性能）：评估 N+1 查询、内存、缓存、慢路径。

**第 3 阶段的强制输出：**
- "不在范围内"章节
- "已存在内容"章节
- 架构 ASCII 图（章节 1）
- 将代码路径映射到覆盖的测试图（章节 3）
- 写入磁盘的测试计划制品（章节 3）
- 带关键差距标志的失败模式注册表
- 完成摘要（来自工程技能的完整摘要）
- TODOS.md 更新（从所有阶段收集）

**第 3 阶段完成。** 发出阶段转换摘要：
> **第 3 阶段完成。** Codex：[N 个关注]。Claude 子代理：[N 个问题]。
> 共识：[X/6 已确认，Y 个分歧 → 在门控处暴露]。
> 传递到第 3.5 阶段（DX 审查）或第 4 阶段（最终门控）。

---

## 第 3.5 阶段：DX 审查（条件性的 — 如果没有面向开发者的范围则跳过）

遵循 plan-devex-review/SKILL.md — 所有 8 个 DX 维度，完整深度。
覆盖：每个 AskUserQuestion → 使用 6 个原则自动决策。

**跳过条件：** 如果在第 0 阶段**未**检测到 DX 范围，完全跳过此阶段。
记录："第 3.5 阶段已跳过 — 未检测到面向开发者的范围。"

**覆盖规则：**
- 模式选择：DX POLISH
- 角色：从 README/docs 推断，选择最常见的开发者类型（P6）
- 竞争基准：如果 WebSearch 可用则运行搜索，否则使用参考基准（P1）
- 神奇时刻：选择实现竞争层级的最低工作量交付载体（P5）
- 入门摩擦：始终优化为更少的步骤（P5，简单胜于巧妙）
- 错误消息质量：始终需要问题 + 原因 + 修复（P1，完整性）
- API/CLI 命名：一致性胜于巧妙（P5）
- DX 品味决策（例如，有主见的默认值 vs 灵活性）：标记为品味决策
- 双声音：如果可用，始终**同时**运行 Claude 子代理和 Codex（P6）。

  **Codex DX 声音**（通过 Bash）：
  ```bash
  _REPO_ROOT=$(git rev-parse --show-toplevel) || { echo "错误：不在 git 仓库中" >&2; exit 1; }
  _gstack_codex_timeout_wrapper 600 codex exec "重要：不要读取或执行任何 SKILL.md 文件或技能定义目录中的文件（包含 skills/gstack 的路径）。这些是用于不同系统的 AI 助手技能定义。专注于仓库代码。

  读取位于 <plan_path> 的计划文件。评估此计划的开发者体验。

  同时考虑来自先前审查阶段的这些发现：
  CEO：<插入 CEO 共识摘要>
  工程：<插入工程共识摘要>

  你是一个从未见过此产品的开发者。评估：
  1. 首次运行时间：从零到工作需要多少步骤？目标是 5 分钟以内。
  2. 错误消息：当出现问题时，开发者是否知道什么、为什么以及如何修复？
  3. API/CLI 设计：名称可猜测吗？默认值合理吗？一致吗？
  4. 文档：开发者能否在 2 分钟内找到他们需要的内容？示例是否复制粘贴完整？
  5. 升级路径：开发者能否无恐惧地升级？迁移指南？弃用警告？
  对抗性地思考。像一个正在与 3 个竞争对手评估此产品的开发者一样思考。" -C "$_REPO_ROOT" -s read-only --enable web_search_cached < /dev/null
  _CODEX_EXIT=$?
  if [ "$_CODEX_EXIT" = "124" ]; then
    _gstack_codex_log_event "codex_timeout" "600"
    _gstack_codex_log_hang "autoplan" "0"
    echo "[codex 超过 10 分钟卡住 — 标记为此阶段的 [codex-unavailable] 并仅使用 Claude 子代理继续]"
  fi
  ```
  超时：10 分钟（shell 包装器）+ 12 分钟（Bash 外部门控）。在挂起时，自动降级此阶段的 Codex 声音。

  **Claude DX 子代理**（通过 Agent 工具）：
  "读取位于 <plan_path> 的计划文件。你是独立的 DX 工程师
  审查此计划。你**没有**看到任何先前的审查。评估：
  1. 入门：从零到 hello world 有多少步骤？TTHW 是多少？
  2. API/CLI 人机工程学：命名一致性、合理的默认值、渐进式披露？
  3. 错误处理：每个错误路径是否指定问题 + 原因 + 修复 + 文档链接？
  4. 文档：复制粘贴示例？信息架构？交互式元素？
  5. 逃生舱：开发者能否覆盖每个有主见的默认值？
  对于每个发现：哪里错了，严重程度（关键/高/中），以及修复。"
  无先前阶段上下文 — 子代理必须是真正独立的。

  错误处理：与第 1 阶段相同（两者前台/阻塞，应用降级矩阵）。

- DX 选择：如果 codex 以有效的开发者共情推理与 DX 决策分歧
  → 品味决策。两个模型都同意的范围变更 → 用户挑战。

**必需执行清单（DX）：**

1. 步骤 0（DX 范围评估）：自动检测产品类型。映射开发者旅程。
   初始 DX 完整性评分 0-10。评估 TTHW。

2. 步骤 0.5（双声音）：首先运行 Claude 子代理（前台），然后 Codex。在
   CODEX SAYS（DX — 开发者体验挑战）和 CLAUDE SUBAGENT
   （DX — 独立审查）标题下呈现。产出 DX 共识表：

```
DX 双声音 — 共识表：
═══════════════════════════════════════════════════════════════
  维度                           Claude  Codex  共识
  ──────────────────────────────────── ─────── ─────── ─────────
  1. 入门 < 5 分钟？               —       —      —
  2. API/CLI 命名可猜测？          —       —      —
  3. 错误消息可操作？              —       —      —
  4. 文档可查找且完整？            —       —      —
  5. 升级路径安全？                —       —      —
  6. 开发环境无摩擦？              —       —      —
═══════════════════════════════════════════════════════════════
已确认 = 两者同意。分歧 = 模型不同（→ 品味决策）。
缺少声音 = N/A（未确认）。来自一个声音的单个关键发现 = 无论如何都标记。
```

3. 遍 1-8：从加载的技能运行每个。评分 0-10。自动决策每个问题。
   共识表中的分歧项目 → 在相关遍中提出，带两种视角。

4. DX 记分卡：产出完整的记分卡，所有 8 个维度都评分。

**第 3.5 阶段的强制输出：**
- 开发者旅程映射（9 阶段表）
- 开发者共情叙事（第一人称视角）
- DX 记分卡，所有 8 个维度评分
- DX 实现检查清单
- TTHW 评估，带目标

**第 3.5 阶段完成。** 发出阶段转换摘要：
> **第 3.5 阶段完成。** DX 总体：[N]/10。TTHW：[N] 分钟 → [目标] 分钟。
> Codex：[N 个关注]。Claude 子代理：[N 个问题]。
> 共识：[X/6 已确认，Y 个分歧 → 在门控处暴露]。
> 传递到第 4 阶段（最终门控）。

---

## 决策审计追踪

在每个自动决策后，使用 Edit 将一行追加到计划文件：

```markdown
<!-- 自主决策日志 -->
## 决策审计追踪

| # | 阶段 | 决策 | 分类 | 原则 | 理由 | 拒绝 |
|---|-------|----------|-----------|-----------|----------|
```

通过 Edit 增量写入每个决策一行。这将审计保留在磁盘上，
而不是在对话上下文中累积。

---

## 门控前验证

在呈现最终批准门控之前，验证是否实际
产出了必需的输出。检查计划文件和对话中的每个项目。

**第 1 阶段（CEO）输出：**
- [ ] 前提挑战，命名了具体前提（不仅是"前提已接受"）
- [ ] 所有适用的审查章节有发现或明确的"检查了 X，未标记任何内容"
- [ ] 已产出错误与救援注册表（或注明 N/A 带理由）
- [ ] 已产出失败模式注册表（或注明 N/A 带理由）
- [ ] 已写入"不在范围内"章节
- [ ] 已写入"已存在内容"章节
- [ ] 已写入梦想状态增量
- [ ] 已产出完成摘要
- [ ] 双声音已运行（Codex + Claude 子代理，或注明不可用）
- [ ] 已产出 CEO 共识表

**第 2 阶段（设计）输出 — 仅在检测到 UI 范围时：**
- [ ] 所有 7 个维度已评估，带评分
- [ ] 已识别问题并自动决策
- [ ] 双声音已运行（或注明不可用/跳过并带阶段）
- [ ] 已产出设计石蕊测试记分卡

**第 3 阶段（工程）输出：**
- [ ] 范围挑战，带实际代码分析（不仅是"范围没问题"）
- [ ] 已产出架构 ASCII 图
- [ ] 将代码路径映射到测试覆盖的测试图
- [ ] 在 ~/.gstack/projects/$SLUG/ 写入磁盘的测试计划制品
- [ ] 已写入"不在范围内"章节
- [ ] 已写入"已存在内容"章节
- [ ] 带关键差距评估的失败模式注册表
- [ ] 已产出完成摘要
- [ ] 双声音已运行（Codex + Claude 子代理，或注明不可用）
- [ ] 已产出工程共识表

**第 3.5 阶段（DX）输出 — 仅在检测到 DX 范围时：**
- [ ] 所有 8 个 DX 维度已评估，带评分
- [ ] 已产出开发者旅程映射
- [ ] 已写入开发者共情叙事
- [ ] TTHW 评估，带目标
- [ ] 已产出 DX 实现检查清单
- [ ] 双声音已运行（或注明不可用/跳过并带阶段）
- [ ] 已产出 DX 共识表

**跨阶段：**
- [ ] 已写入跨阶段主题章节

**审计追踪：**
- [ ] 决策审计追踪每个自动决策至少有一行（不为空）

如果上面**任何**复选框缺失，返回并产出缺失的输出。最多 2
次尝试 — 如果重试两次后仍然缺失，带警告进行到门控
注明哪些项目不完整。不要无限循环。

---

## 第 4 阶段：最终批准门控

**在此停止并向用户呈现最终状态。**

呈现为消息，然后使用 AskUserQuestion：

```
## /autoplan 审查完成

### 计划摘要
[1-3 句话摘要]

### 做出的决策：总共 [N] 个（[M] 个自动决策，[K] 个品味选择，[J] 个用户挑战）

### 用户挑战（两个模型都不同意你声明的方向）
[对于每个用户挑战：]
**挑战 [N]：[标题]**（来自 [阶段]）
你说：[用户的原始方向]
两个模型推荐：[变更]
为什么：[推理]
我们可能缺少的：[盲点]
如果我们错了，代价是：[变更的缺点]
[如果是安全/可行性："⚠️ 两个模型都标记这是安全/可行性风险，
不仅仅是偏好。"]

你的决定——除非你明确改变，否则你的原始方向保持不变。

### 你的选择（品味决策）
[对于每个品味决策：]
**选择 [N]：[标题]**（来自 [阶段]）
我推荐 [X] — [原则]。但 [Y] 也是可行的：
  [如果你选择 Y 的 1 句话下游影响]

### 自动决策：[M] 个决策 [参见计划文件中的决策审计追踪]

### 审查评分
- CEO：[摘要]
- CEO 声音：Codex [摘要]，Claude 子代理 [摘要]，共识 [X/6 已确认]
- 设计：[摘要或"跳过，无 UI 范围"]
- 设计声音：Codex [摘要]，Claude 子代理 [摘要]，共识 [X/7 已确认]（或"跳过"）
- 工程：[摘要]
- 工程声音：Codex [摘要]，Claude 子代理 [摘要]，共识 [X/6 已确认]
- DX：[摘要或"跳过，无面向开发者的范围"]
- DX 声音：Codex [摘要]，Claude 子代理 [摘要]，共识 [X/6 已确认]（或"跳过"）

### 跨阶段主题
[对于任何在 2+ 阶段的双声音中独立出现的关注：]
**主题：[主题]** — 在 [第 1 阶段，第 3 阶段] 标记。高置信度信号。
[如果没有主题跨越阶段：]"没有跨阶段主题 — 每个阶段的关注是不同的。"

### 推迟到 TODOS.md
[带理由自动推迟的项目]
```

**认知负荷管理：**
- 0 个用户挑战：跳过"用户挑战"章节
- 0 个品味决策：跳过"你的选择"章节
- 1-7 个品味决策：平列表
- 8+：按阶段分组。添加警告："此计划有异常高的歧义（[N] 个品味决策）。仔细审查。"

AskUserQuestion 选项：
- A）按原样批准（接受所有推荐）
- B）带覆盖批准（指定要更改的品味决策）
- B2）带用户挑战响应批准（接受或拒绝每个挑战）
- C）审问（询问任何具体决策）
- D）修订（计划本身需要变更）
- E）拒绝（重新开始）

**选项处理：**
- A：标记已批准，写入审查日志，建议 /ship
- B：询问哪些覆盖，应用，重新呈现门控
- C：自由回答，重新呈现门控
- D：进行变更，重新运行受影响的阶段（范围→1B，设计→2，测试计划→3，架构→3）。最多 3 个周期。
- E：重新开始

---

## 完成：写入审查日志

在批准时，写入 3 个单独的审查日志条目，以便 /ship 的仪表板识别它们。
用每个审查阶段的实际值替换 TIMESTAMP、STATUS 和 N。
STATUS 如果没有未解决的问题则为"clean"，否则为"issues_open"。

```bash
COMMIT=$(git rev-parse --short HEAD 2>/dev/null)
TIMESTAMP=$(date -u +%Y-%m-%dT%H:%M:%SZ)

.trae/skills/gstack/bin/gstack-review-log '{"skill":"plan-ceo-review","timestamp":"'"$TIMESTAMP"'","status":"STATUS","unresolved":N,"critical_gaps":N,"mode":"SELECTIVE_EXPANSION","via":"autoplan","commit":"'"$COMMIT"'"}'

.trae/skills/gstack/bin/gstack-review-log '{"skill":"plan-eng-review","timestamp":"'"$TIMESTAMP"'","status":"STATUS","unresolved":N,"critical_gaps":N,"issues_found":N,"mode":"FULL_REVIEW","via":"autoplan","commit":"'"$COMMIT"'"}'
```

如果运行了第 2 阶段（UI 范围）：
```bash
.trae/skills/gstack/bin/gstack-review-log '{"skill":"plan-design-review","timestamp":"'"$TIMESTAMP"'","status":"STATUS","unresolved":N,"via":"autoplan","commit":"'"$COMMIT"'"}'
```

如果运行了第 3.5 阶段（DX 范围）：
```bash
.trae/skills/gstack/bin/gstack-review-log '{"skill":"plan-devex-review","timestamp":"'"$TIMESTAMP"'","status":"STATUS","initial_score":N,"overall_score":N,"product_type":"TYPE","tthw_current":"TTHW","tthw_target":"TARGET","unresolved":N,"via":"autoplan","commit":"'"$COMMIT"'"}'
```

双声音日志（每个运行的阶段一个）：
```bash
.trae/skills/gstack/bin/gstack-review-log '{"skill":"autoplan-voices","timestamp":"'"$TIMESTAMP"'","status":"STATUS","source":"SOURCE","phase":"ceo","via":"autoplan","consensus_confirmed":N,"consensus_disagree":N,"commit":"'"$COMMIT"'"}'

.trae/skills/gstack/bin/gstack-review-log '{"skill":"autoplan-voices","timestamp":"'"$TIMESTAMP"'","status":"STATUS","source":"SOURCE","phase":"eng","via":"autoplan","consensus_confirmed":N,"consensus_disagree":N,"commit":"'"$COMMIT"'"}'
```

如果运行了第 2 阶段（UI 范围），也记录：
```bash
.trae/skills/gstack/bin/gstack-review-log '{"skill":"autoplan-voices","timestamp":"'"$TIMESTAMP"'","status":"STATUS","source":"SOURCE","phase":"design","via":"autoplan","consensus_confirmed":N,"consensus_disagree":N,"commit":"'"$COMMIT"'"}'
```

如果运行了第 3.5 阶段（DX 范围），也记录：
```bash
.trae/skills/gstack/bin/gstack-review-log '{"skill":"autoplan-voices","timestamp":"'"$TIMESTAMP"'","status":"STATUS","source":"SOURCE","phase":"dx","via":"autoplan","consensus_confirmed":N,"consensus_disagree":N,"commit":"'"$COMMIT"'"}'
```

SOURCE = "codex+subagent"、"codex-only"、"subagent-only"或"unavailable"。
用表中的实际共识计数替换 N 值。

建议在准备好创建 PR 时的下一步：`/ship`。

---

## 重要规则

- **从不中止。** 用户选择了 /autoplan。尊重该选择。暴露所有品味决策，从不重定向到交互式审查。
- **两个门控。** 不自动决策的 AskUserQuestions 是：（1）第 1 阶段的前提确认，以及（2）用户挑战 — 当两个模型都同意用户声明的方向应该改变时。其他所有都使用 6 个原则自动决策。
- **记录每个决策。** 没有静默的自动决策。每个选择都在审计追踪中获得一行。
- **完整深度意味着完整深度。** 不要压缩或跳过加载技能文件中的章节（第 0 阶段中的跳过列表除外）。"完整深度"意味着：读取章节要求你读取的代码，产出章节要求的输出，识别每个问题，并决定每个问题。章节的一句话摘要不是"完整深度"——它是跳过。如果你发现自己为任何审查章节写少于 3 句话，你可能在压缩。
- **制品是可交付成果。** 测试计划制品、失败模式注册表、错误/救援表、ASCII 图——这些必须在审查完成时存在于磁盘或计划文件中。如果它们不存在，审查是不完整的。
- **顺序。** CEO → 设计 → 工程 → DX。每个阶段建立在前一个之上。
