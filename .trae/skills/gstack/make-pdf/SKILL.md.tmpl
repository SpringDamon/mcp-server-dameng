---
name: make-pdf
preamble-tier: 1
version: 1.0.0
description: |
  将任何 markdown 文件转换为出版级质量的 PDF。1 英寸页边距、
  智能分页、页码、封面、页眉、弯引号和破折号、可点击目录、
  对角线 DRAFT 水印。不是草稿产物——而是完成的产物。当被要求
  "生成 PDF"、"导出为 PDF"、"将此 markdown 转换为 PDF"或
  "生成文档"时使用。(gstack)
voice-triggers:
  - "make this a pdf"
  - "make it a pdf"
  - "export to pdf"
  - "turn this into a pdf"
  - "turn this markdown into a pdf"
  - "generate a pdf"
  - "make a pdf from"
  - "pdf this markdown"
triggers:
  - markdown to pdf
  - generate pdf
  - make pdf
  - export pdf
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion
---

{{PREAMBLE}}

# make-pdf：从 markdown 生成出版级 PDF

将 `.md` 文件转换为看起来像 Faber & Faber 散文的 PDF：1 英寸页边距、
左对齐正文、全局 Helvetica、弯引号和破折号、可选封面和可点击目录、
需要时的对角线 DRAFT 水印。从 PDF 复制粘贴会生成干净的文本，
而不是 "S a i l i n g" 这样的分散字符。

在 Linux 上，安装 `fonts-liberation` 以正确渲染——Helvetica 和 Arial
默认不存在，Liberation Sans 是标准度量兼容替代品。CI 和 Docker 构建
会通过 Dockerfile.ci 自动安装它。

{{MAKE_PDF_SETUP}}

## 核心模式

### 80% 场景 — 备忘录/信函

一条命令，无标志。默认生成带有页眉 + 页码 + CONFIDENTIAL 页脚的干净 PDF。

```bash
$P generate letter.md                 # 写入 /tmp/letter.pdf
$P generate letter.md letter.pdf      # 显式输出路径
```

### 出版模式 — 封面 + 目录 + 章节分隔

```bash
$P generate --cover --toc --author "Garry Tan" --title "On Horizons" \
  essay.md essay.pdf
```

markdown 中每个顶级 H1 开始新页面。使用 `--no-chapter-breaks` 禁用
（适用于碰巧有多个 H1 的备忘录）。

### 草稿阶段水印

```bash
$P generate --watermark DRAFT memo.md draft.pdf
```

每个页面上 10% 不透明度的对角线 DRAFT 水印。当草稿定稿时，
去掉标志并重新生成。

### 通过预览快速迭代

```bash
$P preview essay.md
```

使用相同的打印 CSS 渲染 HTML 并在浏览器中打开。编辑 markdown 时刷新即可。
准备好之前，跳过 PDF 往返过程。

### 无品牌（无 CONFIDENTIAL 页脚）

```bash
$P generate --no-confidential memo.md memo.pdf
```

## 常用标志

```
页面布局：
  --margins <dim>            1in（默认）| 72pt | 2.54cm | 25mm
  --page-size letter|a4|legal

结构：
  --cover                    封面页（标题、作者、日期、细线规则）
  --toc                      可点击目录，带页码
  --no-chapter-breaks        不在每个 H1 处开始新页面

品牌：
  --watermark <text>         对角线水印（"DRAFT"、"CONFIDENTIAL"）
  --header-template <html>   自定义页眉
  --footer-template <html>   自定义页脚（与 --page-numbers 互斥）
  --no-confidential          抑制 CONFIDENTIAL 右侧页脚

输出：
  --page-numbers             "N of M" 页脚（默认开启）
  --tagged                   无障碍 PDF（默认开启）
  --outline                  来自标题的 PDF 书签（默认开启）
  --quiet                    抑制 stderr 上的进度信息
  --verbose                  每阶段计时

网络：
  --allow-network            获取外部图片。默认关闭
                             （阻止追踪像素）。

元数据：
  --title "..."              文档标题（默认为第一个 H1）
  --author "..."             封面和 PDF 元数据的作者
  --date "..."               封面日期（默认为今天）
```

## Claude 何时运行

留意 markdown 转 PDF 的意图。以下任意模式 → 运行 `$P generate`：

- "你能把这个 markdown 转成 PDF 吗"
- "导出为 PDF"
- "把这封信转成 PDF"
- "我需要这篇论文的 PDF"
- "为我打印这个为 PDF"

如果用户打开了 `.md` 文件并说"让它好看一点"，提议
`$P generate --cover --toc` 并在运行前询问。

## 调试

- 输出为空/空白 → 检查浏览守护进程是否运行：`$B status`。
- 复制粘贴时文本碎片化 → highlight.js 输出（第 4 阶段）。使用 `--no-syntax` 重试
  （该标志存在时）。目前，删除代码围栏块并重新生成。
- Paged.js 超时 → markdown 中可能没有标题。去掉 `--toc`。
- 外部图片缺失 → 添加 `--allow-network`（注意你正在授予 markdown 文件
  从其图片 URL 获取的权限）。
- 生成的 PDF 太高/太宽 → `--page-size a4` 或 `--margins 0.75in`。

## 输出约定

```
stdout: /tmp/letter.pdf          ← 仅路径，一行
stderr: Rendering HTML...        ← 进度旋转器（除非 --quiet）
        Generating PDF...
        Done in 1.5s. 43 words · 22KB · /tmp/letter.pdf

exit code: 0 成功 / 1 错误参数 / 2 渲染错误 / 3 Paged.js 超时
           / 4 浏览不可用
```

捕获路径：`PDF=$($P generate letter.md)` — 然后使用 `$PDF`。
