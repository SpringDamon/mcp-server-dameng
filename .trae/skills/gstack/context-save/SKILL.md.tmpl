---
name: context-save
preamble-tier: 2
version: 1.0.0
description: |
  保存工作上下文。捕获 git 状态、已做的决策和剩余工作，
  以便未来的任何会话都能无缝接续。
  使用场景："保存进度"、"保存状态"、"上下文保存"或
  "保存我的工作"。与 /context-restore 配合使用以便日后恢复。
  原名 /checkpoint — 重命名是因为 Claude Code 在当前环境中将 /checkpoint 视为
  原生回退别名，导致此技能被遮蔽。(gstack)
allowed-tools:
  - Bash
  - Read
  - Write
  - Glob
  - Grep
  - AskUserQuestion
triggers:
  - save progress
  - save state
  - save my work
  - context save
---

{{PREAMBLE}}

# /context-save — 保存工作上下文

你是一名** meticulous 记录会话笔记的 Staff 工程师**。你的工作是
捕获完整的工作上下文 — 正在做什么、做了什么决策、还剩什么 — 以便
任何未来的会话（即使在不同的分支或工作区）都能通过 `/context-restore`
无缝接续。

**硬性门控：** 不要实现代码更改。此技能仅捕获状态。

---

## 检测命令

解析用户的输入以确定模式：

- `/context-save` 或 `/context-save <title>` → **保存**
- `/context-save list` → **列表**

如果用户在命令后提供了标题（例如，`/context-save auth refactor`），
使用它作为标题。否则，从当前工作中推断标题。

如果用户输入 `/context-save resume` 或 `/context-save restore`，告诉他们：
"请使用 `/context-restore` — 保存和恢复现在是独立的技能。"

---

## 保存流程

### 步骤 1：收集状态

```bash
{{SLUG_SETUP}}
```

收集当前工作状态：

```bash
echo "=== BRANCH ==="
git rev-parse --abbrev-ref HEAD 2>/dev/null
echo "=== STATUS ==="
git status --short 2>/dev/null
echo "=== DIFF STAT ==="
git diff --stat 2>/dev/null
echo "=== STAGED DIFF STAT ==="
git diff --cached --stat 2>/dev/null
echo "=== RECENT LOG ==="
git log --oneline -10 2>/dev/null
```

### 步骤 2：摘要上下文

使用收集到的状态和对话历史，生成涵盖以下内容的摘要：

1. **正在做什么** — 高层目标或功能
2. **已做决策** — 架构选择、权衡、选择的方法和原因
3. **剩余工作** — 具体的下一步，按优先级排序
4. **备注** — 未来会话需要知道的任何事项（注意事项、阻塞项、
   待决问题、尝试过但不起作用的方法）

如果用户提供了标题，使用它。否则，从正在进行的工作中
推断一个简洁的标题（3-6 个词）。

### 步骤 3：计算会话时长

尝试确定此会话已活跃了多久：

```bash
if [ -n "$_TEL_START" ]; then
  START_EPOCH="$_TEL_START"
elif [ -n "$PPID" ]; then
  START_EPOCH=$(ps -o lstart= -p $PPID 2>/dev/null | xargs -I{} date -jf "%c" "{}" "+%s" 2>/dev/null || echo "")
fi
if [ -n "$START_EPOCH" ]; then
  NOW=$(date +%s)
  DURATION=$((NOW - START_EPOCH))
  echo "SESSION_DURATION_S=$DURATION"
else
  echo "SESSION_DURATION_S=unknown"
fi
```

如果无法确定时长，从保存的文件中省略 `session_duration_s` 字段。

### 步骤 4：写入保存的上下文文件

在 bash 中计算路径（而不是在 LLM 提示中），这样用户提供的标题就无法
将 shell 元字符注入任何后续命令。清理器是白名单：
只有 `a-z 0-9 - .` 能通过。

```bash
{{SLUG_SETUP}}
CHECKPOINT_DIR="${GSTACK_HOME:-$HOME/.gstack}/projects/$SLUG/checkpoints"
mkdir -p "$CHECKPOINT_DIR"
TIMESTAMP=$(date +%Y%m%d-%H%M%S)
# Bash 端的标题清理。运行此块时将原始标题作为 $1 传入。
# 示例：TITLE_RAW="wintermute progress" bash -c '...'
RAW="${TITLE_RAW:-untitled}"
# 小写，合并空白为连字符，仅保留白名单字符，截断长度。
TITLE_SLUG=$(printf '%s' "$RAW" | tr '[:upper:]' '[:lower:]' | tr -s ' \t' '-' | tr -cd 'a-z0-9.-' | cut -c1-60)
TITLE_SLUG="${TITLE_SLUG:-untitled}"
# 防止冲突的文件名：如果 ${TIMESTAMP}-${SLUG}.md 已存在（同一秒内
# 使用相同标题的双重保存），追加一个短随机后缀。文件名只追加
# — 永远不覆盖。
FILE="${CHECKPOINT_DIR}/${TIMESTAMP}-${TITLE_SLUG}.md"
if [ -e "$FILE" ]; then
  SUFFIX=$(LC_ALL=C tr -dc 'a-z0-9' < /dev/urandom 2>/dev/null | head -c 4 || printf '%04x' "$$")
  FILE="${CHECKPOINT_DIR}/${TIMESTAMP}-${TITLE_SLUG}-${SUFFIX}.md"
fi
echo "CHECKPOINT_DIR=$CHECKPOINT_DIR"
echo "TIMESTAMP=$TIMESTAMP"
echo "FILE=$FILE"
```

磁盘上的目录名是 `checkpoints/`（不是 `contexts/`） — 这是一个遗留
路径，保留它是为了让现有保存的文件仍然可加载。用户永远不会看到它。

将文件写入上方打印的 `$FILE` 路径（使用确切字符串 — 不要
在 LLM 层重建它）。

文件格式：

```markdown
---
status: in-progress
branch: {current branch name}
timestamp: {ISO-8601 时间戳，例如 2026-04-18T14:30:00-07:00}
session_duration_s: {计算出的时长，未知则省略}
files_modified:
  - path/to/file1
  - path/to/file2
---

## Working on: {title}

### Summary

{1-3 句话描述高层目标和当前进度}

### Decisions Made

{架构选择、权衡和推理的要点列表}

### Remaining Work

{具体的下一步的编号列表，按优先级排序}

### Notes

{注意事项、阻塞项、待决问题、尝试过但不起作用的方法}
```

`files_modified` 列表来自 `git status --short`（已暂存和未暂存的
修改文件）。使用相对于仓库根目录的路径。

写入后，向用户确认：

```
CONTEXT SAVED
════════════════════════════════════════
标题：    {title}
分支：    {branch}
文件：    {path to saved file}
修改：    {N} 个文件
时长：    {duration 或 "unknown"}
════════════════════════════════════════

稍后使用 /context-restore 恢复。
```

---

## 列表流程

### 步骤 1：收集已保存的上下文

```bash
{{SLUG_SETUP}}
CHECKPOINT_DIR="${GSTACK_HOME:-$HOME/.gstack}/projects/$SLUG/checkpoints"
if [ -d "$CHECKPOINT_DIR" ]; then
  echo "CHECKPOINT_DIR=$CHECKPOINT_DIR"
  # 使用 find + sort 替代 ls -1t：文件名 YYYYMMDD-HHMMSS 前缀是
  # 规范顺序（在复制/rsync 时稳定；mtime 不稳定），且空结果
  # 行为干净（没有文件 → 没有输出，没有"列出 cwd"回退）。
  find "$CHECKPOINT_DIR" -maxdepth 1 -name "*.md" -type f 2>/dev/null | sort -r
else
  echo "NO_CHECKPOINTS"
fi
```

### 步骤 2：显示表格

**默认行为：** 仅显示**当前分支**的已保存上下文。

如果用户传递了 `--all`（例如，`/context-save list --all`），显示
**所有分支**的上下文。

读取每个文件的 frontmatter 以提取 `status`、`branch` 和
`timestamp`。从文件名中解析标题（时间戳之后的部分）。

以表格形式呈现：

```
SAVED CONTEXTS ({branch} 分支)
════════════════════════════════════════
#  日期        标题                     状态
─  ──────────  ───────────────────────  ───────────
1  2026-04-18  auth-refactor            in-progress
2  2026-04-17  api-pagination           completed
3  2026-04-15  db-migration-setup       in-progress
════════════════════════════════════════
```

如果使用了 `--all`，添加 Branch 列：

```
SAVED CONTEXTS (所有分支)
════════════════════════════════════════
#  日期        标题                     分支                状态
─  ──────────  ───────────────────────  ──────────────────  ───────────
1  2026-04-18  auth-refactor            feat/auth           in-progress
2  2026-04-17  api-pagination           main                completed
3  2026-04-15  db-migration-setup       feat/db-migration   in-progress
════════════════════════════════════════
```

如果没有已保存的上下文，告诉用户："还没有保存的上下文。运行
`/context-save` 来保存你当前的工作状态。"

---

## 重要规则

- **永远不要修改代码。** 此技能仅读取状态并写入上下文文件。
- **始终在 frontmatter 中包含分支名称** — 这对跨分支
  `/context-restore` 至关重要。
- **保存的文件只追加。** 永远不覆盖或删除现有文件。每次
  保存创建一个新文件。
- **推断，而非审问。** 使用 git 状态和对话上下文填充文件。
  只在标题确实无法推断时使用 AskUserQuestion。
- **这是 gstack 技能，不是 Claude Code 内置的。** 当用户输入
  `/context-save` 时，通过 Skill 工具调用此技能。旧的 `/checkpoint`
  名称与 Claude Code 的原生 `/rewind` 别名冲突 — 重命名修复了这个问题。
